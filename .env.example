# 本地開發編排（npm run dev）的設定樣板：複製為 .env 後按需取消註解
#（.env 已入 .gitignore，不進版本控制）。
#   cp .env.example .env
#
# 全部鍵皆有預設——沒有 .env 也能 npm run dev（sqlite 全預設落 .dev/）。
# process env 蓋過 .env 檔值：SPECLINK_STORE_DRIVER=memory npm run dev 可一次性覆寫。

# store driver：sqlite（預設）｜serverfs｜postgres｜memory。
#SPECLINK_STORE_DRIVER=sqlite

# store 資料位置（sqlite/serverfs 適用；postgres/memory 忽略）。
# 預設：sqlite → .dev/store.db；serverfs → .dev/store。
#SPECLINK_STORE_PATH=.dev/store.db

# postgres 連線 URL（driver=postgres 時必填，缺值即拒絕啟動）。
# URL 可不含密碼——server 原生以 SPECLINK_POSTGRES_PASSWORD 補全（該鍵直通 server、
# 編排 script 不處理）。dev:reset 只清 .dev/，postgres 資料重置請自行 drop/recreate database。
#SPECLINK_POSTGRES_URL=postgres://speclink@localhost:5432/speclink
#SPECLINK_POSTGRES_PASSWORD=

# identity 資料庫位置（固定 sqlite driver——memory 會在重啟後丟帳號/PAT）。
#SPECLINK_IDENTITY_PATH=.dev/identity.db

# server 綁定埠（--addr 127.0.0.1:{port}）。
#SPECLINK_PORT=8080

# 對外網址：瀏覽器實際使用的 scheme://host:port（同源檢查與 setup 連結都以此為準）。
# 預設與 SPECLINK_PORT 連動：http://localhost:{port}。
#SPECLINK_PUBLIC_URL=http://localhost:8080
