## 1. Apple 簽章與公證的帳號準備（逐步教學）

- [x] [M] 1.1 註冊 Apple Developer Program：用你的 Apple ID 到 developer.apple.com/programs 點 Enroll，選 Individual（個人）、完成 US$99/年 付款；審核通常 1～2 個工作天。通過後到帳號的 Membership details 頁記下 Team ID（10 碼英數字）。完成判準：Membership 頁顯示有效會籍與 Team ID <!-- speclink-task:tsk_01KZTBCFP4FRBRJ895JN423731 -->
- [x] [M] 1.2 建立並匯出 Developer ID Application 憑證：在 macOS 開「鑰匙圈存取」→憑證輔助程式→從憑證授權要求憑證，產生 CSR 檔（填 email、選「儲存到磁碟」）；到 developer.apple.com 的 Certificates 頁按＋，選 Developer ID Application，中介憑證選 G2 Sub-CA（Previous Sub-CA 是給舊 Xcode 的相容選項，且提前於 2027-02-01 過期），上傳 CSR、下載 .cer 並雙擊裝進鑰匙圈；到 apple.com/certificateauthority 下載 Developer ID - G2 中介憑證並雙擊安裝（macOS 內建的是 2012 年的 G1，缺 G2 會讓憑證鏈驗不到根憑證——憑證與私鑰明明成對卻無法簽章）；在鑰匙圈找到該憑證（名稱形如 Developer ID Application: 你的名字 (TeamID)）、展開含私鑰，右鍵匯出為 .p12 並設一組匯出密碼。完成判準：本機有 .p12 檔，且 security find-identity -v -p codesigning 列出該 Developer ID Application 身分、結尾為 1 valid identities found（-v 只列有效身分；不加 -v 的清單會把鏈斷的憑證也算進去，故判準必須用 -v） <!-- speclink-task:tsk_01KZTBCFP4CKVGH37PG4153BZA -->
- [x] [M] 1.3 產生 App 專用密碼並填入六項 GitHub secrets：到 account.apple.com→登入與安全性→App 專用密碼，產生一組（用途填 speclink notarize）；在終端機以 base64 -i 憑證檔名.p12 取得 base64 字串；到 GitHub repo Settings→Secrets and variables→Actions 建立：APPLE_CERTIFICATE（p12 的 base64）、APPLE_CERTIFICATE_PASSWORD（匯出密碼）、APPLE_SIGNING_IDENTITY（鑰匙圈裡的憑證全名）、APPLE_ID（Apple 帳號 email）、APPLE_PASSWORD（App 專用密碼）、APPLE_TEAM_ID（Team ID）。完成判準：Actions secrets 清單出現六項 <!-- speclink-task:tsk_01KZTBCFP44DYRN68JRV061D47 -->

## 2. release 管線：公證接線與簽章組閘門

- [x] 2.1 在 release.yml 的 desktop job 落實 spec「OS 程式碼簽章為可插鑰匙開關」與設計「D1：macOS 公證接在既有簽章開關上，部分設定 fail-closed」——簽章組閘門與公證環境注入：macOS 憑證半組與公證半組六項齊備時，於 Tauri 建置步驟注入公證三項環境變數（Tauri 簽章後自動公證並 staple）；任一平台簽章組「部分存在」時，前置檢查步驟以列出缺項 secrets 名稱的錯誤讓 job 失敗；全缺時維持現行跳過且全綠。驗證：以 bash 條件模擬三種 secrets 組合（全有、全無、缺一）的檢查邏輯正確，workflow 通過 repo 既有的 CI workflow 檢查 <!-- speclink-task:tsk_01KZTBCFP4PHJXM6YB7XKX212Z -->

- [x] 2.2 主 CI 納入 scripts 測試面，落實 spec「CI 執行完整測試」的擴充：ci.yml 的 build-and-smoke job 在其他測試面之前加入 scripts 測試步驟；執行方式須相容 job 釘選的 Node 版本與三平台預設 shell（Node 20 的 --test 不自行展開 glob、Windows runner 預設 shell 也不展開，故指定 shell: bash 讓 glob 先展開）；delivery-gate.test.mjs 補靜態契約斷言守住此步驟與其 shell 宣告。驗證：以 CI 釘選的 Node 版本在 bash 下實跑該指令，全部 scripts 測試檔確實載入且全綠；node --test scripts/*.test.mjs 全綠；actionlint 對 ci.yml 零問題 <!-- speclink-task:tsk_01KZTGD45N5QSPERZGA9VPSRH7 -->

- [x] 2.3 修正 macOS 桌面 bundle 目標缺更新包的發版阻斷缺陷，落實 spec「更新描述檔隨 release 發布」：Tauri 只為 app／appimage／msi／nsis 產出更新包，release.yml 的兩個 macOS matrix 項卻只宣告 dmg——bundler 會跳過更新包並清除中間產物 Speclink.app，收集步驟隨即因找不到 app.tar.gz 而失敗，且全有全無閘門讓整次 Release 落空（此路徑從未執行過，故缺陷未曾顯現）；改為 app,dmg，並於 delivery-gate.test.mjs 補契約斷言：release.yml 每個 bundles 宣告都必須含至少一個 updater-enabled 目標。驗證：本機實跑 tauri build --bundles dmg 重現「no updater-enabled targets were built」警告與缺檔，改用 app,dmg 後 Speclink.app、dmg、Speclink.app.tar.gz 與 .sig 四者齊備；node --test scripts/*.test.mjs 全綠且該斷言對修正前的設定為紅 <!-- speclink-task:tsk_01KZTK69HNP87Z01KAK7KGHA4P -->

- [x] 2.4 修正未知 API 路徑回裸 404 的規格違反（首發實跑於四個 smoke target 同時紅燈）：正典 server-web-console 的 scenario「拼錯 API 不被 SPA fallback 吞掉」要求回 JSON 404，但 `crates/speclink-server/src/assets.rs` 的 spa_fallback 回 `StatusCode::NOT_FOUND` 裸回應（無 body、無 content type），而既有整合測試只斷言狀態碼與「body 不含 SPA shell」，漏掉 JSON 那一半，實作因此長期偏離而未被察覺；改為沿用既有 `{error:{code,message}}` 外殼，並在 `crates/speclink-server/tests/it/web_assets.rs` 新增斷言 content type 為 application/json 且 body 具該外殼的測試。驗證：新測試對修正前的實作為紅；`cargo test -p speclink-server --test it` 全綠且無回歸 <!-- speclink-task:tsk_01KZV8J6HSKC5JPMNDG9M75EPJ -->

- [x] 2.5 補 Linux 桌面建置缺少的 AppImage 打包依賴（首發實跑於 ubuntu-24.04-arm 紅燈，22.04 因映像內建而未顯現）：AppImage 的 bundler 會呼叫 xdg-open，release.yml 的 Linux 依賴步驟只裝 FUSE 未裝 xdg-utils，導致 arm64 桌面 job 在打包最後一步以「xdg-open binary not found」失敗；補裝 xdg-utils，並於 delivery-gate.test.mjs 補契約斷言 Linux 依賴同時含 xdg-utils 與 FUSE。驗證：該斷言對修正前的設定為紅；node --test scripts/*.test.mjs 全綠；actionlint 對 release.yml 無新增問題 <!-- speclink-task:tsk_01KZV8J6P55NNP3XZJX5519PTG -->

- [x] 2.6 修正 scripts 測試面首次在 CI 三平台執行時暴露的三處環境假設（任務 2.2 只在本機驗過，CI 上三平台全紅）：(a) `scripts/remote-docs.test.mjs` 以 `spawnSync('speclink')` 從 PATH 取 CLI——本機因有安裝版而通過、CI 無安裝版即 ENOENT，且比對到的可能是過期安裝版的 help surface；改為解析本 repo 的 `target/debug` 或 `target/release` binary（debug 優先，對齊專案慣例），皆不存在時跳過。(b) install.ps1 測試的跳過條件寫成「無 pwsh 才跳」，但 macOS 與 Linux runner 皆預裝 pwsh，於是拿 Windows 專用腳本去跑 arm64 mac 而撞上「不支援的架構」；改為僅在 Windows 執行。(c) install.sh 測試在 Windows 的 Git Bash 下無法解析無副檔名的假 curl／uname，且 sh 版不出貨到 Windows；改為 Windows 上整組跳過，由 ps1 那組接手。(d) 同檔 help surface 測試的 Usage 斷言寫死 speclink——clap 的 Usage 行用執行檔名，Windows 上是 speclink.exe；regex 改為容許 .exe 尾碼。驗證：node --test scripts/*.test.mjs 本機全綠；三處閘門各自對應到平台而非工具是否存在；Windows CI 的 scripts 測試面綠 <!-- speclink-task:tsk_01KZV91YZHZK33W09KMQRJMAK0 -->

- [x] 2.7 根治 docker 多架構建置的 QEMU SIGILL flaky（v0.1.1 與 v0.1.2 的首跑連兩次紅在 linux/arm64 的 web 階段，重跑才過）：Node/V8 的 JIT 在 QEMU 使用者態模擬下會偶發非法指令（exit 132），而 web 階段的 Vite 產出是架構無關的靜態 dist，根本不需要在目標架構下執行；crates/speclink-server/Dockerfile 的 web 階段改為 FROM --platform=$BUILDPLATFORM（buildx 內建變數，釘在建置機原生架構跑），Rust 編譯階段維持目標架構不動；scripts/delivery-gate.test.mjs 的 Dockerfile 斷言同步改為要求 web 階段帶 --platform=$BUILDPLATFORM。驗證：新斷言對修正前的 Dockerfile 為紅；node --test scripts/delivery-gate.test.mjs 全綠 <!-- speclink-task:tsk_01M03QF7Y2ABCDEFGHJKMNPQRS -->

- [x] 2.8 docker 多架構建置改為原生 runner 分開建，落實設計「D10：docker 多架構改原生 runner 分開建＋manifest 合併」（v0.1.2 實測單 job 逾 35 分鐘，主因 arm64 的 Rust 編譯在 QEMU 模擬下進行）：release.yml 的 docker 建置拆為 per-arch 兩個 job——ubuntu-latest 原生建 linux/amd64、ubuntu-24.04-arm 原生建 linux/arm64，平行執行、push by digest（不打 tag）——再加合併 job 以 docker buildx imagetools create 把兩個 digest 併成版本 tag 與 latest；release job 的全有全無 needs 閘門改指向合併 job；scripts/delivery-gate.test.mjs 契約斷言同步：不得在單一 job 的 platforms 同時宣告 amd64 與 arm64（禁 QEMU 編譯路徑）、兩個 per-arch job 各綁正確 runner、合併 job 存在且 needs 兩者。驗證：新斷言對修改前的 release.yml 為紅；node --test scripts/*.test.mjs 全綠；actionlint 無新增問題；下一個 tag 實跑 docker 面總時長顯著下降且 docker buildx imagetools inspect 顯示映像 manifest 含兩架構 <!-- speclink-task:tsk_01M03T5V8W9X0Y1Z2A3B4C5D6E -->

## 3. Windows SignPath（教學＋接線）

- [ ] [M] 3.1 申請 SignPath 開源簽章並決定首發路徑。**申請時機**：signpath.org/terms 要求專案「已以待簽章的形態發布過」，故本任務 SHALL 排在 v0.1.0 發版（任務 7.1）之後，首發的 Windows 走未簽章後備路徑（設計 D2）。**申請前先備妥三件事**：(a) README 加「Code signing policy」章節——載明由 SignPath.io 簽章、致謝 SignPath Foundation，並列出維護者角色與連結（terms 的硬性要求）；(b) GitHub 帳號啟用兩步驟驗證（SignPath 與原始碼 repo 皆要求 MFA）；(c) terms 的團隊角色（Authors／Reviewers／Approvers）以多人團隊為前提撰寫，本專案為單一維護者——申請時明文說明此情形交其判斷，不自行假設對應方式。**申請**：到 signpath.org/apply 依指引提交（填 GitHub repo URL、MIT 授權、說明由 GitHub Actions 建置）。**過件後**：在 SignPath 後台建立 project 與 signing policy（release-signing），記下 organization ID、project slug、policy slug，並產生 API token，四項填入 GitHub secrets：SIGNPATH_API_TOKEN、SIGNPATH_ORGANIZATION_ID、SIGNPATH_PROJECT_SLUG、SIGNPATH_POLICY_SLUG。**須知的持續成本**：SignPath 每個 release 都要人工登入核准後 CI 才拿得到簽章，非全自動。完成判準（二擇一）：四項 secrets 已填入；或申請未送出／未過件，明確記錄走未簽章後備路徑，不阻塞後續任務 <!-- speclink-task:tsk_01KZTBCFP41VHVM72RT8A1G4JM -->
- [ ] 3.2 SignPath 送簽腳本與 CI config overlay，落實設計「D2：Windows SignPath 以 signCommand 接入，CI 專用 config overlay，PFX 路徑保留為後備」與 spec「OS 程式碼簽章為可插鑰匙開關」的 SignPath 路徑：新增 scripts/signpath-sign.ps1（參數為待簽檔路徑；讀四項環境變數，呼叫 SignPath 建立 signing request、等候完成、下載簽回檔案原地覆蓋；支援 dry-run 只驗參數與 payload 組裝）；release.yml 在 SignPath secrets 齊備時以 tauri build 的 config 合併參數把 bundle.windows.signCommand 指向該腳本，主設定檔 tauri.conf.json 不寫入 signCommand；scripts/signpath-sign.test.mjs 覆蓋參數驗證與 payload 組裝（無 pwsh 環境自動跳過）。驗證：無 SignPath secrets 時本機與 CI 的桌面建置行為不變；node --test scripts/signpath-sign.test.mjs 綠 <!-- speclink-task:tsk_01KZTBCFP45R6858W0DQH8T3A7 -->

## 4. CLI 安裝腳本（測試先行）

- [x] 4.1 先寫 scripts/install.test.mjs 再實作 scripts/install.sh，落實 spec「安裝腳本一行安裝對應平台 CLI」與設計「D3：安裝腳本以 dry-run 與環境變數覆寫換取可測性」：測試覆蓋 dry-run 的五 target 平台對映矩陣與資產 URL 組裝、SPECLINK_INSTALL_VERSION 釘選、SPECLINK_INSTALL_DIR 覆寫、checksum 不符時非零退出且不落檔（以本地 fixture 檔模擬下載內容）。驗證：node --test scripts/install.test.mjs 全綠 <!-- speclink-task:tsk_01KZTBCFP48V3585R0EF1G9NT4 -->
- [x] 4.2 補 PowerShell 版：在 scripts/install.test.mjs 增加 pwsh 案例（無 pwsh 環境自動跳過）再實作 scripts/install.ps1，行為契約與 install.sh 一致（dry-run、版本釘選、目錄覆寫、checksum 驗證、PATH 提示）。驗證：本機（若有 pwsh）與 Windows CI 測試綠 <!-- speclink-task:tsk_01KZTBCFP4JH6T70AREQB6EY0A -->

## 5. Homebrew formula 產生器（測試先行）

- [x] 5.1 先寫 scripts/homebrew-formula.test.mjs 再實作 scripts/homebrew-formula.mjs，落實 spec「Homebrew formula 產生器」與設計「D4：Homebrew formula 由腳本產生，tap repo 手動維護」：輸入 --tag 與 --sums（SHA256SUMS.txt 路徑），stdout 輸出含 on_macos／on_linux × arm64／x86_64 四組 url＋sha256 的 formula；fixture 驗證四組對應正確、缺任一平台條目時非零退出並指出缺項。驗證：node --test scripts/homebrew-formula.test.mjs 全綠 <!-- speclink-task:tsk_01KZTBCFP47BQ62X51YNMZYGQ6 -->

## 6. 文件：安裝通路與誠實化

- [x] 6.1 README（中英）新增安裝區塊並同步 getting-started（中英）安裝節，落實 spec「安裝通路文件與發布狀態誠實化」與設計「D5：文件的「誠實入口」原則落到安裝面」：桌面三平台下載表（macOS dmg／Windows 安裝器／Linux AppImage 與 deb，連到 GitHub Releases）、CLI 一行安裝（curl 安裝腳本與 brew install tap 指令）、cargo install 從原始碼建置移到開發者導向段落；中英結構與事實對等。驗證：對照 user-documentation 規格的安裝區塊 scenario 逐項自查 <!-- speclink-task:tsk_01KZTBCFP4P7SXKR3NV35FKQB2 -->
- [x] 6.2 sdk-node（中英）改標尚未發布至 npm 並改教 repo 內建置載入路徑，全文移除 npm install @speclink/engine 指示；docs/product-status.zh-TW.md 刷新查核日期並在 Node N-API SDK 列的限制欄補「尚未發布至 npm」。驗證：兩語 sdk-node 全文搜尋無 npm install @speclink/engine 字樣 <!-- speclink-task:tsk_01KZTBCFP4REZEMG1Y2NM78MWB -->

## 7. 首發 v0.1.0 演練（逐步教學）

- [x] [M] 7.1 版號確認與推 tag：確認四處版號均為 0.1.0——crates/speclink-cli/Cargo.toml、crates/speclink-core/Cargo.toml、apps/desktop/src-tauri/tauri.conf.json、根 package.json；本 change 全部程式碼任務已合入 main 後，在 main 上執行 git tag v0.1.0 與 git push origin v0.1.0。完成判準：GitHub Actions 的 Release workflow 被 tag 觸發並開始執行 <!-- speclink-task:tsk_01KZTBCFP4CEZ8AVKCN9VG45JA -->
- [x] [M] 7.2 驗證 Release 產物與三平台安裝實測：Release assets 含五 target 的 CLI 壓縮檔（server 壓縮檔不隨 Release 發布，見任務 8.4）、桌面安裝檔（dmg×2、setup.exe、AppImage×2、deb×2）、latest.json 與 SHA256SUMS.txt；macOS 對下載的 dmg 本身先跑 spctl 與 stapler 確認其簽章與公證狀態（Tauri 是否連 dmg 外層一併簽章公證未經實證，此步即為驗證），再掛載取出 app、安裝後以 spctl 評估通過並確認雙擊可開（公證票證已 staple）；Windows 依 3.1 的路徑檢查：有簽章則檢視檔案內容的數位簽章有效、未簽章則確認 README 的 SmartScreen 放行說明可依循；Linux 確認 AppImage 可執行；再以 curl 安裝腳本實測 speclink --version 輸出 0.1.0。完成判準：上述逐項確認完成 <!-- speclink-task:tsk_01KZTBCFP4T4AW1PARCWKX3FZ0 -->
- [x] [M] 7.3 首版 formula 貼入 tap 並實測（tap repo 由任務 9.1 建立）：下載 v0.1.0 的 SHA256SUMS.txt，執行 node scripts/homebrew-formula.mjs --tag v0.1.0 --sums 該檔路徑，輸出存為 tap repo 的 Formula/speclink.rb 並 push；以 brew install 自己帳號/tap/speclink 實測（測後若日常使用 ~/.local/bin 的安裝版，brew uninstall 避免 PATH 出現兩份）。完成判準：brew 安裝後 speclink --version 輸出 0.1.0 <!-- speclink-task:tsk_01KZTBCFP4HJXJ06QTKPWQH7RQ -->

## 8. 發布面調整：server 退出 assets 與下載指南

- [x] 8.1 先寫 scripts/release-notes.test.mjs 再實作 scripts/release-notes.mjs，落實 spec「Release 說明含下載指南」與設計「D7：Release 下載指南由腳本產生前置於說明，v0.1.0 就地修正不重發 tag」：輸入 --tag（vX.Y.Z 格式，不符即非零退出且 stdout 無輸出），stdout 輸出下載指南 markdown——三平台安裝檔對照表（Speclink_X.Y.Z_aarch64.dmg／Speclink_X.Y.Z_x64.dmg、Speclink_X.Y.Z_x64-setup.exe、Speclink_X.Y.Z_amd64.AppImage／Speclink_X.Y.Z_aarch64.AppImage、Speclink_X.Y.Z_amd64.deb／Speclink_X.Y.Z_arm64.deb，檔名與 release 管線資產命名一致）、CLI 安裝腳本一行指令與 brew install 指令、註明 .app.tar.gz 與全部 .sig 為自動更新機制使用毋須手動下載。測試覆蓋：檔名對齊版號、.sig 註記存在、tag 格式 fail-closed。驗證：node --test scripts/release-notes.test.mjs 全綠 <!-- speclink-task:tsk_01M03CD1JD1JMWJK1G50MJVBEK -->
- [x] 8.2 release.yml 移除 server 打包並掛下載指南，落實 spec「release 產物含 server 與部署文件」與「Release 產出三平台桌面安裝檔」（修改後——assets 無 server 壓縮檔）以及設計「D6：server 發布通路收斂為 Docker，release 保留 server 建置與冒煙作品質閘門」：build job 的 Package 步驟只打包 speclink（server 的建置與無 dist 冒煙步驟保留不動）；release job 於建 Release 前執行 node scripts/release-notes.mjs --tag 落檔 notes.md，action-gh-release 補 body_path 指向該檔（generate_release_notes 保留）；scripts/delivery-gate.test.mjs 補契約斷言：Package 步驟不含 speclink-server 字樣、release job 帶 body_path 且其值指向 release-notes 產生器的輸出檔。驗證：新斷言對修改前的 release.yml 為紅；node --test scripts/*.test.mjs 全綠；actionlint 對 release.yml 無新增問題 <!-- speclink-task:tsk_01M03CD1JDG1E0N1WKS2C60ATC -->
- [x] 8.3 部署文件對齊 server 通路收斂，落實 spec「release 產物含 server 與部署文件」的文件半部：docs/server-deployment.zh-TW.md 開頭形態總覽改為官方形態（Docker 直跑、SQLite compose、PostgreSQL compose；任務 10.4 再補 npx 成四種）＋從原始碼建置的替代路徑；native binary 節改教 npm ci → npm run build -w apps/server-web → cargo build --release -p speclink-server 的建置順序；回退章節的 binary 路徑改為重建上一版 tag 或改用上一版 Docker image tag；全文移除 Release 的 server 壓縮檔下載指示。驗證：全文搜尋無 speclink-server- 開頭的資產下載指示；node --test scripts/delivery-gate.test.mjs 綠（文件建置順序斷言仍過）<!-- speclink-task:tsk_01M03CD1JDMHYD1M2Y66KE2NGX -->
- [x] 8.4 v0.1.0 就地修正（設計 D7 的資產層修正，tag 與 commit 不動）：以 gh release delete-asset 移除五個 speclink-server-* 資產；下載現行 SHA256SUMS.txt、過濾掉 server 條目（其餘條目位元組不變）後以 gh release upload --clobber 覆蓋；執行 node scripts/release-notes.mjs --tag v0.1.0 產出指南，前置於現有 Release 說明並以 gh release edit 更新。驗證：gh release view 的 assets 無 speclink-server-*；重新下載的 SHA256SUMS.txt 無 server 條目且 CLI 條目與先前逐字相同；Release 頁說明開頭為下載指南；SPECLINK_INSTALL_DIR 指向暫存目錄實跑 scripts/install.sh 對 v0.1.0 安裝成功（checksum 驗證仍過）<!-- speclink-task:tsk_01M03CD1JDQXCR9BQV0PGSXHE3 -->

- [x] 8.5 下載指南補 server 一行啟動節，落實 spec「Release 說明含下載指南」（修訂後——含 server npx 與 Docker 一行）：scripts/release-notes.mjs 於 CLI 安裝節之後補「Server 一行啟動」節，寫法比照 CLI 節——npx @speclink/server 指令與 docker run ghcr.io/momochenisme/speclink-server 一行（與部署文件教法一致）；scripts/release-notes.test.mjs 補斷言：npx 指令與 ghcr image 名存在於輸出。完成後以產生器重產 v0.1.2 的指南段並以 gh release edit 更新現行 Release 說明（changelog 段保留）。驗證：node --test scripts/release-notes.test.mjs 全綠（新斷言對修改前為紅）；gh release view v0.1.2 說明含 npx 節 <!-- speclink-task:tsk_01M03V6W7X8Y9Z0A1B2C3D4E5F -->

## 9. Homebrew tap 自動推送

- [x] [M] 9.1 建立 tap repo 與推送憑證（從零到 secret 就位的完整教學）：**(a) 建 repo**——在 GitHub 建公開 repo `homebrew-tap`（名字必須完全一致，Homebrew 靠這個慣例把 `brew install momochenisme/tap/speclink` 對應到它；可用 gh repo create homebrew-tap --public --description "Homebrew tap for Speclink" 或網頁建立），repo 內建一個 `Formula/` 目錄（放個 README 佔位即可）。**(b) 產 PAT**——GitHub 網頁右上頭像 → Settings → 最下方 Developer settings → Personal access tokens → Fine-grained tokens → Generate new token：名稱取 speclink-tap-push、Resource owner 選自己、效期建議一年（記下到期日）、Repository access 選 Only select repositories 並只勾 homebrew-tap、Repository permissions 只把 Contents 設為 Read and write，其餘全部 No access，產生後複製 token（只顯示一次）。**(c) 設 secret**——在 speclink repo 執行 gh secret set TAP_PUSH_TOKEN 貼上 token。完成判準：gh secret list 可見 TAP_PUSH_TOKEN；homebrew-tap repo 存在且含 Formula/ 目錄 <!-- speclink-task:tsk_01M03D001A6Y6M9G4W1PT1V6B0 -->
- [x] 9.2 release.yml 接 tap 自動推送，落實 spec「Formula 隨發版自動推送 tap」與設計「D8：Homebrew tap 由 release 管線自動推送，單一 secret 為開關」：新增 tap-publish job（needs: release、if 條件判 TAP_PUSH_TOKEN 存在）——checkout 後從 Release 下載該版 SHA256SUMS.txt、node scripts/homebrew-formula.mjs --tag 產出 formula、以 GitHub Contents API（gh api PUT /repos/<owner>/homebrew-tap/contents/Formula/speclink.rb，帶 TAP_PUSH_TOKEN、commit 訊息含 tag、附現存檔案 sha 以支援更新）推送；scripts/delivery-gate.test.mjs 補契約斷言：tap-publish job 存在且 needs release、if 條件引用 TAP_PUSH_TOKEN、步驟呼叫 homebrew-formula.mjs 且目標路徑為 Formula/speclink.rb。驗證：新斷言對修改前的 release.yml 為紅；node --test scripts/*.test.mjs 全綠；actionlint 無新增問題 <!-- speclink-task:tsk_01M03D001AV3F1JEQ9NXA0K1ZS -->

## 10. server 的 npm 通路（npx 一行啟動）

- [x] [M] 10.1 npm 帳號、scope 與發布 token（從零到 secret 就位的完整教學）：**(a) 註冊**——到 npmjs.com 免費註冊帳號並啟用 2FA（發布必要）。**(b) 定 scope**——檢查 npmjs.com/org/speclink 是否已被他人占用：可用就建立免費 organization「speclink」（公開套件免費），被占用則改用替代 scope（例如 @momochenisme）並把決定記錄在本任務完成註記裡（後續任務的套件名以此為準；經查 @speclink/server 套件名目前未被發布）。**(c) 產 token**——npm 網站 Access Tokens → Generate New Token → Granular Access Token：只授權該 scope 的 Read and write packages、效期建議一年（記下到期日）。**(d) 設 secret**——gh secret set NPM_TOKEN 貼上。完成判準：gh secret list 可見 NPM_TOKEN；scope 決定已記錄 <!-- speclink-task:tsk_01M03D001AQZT1T3DG1JVXK4RH -->
- [x] 10.2 先寫測試再實作 npm launcher 與套件物化腳本，落實 spec「npm 套件一行啟動 server」與設計「D9：server 的 npm 通路——esbuild 式平台套件，launcher 插值組態、Rust 不動」：先寫 scripts/npm-server-launcher.test.mjs（os/cpu → 平台子套件名對映矩陣、零參數時依 SPECLINK_STORE／SPECLINK_DATA_DIR／SPECLINK_PUBLIC_URL／SPECLINK_PORT 產生的組態 YAML 內容正確且寫入資料目錄、sqlite 為預設 driver、SPECLINK_STORE=postgres 缺 SPECLINK_POSTGRES_URL 時非零退出點名缺項、帶 --config／SPECLINK_CONFIG／子命令時純透傳不產組態、以假 binary 驗證參數與 exit code 透傳、平台無對應子套件時可讀錯誤點名平台）與 scripts/npm-server-package.test.mjs（給定 --version 與 binary fixture，物化出主套件與五平台子套件，各 package.json 的 name／version／os／cpu／optionalDependencies／bin 欄位正確、--scope 覆寫生效），再實作 packages/server-npm（主套件 package.json 與 bin/speclink-server.mjs launcher）與 scripts/npm-server-package.mjs。驗證：node --test 兩個測試檔全綠 <!-- speclink-task:tsk_01M03D001A2N0MHYW0BR4KE97W -->
- [x] 10.3 release.yml 接 npm 發布，落實 spec「npm 套件一行啟動 server」的發布半部與設計 D9：build job 把五平台 server binary 以獨立 artifact（命名 server-<target>）上傳；新增 npm-publish job（needs: release、if 條件判 NPM_TOKEN 存在）——下載五個 server-* artifacts、node scripts/npm-server-package.mjs --version 取自 tag 物化套件、對主套件與五平台子套件依序 npm publish --access public；release job 的資產收集 SHALL 不含 server-* artifacts（download-artifact 以 pattern 圈定非 server 的 artifacts）；scripts/delivery-gate.test.mjs 補契約斷言：server binary artifact 命名契約、npm-publish job gated 於 NPM_TOKEN、release job 的下載 pattern 排除 server-*。驗證：新斷言對修改前的 release.yml 為紅；node --test scripts/*.test.mjs 全綠；actionlint 無新增問題 <!-- speclink-task:tsk_01M03D001AHK7GQM9BJ7C3JX2E -->
- [x] 10.4 npm 通路文件：docs/server-deployment.zh-TW.md 快速啟動節補 npx 一行啟動（形態總覽從 8.3 的三種補為四種官方形態）＋環境變數表（SPECLINK_STORE／SPECLINK_DATA_DIR／SPECLINK_PUBLIC_URL／SPECLINK_PORT／SPECLINK_POSTGRES_URL／SPECLINK_CONFIG，各附預設值與白話說明）；README 與 README.en 的 server 段補 npx 一行指令，中英事實對等。驗證：node --test scripts/delivery-gate.test.mjs 綠（文件斷言仍過）；中英對照自查 <!-- speclink-task:tsk_01M03D001AXQ2R4YT7B1SQ8FJN -->


## 11. 安裝體驗：安裝器語系與 macOS CLI 自動化

- [x] 11.1 NSIS 安裝器繁中化與 Speclink 圖示，落實 spec「Release 產出三平台桌面安裝檔」（修訂後——安裝器語系與圖示）與設計「D11：NSIS 安裝器語系跟隨系統、圖示用現有 logo」：apps/desktop/src-tauri/tauri.conf.json 的 bundle.windows.nsis 補 languages 為 TradChinese 與 English 兩項（NSIS 於系統語言在清單內時直接採用、否則落回英文）與 installerIcon 指向 icons/icon.ico；scripts/delivery-gate.test.mjs 補契約斷言：nsis.languages 含 TradChinese 與 English、installerIcon 指向 icon.ico。驗證：新斷言對修改前的設定為紅；node --test scripts/delivery-gate.test.mjs 全綠；tauri 設定 JSON 可解析 <!-- speclink-task:tsk_01M03W1A2B3C4D5E6F7G8H9JKM -->
- [x] 11.2 macOS CLI 啟動自動佈署與 PATH 冪等追加，落實 spec「安裝 CLI 指令到 PATH」（修訂後）與設計「D12：macOS CLI 啟動自動佈署，PATH 以 ~/.zprofile 冪等追加」：apps/desktop/src/core/cliInstall.ts 的 needsRedeploy 擴為 macOS（未安裝與版本不符皆自動佈署、AppImage 維持版本不符）並新增 zprofile 追加決策純函式（輸入 PATH 涵蓋狀態與 ~/.zprofile 內容，輸出 append 行或 none；識別註解冪等）；adapter 與 app 啟動流程接上自動佈署與 zprofile 寫入（佈署失敗不阻斷啟動）；apps/desktop/src/__tests__/cliInstall.test.ts 先補紅測試：macOS 未安裝→自動佈署決策、版本不符→重佈署、zprofile 已有識別行→不重複追加、已在 PATH→不寫入。驗證：新測試對修改前為紅；npm test -w apps/desktop 全綠 <!-- speclink-task:tsk_01M03W1A2B3C4D5E6F7G8H9JKN -->
