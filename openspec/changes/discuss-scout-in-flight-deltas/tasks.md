## 1. 技能文字（crates/engine/speclink-core/assets/skills/discuss.md）

- [x] 1.1 依 design「D1 delta 命中以 speclink list --json 的 deltaCapabilities 比對」與「D2 時間盒：只讀標題與標記，至多 3 份」改寫 crates/engine/speclink-core/assets/skills/discuss.md 的 Step 2 Canon pass 段落，滿足 spec 需求「事實與決策分診及逐節點查證」的「規格段納入進行中 delta 規格」場景：Canon pass 候選依名稱命中關鍵字數多者優先、同數依指令列出順序；命中 capability 名後以開場已執行的 speclink list --json 各變更項的 deltaCapabilities 比對（不以 status 過濾、不逐一執行 speclink show），含該 capability 者列為「進行中 delta 命中」並記變更名；以 speclink artifact cat specs/<capability> --change <name> 只讀該 delta 的 Requirement 標題與 ADDED／MODIFIED／REMOVED／RENAMED 標記、至多 3 份（依候選順序取前 3 個命中）、不讀全文，清單項帶 worktree 物件時於其 worktree.path 下執行 artifact cat；正式規格零命中、沒有進行中變更或沒有變更的 deltaCapabilities 含命中的 capability 時靜默跳過；不得指示直接開啟 openspec/changes/ 路徑（verb-contract）；Step 2 標題與三段順序句仍為「canon, then prior discussions, then code」，規格段措辭涵蓋正式規格與進行中 delta，不出現獨立的「in-flight changes」段。驗證：內容審閱逐條對照 spec「規格段納入進行中 delta 規格」與「開場 scout 維持淺掃」兩個 scenario 的 THEN；`grep -c "deltaCapabilities" crates/engine/speclink-core/assets/skills/discuss.md` 至少 1；`cargo test -p speclink-core --test it skill_verbization` 綠；2.1 的 golden 測試綠。 <!-- speclink-task:tsk_01M2M1VWC4XYS1SRNCM0KKN8VC -->
- [x] 1.2 依 design「D3 呈現：既有兩類對照附加標記，不加第五類」改寫同檔 Presenting assumptions 的 Canon triage 表，滿足 spec 需求「正式規格接地與三分對照」的「進行中 delta 命中時附加將改動標記」場景：表維持四列；「Covered by canon」與「Conflicts with canon」兩列的做法欄加上 delta 命中時加進行中標記的規定；表下 In-flight marker 段定義標記「（進行中變更 <name> 將改動：<Requirement 名>）」寫在對照類別標籤後、Requirement 名逗號分隔、超出時間盒未讀的命中不帶 Requirement 名、Evidence 併列正式規格與該 delta，並含「不得以此擋下討論方向」字句；紀律句「the user's requirement is the goal; the canon and the prior discussions are evidence, not a verdict」不變。驗證：內容審閱確認表格列數為 4、前兩列含標記規定；2.1 的 golden 測試綠。 <!-- speclink-task:tsk_01M2M1VWC4PEG5VMV9QCPXT33N -->
- [x] 1.3 依 design「D4 Context 段不加新行，擴寫既有相關變更句」改寫同檔 Context 範本（speclink discuss context 的 heredoc 說明）與「The scout exists to ground the discussion…」段的 related changes/specs 句，滿足 spec 需求「Context 相關變更句列出進行中 delta」的「Context 記錄進行中 delta 命中」與「零 delta 命中時 Context 照舊」兩個 scenario：相關變更句規定以 `<name>: <capability>` 形式、逗號分隔列出 delta 命中，零命中時不加空標記；`Prior discussions: <slug list>` 行逐字不變；不新增獨立標記行。驗證：`grep -c "Prior discussions: <slug list>" crates/engine/speclink-core/assets/skills/discuss.md` 仍為 1；內容審閱確認 Context 範本無新增標記行；2.1 的 golden 測試綠。 <!-- speclink-task:tsk_01M2M1VWC4A5TJDY2JGK2FP6J3 -->

## 2. asset 三連動與再生

- [x] 2.1 依 design「D5 asset 三連動與版號對撞處理」把 crates/engine/speclink-core/src/workspace/init.rs 的 ASSET_VERSION 自當時 main 的值升一個 minor 版（本提案時 main 為 v1.36.0、add-plan-handoff-after-archive 排定升 v1.37.0，預期本變更升 v1.38.0），再生 crates/engine/speclink-core/tests/golden/ 下 claude.snapshot.md、claude-worktree.snapshot.md、codex.snapshot.md、neutral-cli.snapshot.md、neutral-tool-call.snapshot.md 五份快照與 assets.lock，使渲染產物含 1.1 至 1.3 的新內容。驗證：`cargo test -p speclink-core --test it render_golden` 綠；assets.lock 鎖定測試綠（同一 `cargo test -p speclink-core --test it` 內）；`grep -c "deltaCapabilities" crates/engine/speclink-core/tests/golden/claude.snapshot.md` 與 codex.snapshot.md 各至少 1。 <!-- speclink-task:tsk_01M2M1VWC427EPVSWEG4PMQZ6Q -->
- [x] 2.2 先 `cargo build -p speclink-cli` 再於本 repo 執行 `speclink update`，再生 .claude/skills 與 .agents/skills 下的 SKILL.md，使本 repo 的 speclink-discuss 技能檔含新偵察規定且 frontmatter 版號為 2.1 的新值。驗證：`grep -c "deltaCapabilities" .claude/skills/speclink-discuss/SKILL.md` 至少 1；`grep "version:" .claude/skills/speclink-discuss/SKILL.md` 為 2.1 的新值；以 git status 盤點再生的 SKILL.md 檔案清單，全部納入同批 commit。 <!-- speclink-task:tsk_01M2M1VWC41HV2KH7HJPC9E994 -->

## 3. 引擎：speclink list --json 曝 deltaCapabilities

- [x] 3.1 依 design「D1 delta 命中以 speclink list --json 的 deltaCapabilities 比對」在 crates/protocol/speclink-protocol/src/query.rs 的 ChangeSummary 增 delta_capabilities（camelCase、serde default、空清單省略），並補齊 apps/desktop/src-tauri 內以 struct literal 建構 ChangeSummary 的測試 helper，滿足 spec 需求「變更清單的 delta capability 欄位」的「delta capability 欄位序列化與缺席省略」場景。驗證：protocol 序列化測試（有值上鍵、空值省略、無此鍵的舊 payload 反序列化得空清單）先紅後綠；`cargo test -p speclink-desktop` 綠。 <!-- speclink-task:tsk_01M2NADTSGTF5G1NXWNAA7AXTR -->
- [x] 3.2 依同一決策在 crates/engine/speclink-core/src/lifecycle/listing.rs 的 ListChangeJson 增 delta_capabilities（serde rename deltaCapabilities、空清單省略），changes_json_with 以 Store 的 delta_capabilities 填值；crates/adapters/speclink-cli/src/verbs/query.rs 的 to_list_change_json 映射 ChangeSummary 的同名欄位，滿足 spec 需求「list --json 曝變更的 delta capability」的兩個場景。驗證：speclink-cli 整合測試——fs 模式帶 delta 規格的變更項含 deltaCapabilities（升冪）、沒有 delta 的變更項無此鍵、人眼 speclink list 不含該欄位；remote_read_path 的 list 同形測試在 mock 清單項帶 deltaCapabilities 時 remote 輸出含同值且與 fs 雙胞胎同鍵——先紅後綠。 <!-- speclink-task:tsk_01M2NAN1SMFNX2Q16PVVY0NC4R -->
- [x] 3.3 在 crates/host/speclink-server/src/api/routes.rs 的 change_summary 把清單項的 delta_capabilities 映射上 wire，滿足 spec 需求「變更清單回應攜帶 delta capability 欄位」。驗證：speclink-server 的 GET /changes 測試（帶 delta 規格的變更項含 deltaCapabilities、沒有 delta 者無此鍵）先紅後綠。 <!-- speclink-task:tsk_01M2NAVNJS0TAWCPKSFJG7JHVQ -->

## 4. 收尾

- [x] 4.1 端到端確認與回歸：`cargo test -p speclink-core --test it` 全綠（含 render_golden 與 assets.lock 鎖定）；`cargo test -p speclink-cli --test it` 全綠（含 list --json 的 deltaCapabilities 與 remote 同形、既有 discuss 系列）；`cargo test -p speclink-protocol`、`cargo test -p speclink-server --test it`、`cargo test -p speclink-desktop` 全綠（server 與 desktop 先補 sidecar 與兩份 dist）；Node SDK 的 vitest 全綠（SDK 與 CLI 的 list --json 比對）；`speclink validate discuss-scout-in-flight-deltas` 通過；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 全綠（詞彙守門與連結檢查）。 <!-- speclink-task:tsk_01M2M1VWC4JEE7PYAT9TP5WZ0N -->
