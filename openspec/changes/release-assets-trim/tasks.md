## 1. 發版 workflow 契約先紅（D1、D2、D7、D8）

- [x] 1.1 在 scripts/release/delivery-gate.test.mjs 改寫與新增對 .github/workflows/release.yml 的靜態斷言，鎖住「Release 產出三平台桌面安裝檔」的新 asset 集合：build job 沒有 Package 步驟且 cli-<target> artifact 的 path 為 raw binary；release job 不下載 cli-*；dist 只合併 desktop-*，updater-* 只以目錄結構下載，複製更新包進 dist 的步驟排除 *.sig；沒有產生 SHA256SUMS 的步驟；Linux 兩列 bundles 為 appimage；macOS 只有 target universal-apple-darwin 一列且 bundles 含 app。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，這些新斷言在現況紅、其餘綠。 <!-- speclink-task:tsk_01M2EVCZX2BZ7W6VZFTQBAW7HT -->
- [x] 1.2 在同一測試檔新增對「CLI 以 npm 套件發布」與「Formula 隨發版自動推送 tap」的 job 斷言：存在 cli-npm-publish job（needs release、NPM_TOKEN 閘門、呼叫 scripts/npm/npm-cli-package.mjs、npm pack、npm view、npm publish --access public、上傳 cli-npm-sums artifact、輸出 published）；tap-publish 的 needs 含 cli-npm-publish、閘門同時檢查 TAP_PUSH_TOKEN 與 published 輸出、下載 cli-npm-sums artifact、不再以 gh release download 抓 SHA256SUMS.txt。驗證：同 1.1 的指令，新斷言紅。 <!-- speclink-task:tsk_01M2EVCZX2Y20XS3W9KMJP50P9 -->

## 2. release.yml 改造（D1、D2、D7、D8）

- [x] 2.1 .github/workflows/release.yml 的 build job：移除 Package 步驟，cli-<target> artifact 改上傳 target/<triple>/release/speclink（Windows 帶 .exe）的 raw binary，與 server-<target> 同形；server 相關步驟不動。驗證：1.1 對 build job 的斷言轉綠。 <!-- speclink-task:tsk_01M2EVCZX2AWC5ECHV1D4SCHV1 -->
- [x] 2.2 desktop job：macOS 兩列合為一列（target universal-apple-darwin、platform darwin-universal、bundles app,dmg、新 matrix 欄位 rust_targets 供 toolchain 步驟同時安裝 aarch64-apple-darwin 與 x86_64-apple-darwin，其餘列 rust_targets 等於 target）；Linux 兩列 bundles 改為 appimage；收集步驟的 darwin 分支改為 darwin-universal 目錄與 Speclink_<版本>_universal.app.tar.gz 檔名、刪除 deb 複製、.sig 只進 updater/。驗證：1.1 對 desktop job 的斷言轉綠；scripts/release/delivery-gate.test.mjs 既有的「每平台至少一個 updater-enabled bundle」斷言仍綠。 <!-- speclink-task:tsk_01M2EVCZX2ZTJ148RD1AQTNJQ8 -->
- [x] 2.3 release job：只把 desktop-* 合併進 dist，updater-* 保留目錄結構下載到 updater/，組裝 latest.json 後把 updater/ 底下非 .sig 的檔複製進 dist，移除 Checksums 步驟；softprops 上傳 dist/* 與下載指南前置不變。驗證：1.1 對 release job 的斷言轉綠。 <!-- speclink-task:tsk_01M2EVCZX2W5QW566REXK7S0NG -->
- [x] 2.4 新增 cli-npm-publish job（needs release）：NPM_TOKEN 缺席時各步跳過、job 綠、輸出 published=false；存在時下載 cli-*、以 scripts/npm/npm-cli-package.mjs 物化、對每個套件 npm pack、對 tgz 算 sha256 寫成 cli-npm-sums.txt 並上傳為 artifact cli-npm-sums、先發五個平台子套件再發主套件（每份先 npm view 查同版，已在即跳過）、比照 engine-npm-publish 輪詢主套件在 registry 可見、輸出 published=true。驗證：1.2 的 cli-npm-publish 斷言轉綠。 <!-- speclink-task:tsk_01M2EVCZX28Z3H1VESACC9VRY0 -->
- [x] 2.5 tap-publish job 改為 needs [release, cli-npm-publish]，閘門為 TAP_PUSH_TOKEN 存在且 needs.cli-npm-publish.outputs.published 為 true，下載 cli-npm-sums artifact 後以 scripts/release/homebrew-formula.mjs --tag 與 --sums 產出 formula，推送步驟不變；兩條件任一不成立即跳過且 job 綠。驗證：1.2 的 tap-publish 斷言轉綠；node --test scripts/*.test.mjs scripts/*/*.test.mjs 全綠。 <!-- speclink-task:tsk_01M2EVCZX2WMAGY4DQNG0DTY49 -->

## 3. latest.json 組裝：darwin 兩鍵共用 universal 更新包（D3）

- [x] 3.1 改寫 scripts/release/release-latest-json.test.mjs 鎖住「更新描述檔隨 release 發布」的新契約：輸入目錄 darwin-universal、windows-x86_64、linux-x86_64（linux-aarch64 可選）時，輸出的 darwin-aarch64 與 darwin-x86_64 兩鍵 url 與 signature 相同且指向 Speclink_<版本>_universal.app.tar.gz；缺 darwin-universal 目錄或該目錄缺 .sig 時非零結束、不寫檔並點名；linux-aarch64 存在時一併收錄。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，新斷言紅。 <!-- speclink-task:tsk_01M2EVCZX2X5C9PXEQXCP15NJK -->
- [x] 3.2 scripts/release/release-latest-json.mjs 的目錄契約改為「更新包目錄 → 一或多個平台鍵」的對映表（darwin-universal → darwin-aarch64 與 darwin-x86_64），必要與可選目錄的 fail-closed 行為不變，檔頭註解同步。驗證：3.1 轉綠。 <!-- speclink-task:tsk_01M2EVCZX2KDFQ4H33835ETAMK -->

## 4. universal sidecar（D2）

- [x] 4.1 在 scripts/desktop/desktop-sidecar.test.mjs 新增斷言：--target universal-apple-darwin 解析出的建置計畫是對 aarch64-apple-darwin 與 x86_64-apple-darwin 各一次 cargo build，輸出路徑為 apps/desktop/src-tauri/binaries/speclink-universal-apple-darwin（以 lipo -create 合成）；其他 target 的計畫與路徑不變（既有測試不改）。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，新斷言紅。 <!-- speclink-task:tsk_01M2EVCZX2FJR919NJVAP6790Q -->
- [x] 4.2 scripts/desktop/desktop-sidecar.mjs 把「要跑哪些 cargo build、輸出到哪、要不要 lipo」抽成可測的純函式並實作 universal 分支：兩次 cargo build 後以 lipo -create 合成到暫存檔，再以既有的 shouldCopy 比對決定是否覆蓋目的檔；lipo 缺席或非零時點名失敗。驗證：4.1 轉綠；在 macOS 本機執行 node scripts/desktop/desktop-sidecar.mjs --target universal-apple-darwin 後，lipo -info apps/desktop/src-tauri/binaries/speclink-universal-apple-darwin 列出 arm64 與 x86_64；「Release 產出三平台桌面安裝檔」要求的 macOS 內含 universal CLI 由此保證。 <!-- speclink-task:tsk_01M2EVCZX2YWE78DZ7JFPJD89T -->

## 5. @speclink/cli npm 套件（D4）

- [x] 5.1 新增 scripts/npm/npm-cli-package.test.mjs 鎖住「CLI 以 npm 套件發布」的物化契約：輸入 --version X.Y.Z --binaries <dir> --out <dir>，輸出主套件（name @speclink/cli、version、bin.speclink 為 bin/speclink、scripts.postinstall、五組同版 optionalDependencies、無 private）與五個子套件（name、version、os、cpu、內含 speclink 或 speclink.exe 且 0755）；缺任一 cli-<target> binary 時非零結束並點名且不產生任何套件；版本不符 X.Y.Z 時非零結束。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，新測試紅。 <!-- speclink-task:tsk_01M2EVCZX24S8M5E6SK84NZVCB -->
- [x] 5.2 新增 packages/cli-npm/package.json（private 擋誤發布、bin、scripts.postinstall、files）與 scripts/npm/npm-cli-package.mjs（比照 scripts/npm/npm-server-package.mjs：TARGETS 對映 cli-<target> artifact → cli-<os>-<cpu> 子套件，主套件副本去 private、蓋版本、補 optionalDependencies、複製 bin/speclink 與 postinstall.mjs）。驗證：5.1 轉綠。 <!-- speclink-task:tsk_01M2EVCZX2MX0B2AP1BQ3NT4BD -->
- [x] 5.3 新增 scripts/npm/npm-cli-launcher.test.mjs 鎖住 shim 與 postinstall 的行為：五組 os/cpu 對映到子套件名、其他回 null；以假 binary 驗 shim 的 spawn 帶回 exit code（0 與非 0）並透傳全部參數；找不到子套件時 stderr 列出支援組合且 exit 1；postinstall 在 darwin/linux 找到 binary 時把 bin/speclink 原地換成該 binary（內容相同、模式 0755），在 win32 不置換，找不到 binary 時保留 shim 且 exit 0。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，新測試紅。 <!-- speclink-task:tsk_01M2EVCZX2HSKCSYEH192A244D -->
- [x] 5.4 新增 packages/cli-npm/bin/speclink（帶 shebang 的無副檔名 JS shim：以 createRequire 解析平台子套件的 package.json 路徑、spawnSync 該 binary 並繼承 stdio、exit code 與訊號原樣帶回；把平台解析與路徑組裝匿名成可匯入的函式供測試）與 packages/cli-npm/postinstall.mjs（同目錄暫存名寫入後 rename 置換 bin/speclink、chmod 0755；win32 直接結束；找不到 binary 印一行說明並以 0 結束）。驗證：5.3 轉綠；在本機以 npm pack 出主套件與本平台子套件後 npm i -g 兩個 tgz，speclink --version 輸出同版且 file 顯示原生執行檔，npm i -g --ignore-scripts 後仍可執行。 <!-- speclink-task:tsk_01M2EVCZX24JR7FJMDXX3XKX2W -->
- [x] 5.5 對 shim、postinstall 與 install.sh 的新參數面套用 sharp-edges 稽核清單（speclink instructions --skill audit）：參數透傳（含以 -- 開頭的參數與空字串）、訊號終止、postinstall 置換的原子性與權限、symlink 目標為原生檔後 npm 重裝或 uninstall 的行為、SPECLINK_INSTALL_REGISTRY 帶尾斜線或非 https 的處理；把發現修進 5.4 與 7.2 並補對應測試。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs 全綠，稽核結論寫入 commit 說明。 <!-- speclink-task:tsk_01M2EVCZX2Y3S4710VJWBYVXD7 -->

## 6. Homebrew formula 改指 npm（D5）

- [x] 6.1 改寫 scripts/release/homebrew-formula.test.mjs 鎖住「Homebrew formula 產生器」的新契約：fixture 校驗清單含五個 cli-<os>-<cpu>-<版本>.tgz 條目時，輸出四組 url 為 https://registry.npmjs.org/@speclink/cli-<os>-<cpu>/-/cli-<os>-<cpu>-<版本>.tgz 且 sha256 對應、不含 win32；缺 darwin-arm64 條目時非零結束並點名；缺參數與清單不存在的既有失敗路徑保留；ruby -c 合法性測試保留；刪除「不把 speclink-server 誤當 CLI 資產」測試。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，新斷言紅。 <!-- speclink-task:tsk_01M2EVCZX2RX8DWNWR9YXF54CB -->
- [x] 6.2 scripts/release/homebrew-formula.mjs 的輸入改讀 tgz 校驗清單、TARGETS 改為 os/cpu 對映、url 改為 registry 樣式，formula 其餘區塊（desc、homepage、version、bin.install、test）不變。驗證：6.1 轉綠。 <!-- speclink-task:tsk_01M2EVCZX2V8362Q9VGQHFSK2S -->

## 7. install.sh 改抓 npm、install.ps1 退役（D6）

- [x] 7.1 改寫 scripts/install.test.mjs 鎖住「安裝腳本一行安裝對應平台 CLI」的新契約：fixture 改為含 package/speclink 的 tgz、latest 回應含 version 的 JSON、版本回應含 dist.integrity（sha512-base64）的 JSON；dry-run 四組平台輸出 registry 的 tgz URL 且不呼叫 curl；SPECLINK_INSTALL_VERSION 帶 v 與不帶 v 輸出相同 URL；未釘選時經 @speclink/cli/latest 解析版本；integrity 不符時非零結束且安裝目錄無檔；安裝成功後 binary 可執行；Windows 的 uname 值非零結束並提到 npm 與 setup.exe；PATH 提示保留；刪除整組 install.ps1 測試與 pwsh 偵測。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，新斷言紅。 <!-- speclink-task:tsk_01M2EVCZX2B5QXT2D052AXTJ6T -->
- [x] 7.2 改寫 scripts/install.sh：平台對映改 npm os/cpu 名；版本解析改 registry（SPECLINK_INSTALL_REGISTRY 可覆寫、預設 https://registry.npmjs.org，移除 SPECLINK_INSTALL_REPO）；下載平台子套件 tgz；以 openssl dgst -sha512 -binary 與 openssl base64 -A 算 integrity 比對（缺 openssl 即非零結束）；驗證通過後以 tar --strip-components=1 解出 package/speclink 再複製到安裝目錄；dry-run 與 --help 文案同步；檔頭註解與用法更新。驗證：7.1 轉綠。 <!-- speclink-task:tsk_01M2EVCZX2T0Y8D68CDDV2Q64C -->
- [x] 7.3 刪除 scripts/install.ps1，並確認 repo 內對 install.ps1 的引用只剩 openspec/changes/archive 與 openspec/discussions（README 與 docs 的引用在第 9 組移除）。驗證：grep -rn install.ps1 排除封存與討論後為空；node --test scripts/*.test.mjs scripts/*/*.test.mjs 全綠。 <!-- speclink-task:tsk_01M2EVCZX2RKAAE3CH1Z4A0G0V -->

## 8. 桌面 Linux 分流：移除 deb（D7）

- [x] 8.1 改寫 apps/desktop/src/__tests__/cliInstall.test.ts 鎖住「安裝 CLI 指令到 PATH」的新分流：平台 linux-unpackaged 的佈署計畫為 action none、reason unpackaged；needsRedeploy 對 linux-unpackaged 在三種狀態皆為 false；移除 linux-deb 與 package-managed 的案例。驗證：npm test -w apps/desktop，新斷言紅。 <!-- speclink-task:tsk_01M2EVCZX26TV1KS56JQC456HW -->
- [x] 8.2 apps/desktop/src/core/cliInstall.ts 的 CliPlatform 以 linux-unpackaged 取代 linux-deb、CliDeployPlan 的 reason 以 unpackaged 取代 package-managed；apps/desktop/src/views/AppSettingsView.tsx 移除 linux-deb 的 FieldHelp 分支，並移除 apps/desktop/src 內 i18n 資源的 cliInstall.packageManaged 鍵（中英）；apps/desktop/src/store.ts 的 deploysToLocalBin 判定不變。驗證：8.1 轉綠；npm test -w apps/desktop 全綠（含 appSettingsView 測試）。 <!-- speclink-task:tsk_01M2EVCZX2KCD0NXQMWFHRAADX -->
- [x] 8.3 apps/desktop/src-tauri/src/cli_install.rs 的 platform_key 在 Linux 無 APPIMAGE 時回 linux-unpackaged，deployed_version_output 對該平台探測 ~/.local/bin/speclink；不再出現 /usr/bin/speclink 與 linux-deb。驗證：cargo test -p speclink-desktop 全綠；grep -rn linux-deb apps/ 為空。 <!-- speclink-task:tsk_01M2EVCZX2G1JS107BS0SNY7QS -->

## 9. 下載指南與文件（D9）

- [x] 9.1 改寫 scripts/release/release-notes.test.mjs 鎖住「Release 說明含下載指南」的新輸出：對照表含 Speclink_<版本>_universal.dmg（註明兩種晶片同一檔）、Speclink_<版本>_x64-setup.exe、Speclink_<版本>_amd64.AppImage 與 Speclink_<版本>_aarch64.AppImage、以及 Linux 伺服器或無圖形介面指向 CLI 一行安裝的一列；CLI 節含 npm i -g @speclink/cli、curl 一行 install.sh、brew install MomoChenisMe/tap/speclink；server 節不變；尾註含 .app.tar.gz 與 latest.json；輸出不含 .sig、.deb、SHA256SUMS、install.ps1、.tar.gz（.app.tar.gz 除外）與 .zip 字樣。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs，新斷言紅。 <!-- speclink-task:tsk_01M2EVCZX2TG0HFRNHZNKG9MZW -->
- [x] 9.2 scripts/release/release-notes.mjs 依 9.1 改寫對照表、CLI 節與尾註。驗證：9.1 轉綠。 <!-- speclink-task:tsk_01M2EVCZX3CDSHHE1FKNX5HQMT -->
- [x] 9.3 README.md 與 README.en.md 的安裝區塊依「安裝通路文件與發布狀態誠實化」與「安裝章節載明桌面 app 與 CLI 的佈署衝突」改寫：桌面表 macOS 一列 universal dmg（兩種晶片同一檔）、Windows 一列、Linux 一列 AppImage 依架構；CLI 三條指令（npm、curl install.sh、brew）並註明 Windows 走 npm 或安裝器、無圖形 Linux 走 CLI 一行；覆蓋行為表移除 deb 行；新增 deb 安裝者遷移一句（移除 deb 套件後改裝 AppImage）；SPECLINK_INSTALL_DIR／SPECLINK_INSTALL_VERSION（帶或不帶 v）／SPECLINK_INSTALL_REGISTRY 說明；中英結構與事實對等。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs（中英對照、連結與詞彙守門測試）全綠；人工對讀兩版一次。 <!-- speclink-task:tsk_01M2EVCZX33EAZ5YPF88BTM3PK -->
- [x] 9.4 docs/getting-started.md 與 docs/getting-started.zh-TW.md 的安裝節改為三條 CLI 指令（移除 irm install.ps1）；docs/development.md 與 docs/development.zh-TW.md 的安裝檔清單改為 universal dmg、NSIS、AppImage 兩架構、移除 SHA256SUMS.txt 句、Linux 放行段移除 deb；docs/product-status.md 與 docs/product-status.zh-TW.md 的安裝通路列改為「桌面安裝檔（macOS universal dmg、Windows NSIS、Linux AppImage）、CLI 的 npm／安裝腳本／Homebrew tap、server 的 npx／Docker」。驗證：node --test scripts/*.test.mjs scripts/*/*.test.mjs 全綠；grep -rn -E "\.deb|install\.ps1|SHA256SUMS" README.md README.en.md docs/ 只剩 deb 遷移句。 <!-- speclink-task:tsk_01M2EVCZX3EC7Q81P7FS8E7RCC -->

## 10. 收尾與發版後驗證

- [ ] [M] 10.1 首次帶本變更的發版（預期 v0.5.0）完成後實機驗證：gh release view 的 assets 恰為六個且無 .sig／.deb／SHA256SUMS；latest.json 的 darwin 兩鍵 url 相同並指向 universal .app.tar.gz；一台 0.4.0 的 Mac 自動更新到 universal 版並可啟動；macOS 與 Windows 各執行 npm i -g @speclink/cli 後 speclink --version 為 0.5.0（macOS 的 file 顯示原生執行檔）；brew upgrade 後 speclink --version 為 0.5.0（驗證 Homebrew 自動進入 tgz 的 package 目錄，若失敗改 formula 的 bin.install 路徑為 package/speclink）；一台無 Node 的 Linux 以 curl 一行 install.sh 安裝成功。驗證：上列每一項的實際輸出記錄於變更的驗證工單。 <!-- speclink-task:tsk_01M2EVCZX3A2W13A9PAF159DT4 -->
- [x] 10.2 全面收尾：node --test scripts/*.test.mjs scripts/*/*.test.mjs、npm test -w apps/desktop、cargo test -p speclink-desktop 三組全綠；grep -rn -E "SHA256SUMS|install\.ps1|linux-deb|package-managed" 排除 openspec/changes/archive、openspec/discussions 與 openspec/manual 後只剩 README 的 deb 遷移句與本變更的 artifacts；git status 盤點所有改動檔皆在 proposal 的 Impact 清單內。驗證：三組測試指令輸出 0 failures，grep 結果如上。 <!-- speclink-task:tsk_01M2EVCZX33XN3G2R86W017Z33 -->
