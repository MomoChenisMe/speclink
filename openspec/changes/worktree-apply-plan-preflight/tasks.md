## 1. 技能資產改寫

- [ ] 1.1 改寫 crates/engine/speclink-core/assets/skills/apply-worktree-pre.md，落實「apply-with-worktree 技能的前置指示」的 (2)：在 P1「Check the worktree policy」之後、原 P2 之前新增「P2. Select the change with plan」——於主 checkout 執行 speclink plan --json；未指名取 next（null 時列每個 change 的 blockedBy 並 STOP）；指名者 blockedBy 非空即印前置清單並 STOP；名字不在 changes（已封存、打錯、列於 skipped）即 STOP 並列可用 change 名；plan 失敗即顯示錯誤並 STOP；任一 STOP 都明寫「do NOT commit the artifacts, do NOT create the worktree」；選定後宣告「Using change: <name>」。原 P2 的 speclink list --json 存在性檢查整段移除（語意併入新 P2），P3 以後的編號與內文不動。驗證：內容審閱——apply-worktree-pre.md 含 `speclink plan --json`、`next`、`blockedBy`、「do NOT create the worktree」字面，且不再含 `speclink list --json`；`git diff crates/engine/speclink-core/assets/skills/apply.md` 為空。 <!-- speclink-task:tsk_01M2NBJ7MZETBDWDFDTJ0RHDWZ -->
- [ ] 1.2 改寫同一份 apply-worktree-pre.md 的 P0「One change per run」，落實 (0)：偵測到多個 change 名時先跑 speclink plan --json，把 blockedBy 為空的名字列成並行配方（沿用既有「一個 change 一個 session」配方句），blockedBy 非空的名字逐一列出前置並說明「等 <前置> 落地再開」，再以 AskUserQuestion 請使用者自並行名單擇一；「Do **NOT** run them one after another」與 P0 位於政策檢查之前的順序不變。驗證：內容審閱——P0 段含 `blockedBy` 與「等」「落地」字面，`One change per run` 仍位於 `Check the worktree policy` 之前。 <!-- speclink-task:tsk_01M2NBJ7MZJXFRAAVXX5YJAG34 -->
- [ ] 1.3 改寫同一份 apply-worktree-pre.md 的 P6「Work inside the worktree from here on」，落實 (6) 的複查指示：新增一段明寫「apply 本體第 1 步的 speclink plan --json 在 worktree 內只是複查；worktree 內的 plan 讀不到其他 worktree 的進度與主線後續的封存；其結果與 P2 不同時以主 checkout 的判定為準、不因複查結果 STOP」，寫法沿 apply-worktree-post.md「The apply body's own Next steps section does not apply here」的先例。驗證：內容審閱——P6 段含「複查」或 re-check 語意與「主 checkout」「wins」字面。 <!-- speclink-task:tsk_01M2NBJ7MZM59DWPDTZWZMYTE0 -->
- [ ] 1.4 改寫 crates/engine/speclink-core/assets/skills/ingest.md，落實「ingest 收尾重判本變更的軟依賴」：在第 8 步 Validation 之後、第 9 步 Seal 之前插入新步驟「Re-judge soft dependencies」（後續步驟編號順延）：以 speclink list --json 列作用中變更，只有本變更時跳過；兩個以上時只針對本變更、以更新後的 artifacts 為準讀其他變更的 proposal Impact 判定是否建立在其成果上或動到同一段程式碼，有則執行 speclink change depends <本變更> --on <前置>...；動詞拒絕時回報並續行；硬信號交引擎；不移除既有 depends_on；不代跑 apply。字面沿 propose.md「Pending-change landscape check」第 3 點。驗證：內容審閱——ingest.md 含 `speclink change depends`、「Do NOT judge overlap yourself」與「only one active change」跳過句，Next steps 段位元不變。 <!-- speclink-task:tsk_01M2NBJ7MZE99AC0A8RSQH0JZP -->

## 2. 渲染內容測試

- [ ] 2.1 在 crates/engine/speclink-core/tests/it/render_golden.rs 依規格場景新增三個測試：`apply_with_worktree_selects_and_guards_with_plan`（兩個工具的 apply-with-worktree 內文含 `speclink plan --json`、`next`、`blockedBy`、「do NOT create the worktree」字面，且 `Select the change with plan` 位於 `Check the worktree policy` 之後、`into HEAD` 之前）、`apply_with_worktree_treats_the_body_plan_as_a_recheck`（內文含主 checkout 為準句；同時斷言 speclink-apply 的生成內文與 B_APPLY 逐位元一致）、`ingest_skill_rejudges_soft_dependencies_before_handoff`（兩個工具的 ingest 內文含 `speclink change depends` 且位於 `speclink validate` 之後、`## Next steps` 之前）；既有 `apply_with_worktree_refuses_more_than_one_change` 補斷言 P0 段含 `blockedBy`。驗證：`cargo test -p speclink-core --test it render_golden::` 全綠。 <!-- speclink-task:tsk_01M2NBJ7MZ903Y4FMSS6H3NRAH -->

## 3. 版號與 golden 三連動

- [ ] 3.1 crates/engine/speclink-core/src/workspace/init.rs 的 `ASSET_VERSION` 自 v1.37.0 升為 v1.38.0，並以 `UPDATE_ASSETS_LOCK=1` 與 `UPDATE_GOLDEN=1` 同批更新 crates/engine/speclink-core/tests/golden/ 下 claude.snapshot.md、claude-worktree.snapshot.md、codex.snapshot.md、neutral-cli.snapshot.md、neutral-tool-call.snapshot.md 與 assets.lock。驗證：`cargo test -p speclink-core --test it render_golden::` 與 assets.lock 鎖定測試全綠；`git diff --stat crates/engine/speclink-core/assets/skills/` 只列 apply-worktree-pre.md 與 ingest.md；claude-worktree 快照的 apply-with-worktree 段含 `Select the change with plan`。 <!-- speclink-task:tsk_01M2NBJ7MZD6T6SK1QF82ZQSSQ -->
- [ ] 3.2 `cargo build -p speclink-cli` 後執行 `speclink update` 再生 .claude/skills 與 .agents/skills 下的 SKILL.md，以 `git status` 確認 speclink-apply-with-worktree 與 speclink-ingest（claude 與 codex）皆更新、speclink-apply 未變；這批再生檔不進 evidence，收尾 commit 前手動帶上。驗證：`grep -l "speclink plan --json" .claude/skills/speclink-apply-with-worktree/SKILL.md` 命中且 `grep -c "speclink plan --json"` 為 2（前段一次、本體一次）；`grep -l "speclink change depends" .claude/skills/speclink-ingest/SKILL.md` 命中；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 全綠。 <!-- speclink-task:tsk_01M2NBJ7MZ63TV7T1WQQ5EHAMR -->
