## 1. 技能資產改寫

- [x] 1.1 改寫 crates/engine/speclink-core/assets/skills/apply-worktree-pre.md，落實「apply-with-worktree 技能的前置指示」的 (2)：在 P1「Check the worktree policy」之後、原 P2 之前新增「P2. Select and guard the change in the main checkout」（標題點出主 checkout，與本體第 1 步的「Select the change with plan」區分）——於主 checkout 執行 speclink plan --json；未指名取 next（null 時列每個 change 的 blockedBy 並 STOP）；指名者 blockedBy 非空即印前置清單並 STOP；名字不在 changes（已封存、打錯、列於 skipped）即 STOP 並列可用 change 名；plan 失敗即顯示錯誤並 STOP；任一 STOP 都明寫「do NOT commit the artifacts, do NOT create the worktree」；選定後宣告「Using change: <change-name>」（佔位名與同檔其他步驟一致）。原 P2 的 speclink list --json 存在性檢查整段移除（語意併入新 P2），P3 以後的編號與內文不動。驗證：內容審閱——apply-worktree-pre.md 含 `speclink plan --json`、`next`、`blockedBy`、「do NOT create the worktree」字面，且不再含 `speclink list --json`；`git diff crates/engine/speclink-core/assets/skills/apply.md` 為空。 <!-- speclink-task:tsk_01M2NBJ7MZETBDWDFDTJ0RHDWZ -->
- [x] 1.2 改寫同一份 apply-worktree-pre.md 的 P0「One change per run」，落實 (0)：偵測到多個 change 名時先跑 speclink plan --json，把 blockedBy 為空的名字列成並行配方（沿用既有「一個 change 一個 session」配方句），blockedBy 非空的名字逐一列出前置並說明「等 <前置> 落地再開」，再以 AskUserQuestion 請使用者自並行名單擇一；恰有一個可並行時改以 AskUserQuestion 確認是否在此執行（不印配方），沒有可並行者即列出須等與不可用的名字並 STOP；「Do **NOT** run them one after another」與 P0 位於政策檢查之前的順序不變。驗證：內容審閱——P0 段含 `blockedBy`、「等」「落地」、「from the parallel-ready names only」、「confirm running that change here」與「report the waiting and unavailable names」字面，`One change per run` 仍位於 `Check the worktree policy` 之前。 <!-- speclink-task:tsk_01M2NBJ7MZJXFRAAVXX5YJAG34 -->
- [x] 1.3 改寫同一份 apply-worktree-pre.md 的 P6「Work inside the worktree from here on」，落實 (6) 的複查指示：新增一段明寫「apply 本體第 1 步的 speclink plan --json 在 worktree 內只是複查；worktree 內的 plan 讀不到其他 worktree 的進度與主線後續的封存；其結果與 P2 不同時以主 checkout 的判定為準、不因複查結果 STOP，第 2 步（含 review prepare 與 in-progress add）照常執行」，寫法沿 apply-worktree-post.md「The apply body's own Next steps section does not apply here」的先例。驗證：內容審閱——P6 段含 re-check、「the main checkout's verdict wins」與「`speclink review prepare` and `speclink in-progress add` included」字面。 <!-- speclink-task:tsk_01M2NBJ7MZM59DWPDTZWZMYTE0 -->
- [x] 1.4 改寫 crates/engine/speclink-core/assets/skills/ingest.md，並對齊 crates/engine/speclink-core/assets/skills/propose.md 的落檔段落，落實「ingest 收尾重判本變更的軟依賴」：在第 8 步 Validation 之後、第 9 步 Seal 之前插入新步驟「Re-judge soft dependencies」（後續步驟編號順延）：以 speclink list --json 列作用中變更，只有本變更時跳過；兩個以上時只針對本變更、以更新後的 artifacts 為準讀其他變更的 proposal Impact 判定是否建立在其成果上，有則對每個前置各執行一次 speclink change depends <本變更> --on <前置>；只動到同一段程式碼不記為前置、改向使用者提出；動詞拒絕時回報並續行下一個前置、不重試；硬信號交引擎；不移除既有 depends_on；不代跑 apply。落檔段落（「Record each prerequisite with its own call」至「Do NOT judge overlap yourself.」）與 propose.md「Pending-change landscape check」第 3 點逐字一致，propose.md 同段同步改為每個前置各一次呼叫（propose 的判定規則不變）。Next steps 段的出邊不變，唯一改動是 seal 的步驟參照隨編號順延自「(step 9)」改為「(step 10)」（不改會指到新步驟）。驗證：內容審閱——ingest.md 含 `speclink change depends`、「Do NOT judge overlap yourself」、「Editing the same code areas alone is not a prerequisite here」與「Only one active change」跳過句，Next steps 段與改動前的差異只有「(step 9)」→「(step 10)」；`propose_and_ingest_skills_share_the_depends_recording_wording` 通過。 <!-- speclink-task:tsk_01M2NBJ7MZE99AC0A8RSQH0JZP -->

## 2. 渲染內容測試

- [x] 2.1 在 crates/engine/speclink-core/tests/it/render_golden.rs 依規格場景新增四個測試：`apply_with_worktree_selects_and_guards_with_plan`（兩個工具的 apply-with-worktree 在 P2 段含 `speclink plan --json`、「take `next`」、「print the prerequisite list」、「report which change names are available」、「do NOT commit the artifacts, do NOT create the worktree」字面，`Select and guard the change in the main checkout` 位於 `Check the worktree policy` 之後、`into HEAD` 之前，且前置段不含 `speclink list --json`）、`apply_with_worktree_treats_the_body_plan_as_a_recheck`（P6 含複查、主 checkout 為準與第 2 步照常執行句；speclink-apply 的生成內文在 frontmatter 之後只有 apply 資產且不含 re-check；apply 資產本身的位元不變由 golden 快照把關）、`ingest_skill_rejudges_soft_dependencies_before_handoff`（第 9 步位於 `speclink validate` 之後、Seal 與 `## Next steps` 之前；步驟範圍內含只記建立在成果上、同一段程式碼不記、每個前置各一次呼叫、不重試與不刪既有依賴等字面；Next steps 的 seal 步驟參照綁定 Seal 實際編號）、`propose_and_ingest_skills_share_the_depends_recording_wording`（propose 與 ingest 的落檔段落以空白正規化後逐字相同）；既有 `apply_with_worktree_refuses_more_than_one_change` 補斷言 P0 段含 `speclink plan --json`、`blockedBy`、「落地再開」、「from the parallel-ready names only」、「confirm running that change here」與「report the waiting and unavailable names」，既有 `apply_with_worktree_embeds_the_entire_apply_body` 與新測試共用 `rendered_apply_body` helper。驗證：`cargo test -p speclink-core --test it render_golden::` 全綠。 <!-- speclink-task:tsk_01M2NBJ7MZ903Y4FMSS6H3NRAH -->

## 3. 版號與 golden 三連動

- [x] 3.1 crates/engine/speclink-core/src/workspace/init.rs 的 `ASSET_VERSION` 自 v1.38.0 升為 v1.39.0（v1.38.0 已由先落地的 discuss-scout-in-flight-deltas 使用），並以 `UPDATE_ASSETS_LOCK=1` 與 `UPDATE_GOLDEN=1` 同批更新 crates/engine/speclink-core/tests/golden/ 下 claude.snapshot.md、claude-worktree.snapshot.md、codex.snapshot.md、neutral-cli.snapshot.md、neutral-tool-call.snapshot.md 與 assets.lock。驗證：`cargo test -p speclink-core --test it render_golden::` 與 assets.lock 鎖定測試全綠；`git diff --stat crates/engine/speclink-core/assets/skills/` 只列 apply-worktree-pre.md、ingest.md 與 propose.md；claude-worktree 快照的 apply-with-worktree 段含 `Select and guard the change in the main checkout`。 <!-- speclink-task:tsk_01M2NBJ7MZD6T6SK1QF82ZQSSQ -->
- [x] 3.2 `cargo build -p speclink-cli` 後執行 `speclink update` 再生 .claude/skills 與 .agents/skills 下的 SKILL.md，以 `git status` 確認 speclink-apply-with-worktree、speclink-ingest 與 speclink-propose（claude 與 codex）內文皆更新、speclink-apply 除 frontmatter 的 version 行外未變；這批再生檔不進 evidence，收尾 commit 前手動帶上。驗證：`grep -l "speclink plan --json" .claude/skills/speclink-apply-with-worktree/SKILL.md` 命中且 `grep -c "speclink plan --json"` 為 3（前段 P0 與 P2 各一次、本體一次）；`grep -l "speclink change depends" .claude/skills/speclink-ingest/SKILL.md` 命中；`grep -l "Record each prerequisite with its own call" .claude/skills/speclink-propose/SKILL.md` 命中；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 全綠。 <!-- speclink-task:tsk_01M2NBJ7MZ63TV7T1WQQ5EHAMR -->
