## 1. 技能規則與模板（crates/engine/speclink-core/assets/skills/discuss.md）

- [ ] 1.1 先寫紅燈測試：在 crates/engine/speclink-core/tests/it/render_golden.rs 新增 `discuss_skill_carries_the_conclusion_bullets_rule`，比照 `archive_skill_describes_two_field_trace_and_the_zero_evidence_note` 的寫法，以 `skill_for_both_tools` 取得各渲染目標產出的 speclink-discuss 技能檔並逐份斷言：Document rules 含第 8 條「Conclusion bullets over prose」字面、含「Rationale、Capture to、Next 維持單段」語意的英文句、含多刀 bullet 起頭 `**cut N \`change-name\`**` 的字面；conclude 指令範例與「Capture decisions」摘要的 Decision 行之後緊接 `- ` 起頭的條列行。驗證：`cargo test -p speclink-core --test it render_golden::discuss_skill_carries_the_conclusion_bullets_rule` 此時紅燈。 <!-- speclink-task:tsk_01M33MQT83RD14GADWQ0RC6BQV -->
- [ ] 1.2 落實正式規格「討論記錄的樹慣例與格式不變」新增的結論條列規則：在 discuss.md 的 Document rules 加第 8 條「Conclusion bullets over prose」——Decision、Rejected alternatives、Deferred 超過一句 SHALL 條列（一句定論起頭、其後 `- ` 一點一行）；Rationale、Capture to、Next 維持單段；Decision 保留全部定案細節、不縮水、子項不回指輪次；多刀時每刀一個 bullet `**cut N \`change-name\`**：一句範圍`、細節縮排子項一子項一件事；Rejected alternatives 每項一行「方案——落敗理由」、Deferred 每項一行「問題——為何現在不解」或 none；規則只約束新寫入的結論。與第 5 條句型對稱、用英文寫。驗證：任務 1.1 的測試中 Document rules 相關斷言轉綠。 <!-- speclink-task:tsk_01M33MQT832Q2CN34VFSASR2GD -->
- [ ] 1.3 把 discuss.md 的 `speclink discuss conclude … --stdin` 範例與「Capture decisions」的 Conclusion 摘要格式改成條列形：Decision 一句總結後接兩到三行 `- ` 示範（含一行多刀 `**cut N \`change-name\`**：` 形狀）、Rejected alternatives 之後接 `- 方案——落敗理由` 示範行、Deferred 示範 none；Rationale、Capture to、Next 維持單行。技能其餘段落逐位元不變。驗證：任務 1.1 的測試全部轉綠；`git diff --stat crates/engine/speclink-core/assets/skills/discuss.md` 只落在 Document rules、conclude 範例與 Capture decisions 三處。 <!-- speclink-task:tsk_01M33MQT8379Q30AXZQHZY1J31 -->

## 2. 版號、golden 與技能實例同步

- [ ] 2.1 ASSET_VERSION（crates/engine/speclink-core/src/workspace/init.rs）自 v1.39.0 升至 v1.40.0，並依測試訊息指示的順序重生鎖定檔：先 `UPDATE_ASSETS_LOCK=1 cargo test -p speclink-core --test it render_golden::` 重生 crates/engine/speclink-core/tests/golden/assets.lock，再 `UPDATE_GOLDEN=1 cargo test -p speclink-core --test it render_golden::` 重生五份 golden（claude、claude-worktree、codex、neutral-cli、neutral-tool-call）。驗證：不帶環境變數重跑 `cargo test -p speclink-core --test it render_golden::` 全綠；`git diff --stat crates/engine/speclink-core/tests/golden/` 五份 snapshot 與 assets.lock 皆有變動，且 snapshot 的差異只落在 speclink-discuss 技能段與各技能 version 行。 <!-- speclink-task:tsk_01M33MQT83DTG8M45HVCW0EV25 -->
- [ ] 2.2 先 `cargo build -p speclink-cli`，再以 target/debug/speclink 在本 repo 執行 `speclink update`，重生 .claude/skills/speclink-discuss/SKILL.md 與 .agents/skills/speclink-discuss/SKILL.md。驗證：兩份 SKILL.md 各含第 8 條規則與條列形 conclude 範例；`git status --short .claude .agents` 顯示 37 份 SKILL.md 變動（speclink-discuss 兩份含內文，其餘只有 version 行 v1.40.0）；`target/debug/speclink --version` 的引擎版號為 v1.40.0。 <!-- speclink-task:tsk_01M33MQT83D5QJKEVZ4NTQWKCY -->

## 3. 回歸驗證

- [ ] 3.1 受影響面全綠：`cargo test -p speclink-core` 通過（含 render_golden 五份 golden 位元一致、assets.lock 鎖定測試、skill_verbization 與 no_direct_fs 守門）；`speclink validate discuss-conclusion-bullets` 通過；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 通過（詞彙守門掃 openspec/ 與 docs 面）。驗證：三條指令的 exit code 皆為 0。 <!-- speclink-task:tsk_01M33MQT83YCHTJZTVD5SZBTSD -->
