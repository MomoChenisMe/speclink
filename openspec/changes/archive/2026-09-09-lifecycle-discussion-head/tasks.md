## 1. 基準

- [x] 1.1 建立回歸基準：在改碼前跑 `cargo test -p speclink-core --test it render_golden::` 與 `cargo test -p speclink-cli --test it discuss`，記下全綠與 golden 零 diff 的狀態（若基準本身就紅，先停下回報，不在本 change 修）。驗證：兩條指令 exit code 0。路徑：crates/speclink-core/tests/golden、crates/speclink-cli/tests/it <!-- speclink-task:tsk_01M22QT9VTFEPXBSZJ754XZ45D -->

## 2. KeyLines 原語（design D1 KeyLines 落點為獨立模組 keylines.rs、D2 KeyLines 介面與語意）

- [x] 2.1 【Red】寫 `KeyLines` 單元測試（crates/speclink-core/src/keylines.rs 的 `#[cfg(test)]` 模組）釘住 D2 KeyLines 介面與語意：`get` 只認區域內第 0 欄 `key:`；`set` 第一條原位代換、重複鍵整行丟、缺則補在 closing `---` 前（Frontmatter）或檔尾（Whole，尾端無換行先補）；未閉合 frontmatter 上新插一行回 `Err` 且 `text()` 逐位元不變；`remove` 連同縮排續行、第 0 欄 `- ` 序列項與區塊內空行一起移除；`list`／`push_list`（已含回 false 不改）／`remove_list`（清空即整行移除）；CRLF 記錄新行沿 CRLF；`frontmatter()` 對首行非 `---` 回 `None`。驗證：`cargo test -p speclink-core keylines` 全部失敗於「模組不存在」或斷言。 <!-- speclink-task:tsk_01M22QT9VTNJ3E0Q3YE6ARJJ30 -->
- [x] 2.2 【Green】實作 `KeyLines`（`Region::{Frontmatter, Whole}`、`whole`／`frontmatter` 建構、`get`／`set`／`remove`／`list`／`push_list`／`remove_list`／`changed`／`text`）於 crates/speclink-core/src/keylines.rs，並在 crates/speclink-core/src/lib.rs 註冊 `pub mod keylines`（D1 KeyLines 落點為獨立模組 keylines.rs：不放 util.rs 或 model.rs）。值逐字寫入、不做 YAML 跳脫（跳脫責任在呼叫端，模組文件註明）。驗證：`cargo test -p speclink-core keylines` 全綠。 <!-- speclink-task:tsk_01M22QT9VTCPPTC26A7YD0X3C6 -->
- [x] 2.3 對 `KeyLines` 新公開 API 套用 sharp-edges 稽核清單（`speclink instructions --skill audit`）：空文件、只有 `---` 一行的文件、鍵名含冒號、值為空字串、重複鍵三條以上、混合行尾——每個邊界各補一條測試或在文件註明刻意行為。驗證：`cargo test -p speclink-core keylines` 全綠，稽核結論寫進 keylines.rs 模組文件。路徑：crates/speclink-core/src/keylines.rs <!-- speclink-task:tsk_01M22QT9VTGSSXMEPA2B71JZ7X -->

## 3. DiscussionHead 型別（design D3 DiscussionHead 只管 frontmatter、讀端永遠成功、回寫沿 None 契約；D4 三條轉移規則住在 head 方法上）

- [x] 3.1 【Red】寫 `DiscussionHead` 單元測試（crates/speclink-core/src/discuss.rs 測試模組）釘住 D3 DiscussionHead 只管 frontmatter、讀端永遠成功、回寫沿 None 契約與 D4 三條轉移規則住在 head 方法上：`parse` 對無 frontmatter 記錄回全預設且 `write_back()` 為 `None`；status 缺席→Open、未知字面→`Unknown(原字串)` 且 `as_str` 逐位元回原字串；`promote` 新名字清 hold 回 true、舊名字不清 hold 回 false、Concluded／Unknown→Promoted；`unlink` 不在清單回 `None`、縮清單保持 Promoted、清空時移除 promoted_to 行並依 `has_conclusion` 回退 Concluded／Open；`conclude(hold)` Open／Unknown→Concluded、Promoted 保持、hold 設或清；**內文含 `status: open` 與 `status: concluded` 字串的記錄，promote 後內文逐位元不變、只有 frontmatter 改動**（本 change 立案時對 improve-lifecycle-layer 發作的案例）。驗證：`cargo test -p speclink-core discuss::tests::head_` 全部失敗。 <!-- speclink-task:tsk_01M22QT9VTF28N2STFXPHGKFSX -->
- [x] 3.2 【Green】於 crates/speclink-core/src/discuss.rs 實作 `Status`（Open／Concluded／Promoted／Unknown(String)、`as_str`）與 `DiscussionHead`（九欄、`parse`、`write_back`、`promote`、`unlink`、`conclude`），建在 `KeyLines::frontmatter` 之上；`write_back` 只寫 status／promoted_to／hold／board_rank 四欄，其餘唯讀。驗證：`cargo test -p speclink-core discuss::tests::head_` 全綠。 <!-- speclink-task:tsk_01M22QT9VTC4CYQ4F1T9MXT819 -->

## 4. 寫入動詞改走 head、刪除文字手術（design D7 刪除五支文字探測函式與全部 frontmatter replacen）

- [x] 4.1 `mark_promoted`、`unlink_discarded`、`conclude`、`set_board_rank` 四支改為「`DiscussionHead::parse` → 呼叫方法 → `write_back`」；`write_back` 回 `None` 時 `conclude --hold` 與 `set_board_rank` 回既有錯誤字串（「has no frontmatter — cannot hold it live」／「has no frontmatter — cannot set board rank」），`mark_promoted`／`unlink_discarded`／不帶 hold 的 `conclude` 保留原文不落檔；`unlink_discarded` 的 `has_conclusion` 以 `conclusion_body(&text).is_some()` 提供。可觀察行為：既有 mark_promoted／unlink／conclude／hold／board_rank 測試斷言一字不改仍綠。驗證：`cargo test -p speclink-core discuss` 全綠。路徑：crates/speclink-core/src/discuss.rs <!-- speclink-task:tsk_01M22QT9VT71PYQ0X5C8Y84XJA -->
- [x] 4.2 執行 D7 刪除五支文字探測函式與全部 frontmatter replacen：刪除 `frontmatter_value`、`set_frontmatter_line`、`promoted_to_in`、`concluded_in`、`held_in`，`stamp_restale` 讀 promoted_to 與 `archive_discussion` 讀 created 改用 `DiscussionHead::parse`；`discuss.rs` 非測試碼內對 frontmatter 的 `replacen` 全數消失，對 change meta 的三處 `replacen`（`link` 的 from_discussion、`stamp_restale`／`clear_restale` 的 restale_from）保留不動（刀二範圍）。驗證：`grep -n 'fn frontmatter_value\|fn set_frontmatter_line\|fn promoted_to_in\|fn concluded_in\|fn held_in' crates/speclink-core/src/discuss.rs` 零命中；`cargo test -p speclink-core discuss` 全綠（host crate 此時尚未編譯過，4.2 之後立刻做 5.4）。 <!-- speclink-task:tsk_01M22QT9VTSDPFGMXPRNEPXN5G -->

## 5. DiscussionInfo 投影與三個轉接移除（design D5 DiscussionInfo 以 serde(skip) 欄位吸收四個獨立讀取）

- [x] 5.1 【Red】寫測試釘住 D5 DiscussionInfo 以 serde(skip) 欄位吸收四個獨立讀取：`info_from_doc` 產出的 `promoted_to`／`concluded`／`held`／`board_rank` 與記錄內容一致；`serde_json::to_string(&info)` 的輸出不含 `promotedTo`／`promoted_to`／`concluded`／`held`／`boardRank` 任一鍵（`--json` 逐位元不變的斷言）；從不含這四鍵的 JSON 反序列化成功且四欄為預設值。驗證：`cargo test -p speclink-core discuss::tests::info_` 失敗於欄位不存在。路徑：crates/speclink-core/src/discuss.rs <!-- speclink-task:tsk_01M22QT9VT59NA9N9K68M1J6Q2 -->
- [x] 5.2 【Green】`DiscussionInfo` 新增四個 `#[serde(skip)]` 欄位；`info_from_doc` 改由一趟 `DiscussionHead::parse` 產出九欄（slug／topic 缺席回退 `doc.slug`、created 缺席回空字串、status 用 `as_str`）、`concluded` 用同一份全文的 `conclusion_body`；公開函式 `promoted_to(store, slug)`、`discussion_concluded`、`discussion_held`、`board_rank(store, slug)` 簽名不變、改由 head 實作。驗證：`cargo test -p speclink-core discuss` 全綠；`cargo test -p speclink-core --test it render_golden::` 零 diff。路徑：crates/speclink-core/src/discuss.rs <!-- speclink-task:tsk_01M22QT9VTV8V4VJBZZS0AGJQC -->
- [x] 5.3 desktop 討論列表改讀 info 欄位：`entry()` 的 `promotedTo`／`concluded` 直接取 `info.promoted_to`／`info.concluded`，`board_sorted_active` 直接取 `info.board_rank`，每張卡不再額外讀檔；desktop payload 的鍵名（camelCase）與值不變。驗證：`cargo test -p speclink-desktop-core discussions` 既有斷言不改全綠。路徑：apps/desktop/core/src/discussions.rs <!-- speclink-task:tsk_01M22QT9VTJVN4FCK48BDN9P29 -->
- [x] 5.4 server 與 host 轉接移除：`discussion_dtos_with_extras` 改為對每個 info 直接填 `dto.promoted_to = info.promoted_to`、`dto.concluded = Some(info.concluded)`，不再 `spawn_blocking` 也不再 materialize bridge view；刪除 `speclink_host::bridge::discussions_extras`；CLI 的 `to_discussion_info` 結構體字面補四欄（`promoted_to` 取 wire 的 promotedTo、`concluded` 取 wire 的 `concluded.unwrap_or(false)`、其餘預設）。可觀察行為：`GET /discussions` 的 `promotedTo`／`concluded` 值與形狀不變；remote 與 local 的 discuss 動詞輸出一致。驗證：`cargo test -p speclink-host`、`cargo test -p speclink-server discussion`、`cargo test -p speclink-cli --test it remote_verb_parity` 全綠；`grep -rn 'discussions_extras' crates apps --include='*.rs'` 零命中。路徑：crates/speclink-server/src/routes.rs、crates/speclink-host/src/bridge.rs、crates/speclink-cli/src/verbs/discuss.rs <!-- speclink-task:tsk_01M22QT9VTNT59F5N9YNXHW5PA -->

## 6. 討論收尾判準單源（design D6 close_if_finished 收三條件與封存步、archive 路徑 fail-closed）

- [x] 6.1 【Red】在 crates/speclink-core/src/archive.rs 測試模組新增「在途變更 meta 損壞時討論不隨變更封存」（規格 discussion-docs「討論以 link 動詞併入既有變更」的新 scenario）：討論 d1 已結論、無 hold；變更 cut 的 meta 含 `from_discussion: d1`、tasks 全完成；另一在途變更 broken 的 meta 為壞 YAML；`archive(cut)` 成功、`archived_discussions` 為空、d1 仍 live。驗證：`cargo test -p speclink-core archive::tests::archive_leaves_discussion_live_when_an_in_flight_meta_is_corrupt` 失敗（現況把 d1 掃進封存區）。 <!-- speclink-task:tsk_01M22QT9VT92KV73EK3SX1DE64 -->
- [x] 6.2 【Green】實作 D6 close_if_finished 收三條件與封存步、archive 路徑 fail-closed：`discuss::close_if_finished(store, slug) -> Result<Option<String>>`（無在途引用且 `meta_error` 視為引用、已有結論、無 hold 三條件皆過才 `archive_discussion`）；`conclude` 的閉環步改為「promoted_to 非空時呼叫它」、`closing_error` 包裝不變；`archive()` 的討論 `filter_map` 改為逐 slug 呼叫、`Ok(None)` 與 `Err` 皆跳過。可觀察行為：conclude 家族與 archive 隨行封存家族既有測試斷言不改全綠，6.1 轉綠。驗證：`cargo test -p speclink-core discuss::tests::conclude`、`cargo test -p speclink-core archive::tests::archive_` 全綠。路徑：crates/speclink-core/src/discuss.rs、crates/speclink-core/src/archive.rs <!-- speclink-task:tsk_01M22QT9VTDT6BKQETA6286DFG -->

## 7. 收尾（design D8 測試策略：既有斷言不動、新增落在三處）

- [x] 7.1 依 D8 測試策略：既有斷言不動、新增落在三處做全面驗收：`git diff` 確認 discuss.rs／archive.rs／discussions.rs／routes.rs 既有測試只改呼叫的函式名、不改斷言字串；跑 `cargo test -p speclink-core`、`cargo test -p speclink-core --test it`、`cargo test -p speclink-host`、`cargo test -p speclink-server`、`cargo test -p speclink-cli --test it`、`cargo test -p speclink-desktop-core` 全綠；`cargo test -p speclink-core --test it render_golden::` 零 diff；`grep -rn 'frontmatter_value\|set_frontmatter_line\|promoted_to_in\|concluded_in\|held_in\|discussions_extras' crates apps --include='*.rs'` 零命中。驗證：上列指令全部 exit code 0。 <!-- speclink-task:tsk_01M22QT9VT9FPNS44A6BGG57BC -->
- [x] 7.2 詞彙與文件守門：`cargo clippy -p speclink-core -p speclink-host -p speclink-server --all-targets -- -D warnings` 零警告（刪除轉接後不留孤兒 import／函式）；跑 `node --test scripts/*.test.mjs` 全綠。驗證：兩條指令 exit code 0。路徑：crates/、scripts/ <!-- speclink-task:tsk_01M22QT9VTCSEXR6FZ8QB3X1XJ -->
