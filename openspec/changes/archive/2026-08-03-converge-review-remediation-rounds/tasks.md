## 1. Apply baseline（D1 — Apply baseline 是 host-local sidecar，不是 touched v3）

- [x] 1.1 先在 `crates/speclink-host/src/change_diff.rs`、`crates/speclink-cli/tests/it/review_verbs.rs` 建立「Apply 開始前記錄 host-local baseline」紅燈測試：initial 精確記 HEAD／dirty paths／UTC、started-without-baseline 產生 late、無 Git 產生 unavailable、baseline JSON camelCase、touched fixture 位元級不變、寫入失敗不執行 in-progress add；驗證：`cargo test -p speclink-host change_diff` 與 `cargo test -p speclink-cli review_prepare` 因功能未實作而失敗 <!-- speclink-task:tsk_01KZ2SHPGEYPQRTGEJ1YPK7YVJ -->
- [x] 1.2 在 `crates/speclink-host/src/change_diff.rs`、`crates/speclink-host/src/lib.rs`、`crates/speclink-core/src/workspace.rs`、`crates/speclink-cli/src/main.rs`、`crates/speclink-cli/src/commands.rs` 實作 review prepare 的原子 sidecar 與 silent／warning／error 契約，不改 touched 或 change metadata shape；驗證：1.1 測試轉綠且 `cargo test -p speclink-core tasks` 通過 <!-- speclink-task:tsk_01KZ2SHPGEM8VNMG89RDT27QRY -->

## 2. Discovery resolver 與歧義選擇（D2 — Host change-diff resolver 是唯一 Git 與 hunk 語意；D3 — 歧義先輸出 candidate，再以 hash-pinned selection 解鎖）

- [x] 2.1 先在 `crates/speclink-host/src/change_diff.rs` 建立「Git-backed discovery scope 解析完整 worktree patch」紅燈測試，覆蓋 staged＋unstaged、touched untracked addition、delete、rename、binary、多 hunk、addition oldLines=0、deletion newLines=0、before／after hashes，並釘住 three-dot 對未提交 fixture 為空但 resolver 仍有 patch；驗證：`cargo test -p speclink-host discovery_scope` 因 resolver 未實作而失敗 <!-- speclink-task:tsk_01KZ2SHPGEYYZ34M1Y4EF95TAH -->
- [x] 2.2 在 `crates/speclink-host/src/change_diff.rs` 實作 baseCommit→worktree 的單一 Git adapter、canonical patch／candidateHash／patchHash、file delta 與 oldStart／oldLines／newStart／newLines parser，排除 `openspec/` 與 `.speclink/` targets；驗證：2.1 測試轉綠，並以 `cargo test -p speclink-host discovery_scope` 確認 rename／binary／跨平台 path normalization <!-- speclink-task:tsk_01KZ2SHPGE5N4Y75R17Z7PP431 -->
- [x] 2.3 先在 `crates/speclink-host/src/change_diff.rs`、`crates/speclink-cli/tests/it/review_verbs.rs` 建立「review scope 的 human 與 JSON 契約」及「歧義 scope 必須 fail closed 並以 hash-pinned selection 解鎖」紅燈測試：dirty-at-start、active overlap、空 touched、candidate 漂移、無效／重複／binary hunk、needsInput 非零且零 snapshot effects、resolved camelCase payload 與 `--no-color`；驗證：`cargo test -p speclink-cli review_scope` 因 CLI／selection 未實作而失敗 <!-- speclink-task:tsk_01KZ2SHPGEPP75J81KK19YDFKB -->
- [x] 2.4 在 `crates/speclink-host/src/change_diff.rs`、`crates/speclink-cli/src/main.rs`、`crates/speclink-cli/src/commands.rs` 實作 `review scope --json [--base] [--candidate-hash] [--include-hunk]`、human 呈現與 hash-pinned selection，確保 needsInput 不凍結、成功只含被選 hunks；驗證：2.3 測試轉綠，JSON 逐欄斷言型別與 camelCase <!-- speclink-task:tsk_01KZ2SHPGE8B21VR2JSBF69V7A -->

## 3. Review snapshots 與 local／remote 組裝（D4 — Frozen review snapshot 只保存審查面；D8 — Apply／Review 技能與 local／remote entry points 共用契約）

- [x] 3.1 先在 `crates/speclink-host/src/change_diff.rs`、`crates/speclink-cli/tests/it/review_verbs.rs` 建立「frozen snapshot 綁定 discovery 與 validation patch」紅燈測試：digest-only Windows-safe 檔名、原子寫入、beforeText／afterText、validation 只含 finding paths＋新髒檔、先前已髒未保存檔 fail closed、snapshot 缺失不退回 discovery、orphan 清理、stamp／discard 清 snapshot 保留 baseline；驗證：`cargo test -p speclink-host review_snapshot` 與 CLI cleanup 測試因功能未實作而失敗 <!-- speclink-task:tsk_01KZ2SHPGEVXFMC1QY9GFTCWA7 -->
- [x] 3.2 在 `crates/speclink-host/src/change_diff.rs`、`crates/speclink-cli/src/commands.rs` 實作 discovery／validation snapshot lifecycle、remediation diff 與成功 stamp／discard 後 cleanup；明示 snapshot 已寫但 add-round 失敗只留下可清 orphan，canonical mutation 成功後 cleanup 失敗僅 warning；驗證：3.1 測試轉綠且 `cargo test -p speclink-cli review_verbs` 通過 <!-- speclink-task:tsk_01KZ2SHPGECAXNAPFY6GBJKTY5 -->
- [x] 3.3 先在 `crates/speclink-cli/tests/it/remote_verb_parity.rs`、`crates/speclink-remote/tests/it/typed_client.rs` 建立「remote workspace 使用同一 host resolver」紅燈測試：remote scope 使用 local Git、不上傳 patch／snapshot、離線／認證失效零 sidecar effects、local／remote resolved 欄位同構；驗證：對應測試因 remote entry point 尚未組裝而失敗 <!-- speclink-task:tsk_01KZ2SHPGES5S4CYQMTX5E0NT5 -->
- [x] 3.4 在 `crates/speclink-cli/src/remote_commands.rs`、`crates/speclink-remote/src/client.rs` 組裝 remote prepare／scope 的 active changes、ticket 與同一 Host resolver，不新增 server Git endpoint；驗證：3.3 測試轉綠，`cargo test -p speclink-remote` 與 `cargo test -p speclink-cli remote_verb_parity` 通過 <!-- speclink-task:tsk_01KZ2SHPGE6V8B9P4N5XHC8HN3 -->

## 4. Structured review rounds（D5 — 工單以 phase 與 patch hash 綁定 snapshot）

- [x] 4.1 先在 `crates/speclink-core/src/review.rs`、`crates/speclink-cli/tests/it/review_verbs.rs`、`crates/speclink-protocol/src/command.rs`、`crates/speclink-server/tests/it/review_api.rs` 建立「審查工單的建立與追加」及「審查工單的讀取」紅燈測試：Phase／Patch 成對與格式、首輪 discovery／續輪 validation、legacy null、append-only、local／remote `phase`／`patchHash` JSON 同構；驗證：core、CLI、protocol、server 對應測試因欄位未實作而失敗 <!-- speclink-task:tsk_01KZ2SHPGEGP6J0F9W7JM1BX0V -->
- [x] 4.2 在 `crates/speclink-core/src/review.rs`、`crates/speclink-cli/src/commands.rs`、`crates/speclink-cli/src/remote_commands.rs`、`crates/speclink-protocol/src/command.rs`、`crates/speclink-remote/src/client.rs`、`crates/speclink-server/src/routes.rs` 實作 nullable phase／patchHash parser、sequence guard 與 additive DTO mapping，stamp／reviewed_scope 行為不變；驗證：4.1 測試轉綠，`cargo test -p speclink-core review`、`cargo test -p speclink-protocol`、`cargo test -p speclink-server review_api` 通過 <!-- speclink-task:tsk_01KZ2SHPGESDSZS59XQ03AGBKS -->

## 5. 有界審查技能（D6 — Round 1 保留 Matt-compatible discovery 形狀；D7 — Round 2+ 是 remediation validation，嚴格進展決定終止）

- [x] 5.1 先在 `crates/speclink-core/tests/it/render_golden.rs` 與 golden fixtures 建立「審查流程的技能行為」「審查後的迴圈與收尾」「續輪重大晚發問題的安全退出」紅燈斷言：apply 在 in-progress 前 prepare；review 首輪同一 frozen patch 的 Standards／Correctness；續輪只驗原 finding＋direct regression；2→1 可續、1→1 failed、1→0 stamp、accepted 走 `--accept`；無關新 smell 不入工單、有證據重大問題 scope changed；驗證：`cargo test -p speclink-core --test it render_golden::` 因正典模板尚未更新而失敗 <!-- speclink-task:tsk_01KZ2SHPGEN7RSV7PMQSX7N932 -->
- [x] 5.2 在 `crates/speclink-core/assets/skills/apply.md`、`crates/speclink-core/assets/skills/review.md`、`crates/speclink-core/src/skills.rs` 實作 D6／D7／D8 技能流程，保持 smell baseline、locale、TDD 修復門與兩軸原報告不變，執行 `speclink update` 再生 claude／codex golden；驗證：5.1 轉綠，生成技能不含整檔續輪 discovery 或 `<base>...HEAD` <!-- speclink-task:tsk_01KZ2SHPGE6JXM8AZ7TA726TEN -->

## 6. 整合驗證與 sharp-edges audit

- [x] 6.1 以 `crates/speclink-cli/tests/it/review_verbs.rs` 建立完整 fixture：prepare→task touched→discovery scope→structured Round 1→remediation validation→2→1 續跑／1→1 未通過／1→0 蓋章／accepted 帶保留，並對 `--base`、`--candidate-hash`、`--include-hunk` 執行 sharp-edges audit（空值、重複、漂移、binary、路徑穿越皆 fail closed）；驗證：fixture 全綠且沒有 silent fallback <!-- speclink-task:tsk_01KZ2SHPGEA4JZMN132G459DJC -->
- [x] 6.2 執行 `cargo test -p speclink-host`、`cargo test -p speclink-core`、`cargo test -p speclink-cli`、`cargo test -p speclink-protocol`、`cargo test -p speclink-remote`、`cargo test -p speclink-server review_api`、`speclink analyze converge-review-remediation-rounds --json` 與 `speclink validate converge-review-remediation-rounds`；驗收 Critical／Warning 為 0、touched／list／stamp／archive 回歸對照未變，並記錄任何環境阻擋而不誤報完成 <!-- speclink-task:tsk_01KZ2SHPGETTH124Y5KMERD5YV -->
