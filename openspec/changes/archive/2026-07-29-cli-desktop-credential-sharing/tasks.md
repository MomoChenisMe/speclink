## 1. 憑證儲存與換發編排下沉（speclink-remote）

- [x] 1.1 撰寫 CredentialStore 單元測試：in-memory 實作對 origin＋kind（Refresh／Pat／Bearer）的 get／set／delete 隔離性、未知條目回 None。測試置於 `crates/speclink-remote/src/credentials.rs` 的 `#[cfg(test)]` 模組；驗證：cargo test -p speclink-remote 新測試紅燈（模組尚不存在）後轉綠。 <!-- speclink-task:tsk_01KYMJMA97PAJB53XFW8J4V9F1 -->
- [x] 1.2 實作「CredentialStore 與換發編排下沉至 speclink-remote」：CredentialStore trait、CredentialKind（含新增 Bearer）、keyring 生產實作與 in-memory 測試實作自 `apps/desktop/src-tauri/src/credentials.rs` 移入 `crates/speclink-remote/src/credentials.rs`，keyring 相依自 `apps/desktop/src-tauri/Cargo.toml` 移至 `crates/speclink-remote/Cargo.toml`，Keychain service 與 account（kind:origin）字串逐字不變。驗證：cargo test -p speclink-remote 綠；cargo build 全 workspace 通過（desktop 暫以 re-export 相容）。 <!-- speclink-task:tsk_01KYMJMA97DWCYFB0T5HT9580J -->
- [x] 1.3 撰寫「共用 credential family 與換發序列化」測試後實作「檔案鎖序列化跨行程換發」：`crates/speclink-remote/src/refresh.rs` 提供持鎖換發編排——使用者設定目錄獨立鎖檔、鎖內二次讀取 refresh 條目、rotation 成功即回寫新 refresh。測試（in-memory store＋stub server）：兩執行緒併發請求換發，斷言 server 僅收到一次換發請求、兩方皆取得可用 bearer、無 reuse 錯誤。驗證：cargo test -p speclink-remote 綠。 <!-- speclink-task:tsk_01KYMJMA97PX8JNEFAHJJR76TD -->
- [x] 1.4 撰寫測試後實作「Bearer 快取入 Keychain 避免逐指令 rotation」：access token 連同絕對到期時刻以 JSON（token、expiresAt）存入 Bearer 條目；未到期直接使用且不取鎖不換發、到期或缺席才走 1.3 的持鎖換發、server 回未授權時強制換發重試恰一次。測試斷言：快取命中時 stub server 收到 0 次換發請求；條目值無法解析視為無快取。驗證：cargo test -p speclink-remote 綠。 <!-- speclink-task:tsk_01KYMJMA975S35T610JB6X3DYM -->

## 2. CLI 憑證解析階梯

- [x] 2.1 撰寫「憑證解析階梯」順序測試：SPECLINK_TOKEN → 金鑰圈 refresh → 金鑰圈 PAT → credentials.yaml PAT 的命中順序、金鑰圈不可用時靜默下探、四層皆空報未登入。測試以 in-memory store 注入、不觸真實 OS keyring；置於 `crates/speclink-remote/tests/auth_store.rs` 或同層新測試檔。驗證：cargo test -p speclink-remote 紅燈後轉綠。 <!-- speclink-task:tsk_01KYMJMA97HHR314AGEE2FA1S5 -->
- [x] 2.2 實作「CLI 憑證解析階梯與靜默下探」：`crates/speclink-remote/src/auth.rs` 的解析入口整合四層階梯並回報來源層，`crates/speclink-cli/src/remote_commands.rs` 的 remote 動詞前置改用之；既有 env → credentials.yaml 測試不改仍綠。驗證：cargo test -p speclink-remote 與 crates/speclink-cli 既有整合測試（remote_read_path、remote_write_path）全綠。 <!-- speclink-task:tsk_01KYMJMA97VGVEW0WZ3448V2Z0 -->
- [x] 2.3 撰寫測試後實作「憑證失效的處理」新語意：憑證來源為金鑰圈 refresh 的 remote 動詞收到未授權時同族換發重試恰一次；family 已撤銷則清除金鑰圈 refresh 與 bearer 條目、非 0 exit code 提示 speclink auth login、該次執行不改用其他層；env／PAT 來源維持既有不重試行為。重試編排下沉 `crates/speclink-remote/src/auth.rs`（CLI 動詞前置只做委派），因為 CLI 整合測試以子行程執行，會觸到開發者的真實 OS Keychain——design「測試不得依賴真實 OS keyring」禁止之。驗證：`crates/speclink-remote/tests/reauth_retry.rs` 以 in-memory store＋stub server 覆蓋「快取被拒→換發重試恰一次」「重試至多一次」「換發被拒→清除金鑰圈登入並失敗」「PAT 不重試」「403 不換發」；`crates/speclink-remote/tests/credential_ladder.rs` 覆蓋「family 撤銷該次不下探、下次執行以剩餘層成功」；CLI 既有 remote 整合測試零改動仍綠。 <!-- speclink-task:tsk_01KYMJMA970K73DYWWKEWT8N4Y -->

## 3. auth 指令面（speclink auth login／status／logout）

- [x] 3.1 撰寫測試後實作「auth login 雙軌與 TTY 判定」，覆蓋規格「裝置授權登入」與「PAT 登入與憑證儲存」：互動 TTY 無旗標走裝置授權（stdout 印 verification URL 與 user code、輪詢、核准後 refresh 與 bearer 入金鑰圈）；--pat 互動貼 PAT 入 credentials.yaml；--token-stdin 位元級不變；--pat 與 --token-stdin 互斥非 0；非互動無旗標非 0 指引 --token-stdin；金鑰圈不可用非 0 指引 --pat；denied／expired 訊息可區分。旗標定義於 `crates/speclink-cli/src/main.rs`，流程於 `crates/speclink-cli/src/remote_commands.rs`。裝置授權編排下沉 `crates/speclink-remote/src/login.rs`（終端／瀏覽器／時鐘以 DeviceLoginIo 注入），CLI 只實作 IO 與委派——CLI 整合測試驅動真實二進位、會觸到真實 OS Keychain，design 禁止之。驗證：`crates/speclink-remote/tests/device_login_flow.rs` 覆蓋核准存雙憑證、網址與裝置碼必印、瀏覽器 URL 預填 user code、依宣告間隔輪詢、slow_down 加寬、denied／expired／unsupported 各為獨立結果、金鑰圈不可寫即失敗；`crates/speclink-cli/tests/remote_connect.rs` 覆蓋 --pat 貼入憑證檔、--pat 與 --token-stdin 互斥非 0、無終端非 0 指引 --token-stdin；--token-stdin 既有測試零改動仍綠。 <!-- speclink-task:tsk_01KYMJMA976JSRVME3YT1FYNB4 -->
- [x] 3.2 撰寫測試後實作「auth status 顯示憑證來源層」：人眼輸出增列來源描述、--json 新增 credentialSource 欄位（camelCase，值域 env、keychain_refresh、keychain_pat、credentials_file），既有欄位不變。auth status 本無 --json，本任務一併新增該旗標。驗證：`crates/speclink-cli/tests/remote_connect.rs` 斷言人眼輸出點名來源層、--json payload 的 credentialSource 為 env 與 credentials_file 且 user 欄位仍在。 <!-- speclink-task:tsk_01KYMJMA97DT7XQJX42E8C90MB -->
- [x] 3.3 撰寫測試後實作「auth logout 的撤銷與本機清除語意」，覆蓋規格「登出」：有 refresh 時呼叫撤銷端點撤銷 credential family、清除該 origin 金鑰圈全部條目與憑證檔條目、server 端 PAT 不撤銷；無憑證非 0 報未登入；撤銷網路失敗仍清本機、exit 0、stderr 警告。子指令定義於 `crates/speclink-cli/src/main.rs`。驗證：`crates/speclink-cli/tests/remote_connect.rs` 覆蓋未登入非 0、憑證檔條目被清除且後續 status 非 0（金鑰圈側的撤銷與清除同樣無法在 CLI 子行程測試，由 speclink-remote 的 clear_all_local_credentials 單元覆蓋）。 <!-- speclink-task:tsk_01KYMJMA970T3E7KNTBECHGCHX -->

## 4. desktop 改用下沉層

- [x] 4.1 desktop 換發與憑證存取改呼叫 speclink-remote 下沉層：`apps/desktop/src-tauri/src/connections.rs` 的 refresh 編排與 `apps/desktop/src-tauri/src/remote.rs` 的 bearer 取得改用 `crates/speclink-remote` 的持鎖換發與 bearer 快取（記憶體內 bearer 快路徑保留，換發成功同步回寫金鑰圈），刪除 `apps/desktop/src-tauri/src/credentials.rs`。行為契約：desktop 登入／登出／換發的使用者可見行為不變。驗證：cargo test -p speclink-desktop 既有連線與登入測試零場景刪減全綠；cargo build --release -p speclink-desktop 通過。 <!-- speclink-task:tsk_01KYMJMA977ZS8HRSZP43QG0AR -->

## 5. 回歸與收尾

- [x] 5.1 輸出回歸對照：確認 render golden 與 CLI 輸出測試的 diff 僅含本提案記載的刻意變更（speclink auth login 互動預設改裝置授權、speclink auth status 新增來源列與 credentialSource 欄位）；golden 需再生時於乾淨樹執行 UPDATE_GOLDEN=1 cargo test -p speclink-core --test render_golden 並審視 diff。結果：golden 快照不含任何 auth 輸出、`crates/speclink-core/` 零 diff——刻意變更（auth login 預設流程、auth status 新列與 credentialSource）不觸及 render golden，無需再生。驗證：cargo test -p speclink-core --test render_golden 綠（8）、crates/speclink-cli 全部整合測試綠。 <!-- speclink-task:tsk_01KYMJMA98EH3BV64M5QKS8NJ3 -->
- [x] 5.2 端到端驗收「desktop 登入後 CLI 免登入」。已自動驗證（本機 server ＋ HTTP 核准 ＋ pty，見 scratchpad/e2e.sh 的等價流程）：CLI 裝置授權登入印出網址與裝置碼、核准後 refresh 與 access token 寫入真實 macOS Keychain、`auth status --json` 回 credentialSource=keychain_refresh、`list --json` 全程免登入成功、`auth logout` 後條目清空且 status 非 0；另附帶驗證金鑰圈不可用時 auth login 非 0 並指引 --pat。 <!-- speclink-task:tsk_01KYNT7PG1T5XQYVAXQVQTJYXN -->
  **未完成的一項**：跨 binary 讀取（desktop 寫入 → CLI 讀取）。實測 CLI 首次讀取 desktop 建立的條目會觸發 macOS 授權對話框並阻塞——即 design 風險欄「macOS Keychain ACL」確認成真。尚未驗證的是「按『一律允許』後是否對後續 CLI 呼叫持續生效」，此步需真人點擊。
  驗證步驟：在指向已由 desktop 登入之 server 的 checkout 執行 `env -u SPECLINK_TOKEN speclink auth status`，於對話框按「一律允許」，再執行第二次——第二次若不再跳提示即通過；若每次都跳，代表共用體驗在 macOS 上需要額外處理（簽章一致化，或由 desktop 代為佈建 PAT 的折衷）。 <!-- speclink-task:tsk_01KYMJMA98BYM0JRY38W0G3MW9 -->
- [x] 5.3 全量驗證：cargo test 全 workspace 綠、cargo build --release 通過；speclink validate cli-desktop-credential-sharing 通過。 <!-- speclink-task:tsk_01KYMJMA986VN3GVSCRD57NAM5 -->
