## 1. 更新日誌 JSON 為唯一真相與渲染腳本一支三用

- [x] 1.1 先寫紅燈測試 `scripts/release-notes-render.test.mjs`：renderEntry 對固定條目輸出 `## 0.3.0（2026-09-10）` 加 `### 新功能`／`### 修正` 分組與 `- ` 條目；renderChangelog 首行 `# 更新日誌`、條目依序、檔尾單一換行、LF；CLI `--write` 寫出 CHANGELOG.md exit 0、`--version 0.9.9` 找不到版本 exit 1 且 stderr 含頂端版號、`--check` 一致 exit 0、手改一字後 exit 1 且 stderr 含差異摘要、CHANGELOG.md 以 CRLF 讀入時 `--check` 仍 exit 0。驗證：`node --test scripts/*.test.mjs` 新測試紅。 <!-- speclink-task:tsk_01M22EK2869PV214Y79J8P16X8 -->
- [x] 1.2 實作渲染腳本一支三用——渲染腳本由 JSON 產出 CHANGELOG.md 與單版片段：`scripts/release-notes-render.mjs`（純 Node、無依賴），匯出 renderEntry／renderChangelog，CLI 三用法 `--write`／`--version X.Y.Z`／`--check`，JSON 路徑固定為 `apps/desktop/src/release-notes/release-notes.json`、CHANGELOG.md 由 repo 根解析（node:path），比對前 CRLF 正規化。驗證：`node --test scripts/*.test.mjs` 全綠。 <!-- speclink-task:tsk_01M22EK286F5KS83G18XXQ19AK -->
- [x] 1.3 落實更新日誌 JSON 為唯一真相：建立 `apps/desktop/src/release-notes/release-notes.json` 與型別檔 `apps/desktop/src/release-notes/release-notes.ts`（匯出 ReleaseNotesEntry／ReleaseNotesSection 型別與 RELEASE_NOTES 常數，title 收窄為「新功能／修正／改善」字面聯集）；JSON 首筆為回填 0.2.0 與詞彙的回填條目：依 release-skill 的策展規則從 `git log v0.1.3..v0.2.0` 改寫（封存變更一變更一條、雜訊不進、使用者語氣），date 取 v0.2.0 tag 的提交日期。驗證：`node scripts/release-notes-render.mjs --write` 產出 repo 根 `CHANGELOG.md`，`node scripts/release-notes-render.mjs --check` exit 0；內容審閱——沒有任何條目照抄 commit 主旨、沒有 chore／test／refactor 條目。 <!-- speclink-task:tsk_01M22EK286STYPFTPRJA4M5NM7 -->

## 2. Release 說明含下載指南接該版片段與 release job 守門

- [x] 2.1 先寫紅燈測試於 `scripts/release-notes.test.mjs`：以 tag v0.2.0 執行 `scripts/release-notes.mjs` 的輸出在下載指南（`---` 分隔線）之後含 `## 0.2.0（` 片段；以 JSON 沒有的 tag（如 v9.9.9，仍符 vX.Y.Z）執行時 exit 1 且 stderr 說明缺該版。驗證：`node --test scripts/*.test.mjs` 新測試紅。 <!-- speclink-task:tsk_01M22EK286A7JRF7278Y8BXBS0 -->
- [x] 2.2 Release 說明含下載指南接該版片段：修改 `scripts/release-notes.mjs`，印完指南後 import renderEntry 找出 tag 對應版本條目接在指南後，JSON 缺該版即非零結束；既有指南內容與檔名對照表位元級不變。驗證：`node --test scripts/*.test.mjs` 全綠（含既有指南測試未改動）。 <!-- speclink-task:tsk_01M22EK2863MD9A6TKHQT5BPKS -->
- [x] 2.3 `.github/workflows/release.yml`：desktop job 既有「Assert tag matches desktop version」步驟擴充（design：release job 守門由三處同版擴為四處並比對渲染；spec：release 管線守門版號四處同版與渲染一致）——加斷言 release-notes.json 頂端 version 等於 tag 去 v（`::error::` 點名兩邊版號）並執行 `node scripts/release-notes-render.mjs --check`；標頭 Usage 註解改為「執行 /release 技能」。驗證：以本機 shell 模擬該步驟三種不齊情境（tag≠conf、tag≠JSON 頂端、CHANGELOG 漂移）各回非零、齊時回零，並以 `node -e` 解析 YAML 確認語法有效。 <!-- speclink-task:tsk_01M22EK2868Y4QJ26RPT5XMTW7 -->

## 3. 桌面更新日誌純判定與 modal 兩模式

- [x] 3.1 先寫紅燈測試 `apps/desktop/src/__tests__/whatsNew.test.ts`：whatsNewDecision 六種情境——dev 為真→none；頂端版號≠appVersion→none；lastSeen 為 null→record；lastSeen 等於 appVersion→none；lastSeen 0.2.0、清單 0.3.1／0.3.0／0.2.0→show 含 0.3.1 與 0.3.0；lastSeen 不在清單→show 只含頂端；readLastSeenVersion 對壞值（非字串／JSON）回 null、writeLastSeenVersion 寫入鍵 `speclink.lastSeenVersion`。驗證：`npm test -w apps/desktop -- whatsNew` 紅。 <!-- speclink-task:tsk_01M22EK2865MWTWNHPR5P3N4E8 -->
- [x] 3.2 桌面更新日誌純判定與 modal 兩模式之判定半：實作 `apps/desktop/src/core/whatsNew.ts`，純函式 whatsNewDecision（規則依 design 順序）與 localStorage 讀寫（Storage 可注入，比照 assetPrompt.ts）。驗證：`npm test -w apps/desktop -- whatsNew` 綠。 <!-- speclink-task:tsk_01M22EK2864PQK23Y35MBH5TZ0 -->
- [x] 3.3 先寫紅燈測試 `apps/desktop/src/__tests__/releaseNotesDialog.test.tsx`：whatsNew 模式標題「0.3.1 更新內容」、主鈕「知道了」按下呼叫 onOpenChange(false)；browse 模式標題「更新日誌」、主鈕「關閉」、列出全部條目的「版號（日期）」小標與三類分組；entries 為空時顯示 `releaseNotes.empty` 文案。驗證：`npm test -w apps/desktop -- releaseNotesDialog` 紅。 <!-- speclink-task:tsk_01M22EK2869TDV4B729ZD9YJCT -->
- [x] 3.4 實作 `apps/desktop/src/components/ReleaseNotesDialog.tsx`（@speclink/ui AlertDialog，MigrationDialog 先例；內容區可捲動、寬度沿 markdown 行寬上限）並於 `apps/desktop/src/i18n/messages.ts` 補 zh-TW 與 en 的 `releaseNotes.whatsNewTitle`（含 {version}）、`releaseNotes.browseTitle`、`releaseNotes.gotIt`、`releaseNotes.close`、`releaseNotes.open`、`releaseNotes.empty`。驗證：`npm test -w apps/desktop -- releaseNotesDialog` 綠。 <!-- speclink-task:tsk_01M22EK286RPK5TQNPA1738JQR -->
- [x] 3.5 先寫紅燈測試於 `apps/desktop/src/__tests__/appSettingsView.test.tsx`：更新卡多出「更新日誌」按鈕，按下呼叫 onShowReleaseNotes；updater 未注入時按鈕不存在。驗證：`npm test -w apps/desktop -- appSettingsView` 紅。 <!-- speclink-task:tsk_01M22EK286Y508P3M6EM2K6ZNW -->
- [x] 3.6 接線更新日誌彈窗：`apps/desktop/src/views/AppSettingsView.tsx` 的 AppSettingsUpdaterProps 加 onShowReleaseNotes 與按鈕；`apps/desktop/src/App.tsx` 以 React 本地狀態掛 ReleaseNotesDialog——既有取 appVersion 的 effect 之後呼叫 whatsNewDecision（dev 傳 import.meta.env.DEV、entries 傳 RELEASE_NOTES、lastSeen 由 readLastSeenVersion 讀）：show→開 whatsNew 模式、record→只寫 lastSeen；whatsNew 模式關閉時寫 appVersion、browse 模式關閉不寫。驗證：`npm test -w apps/desktop -- appSettingsView` 綠，且 `npm test -w apps/desktop` 全綠、零 act 警告。 <!-- speclink-task:tsk_01M22EK286SDAQ6TQB707R9P03 -->
- [x] [M] 3.7 以 `node scripts/desktop-install.mjs --install` 安裝本機建置後手動驗證：先在 localStorage 把 `speclink.lastSeenVersion` 設為 0.1.0 再啟動 app，看到「<現版號> 更新內容」對話框且按「知道了」後重啟不再彈；清掉該鍵後啟動不彈且鍵值變為現版號；設定頁「更新日誌」按鈕開瀏覽模式、關閉後鍵值不變；`npm run tauri dev` 啟動的 dev 版不彈。 <!-- speclink-task:tsk_01M22EK286NYBV42HZJKW7XJFM -->

## 4. 發版技能為 repo 本地技能、執行即發版、push 前停一次

- [x] 4.1 發版技能為 repo 本地技能、執行即發版、push 前停一次：撰寫 `.claude/skills/release/SKILL.md`（frontmatter name: release、description 說明「執行即發版」與觸發語），內文依序涵蓋：守門不過即停且不改動工作樹（main、樹乾淨、與 origin/main 同步、`gh run list --workflow CI --commit <sha> --json conclusion` 為 success、in-progress 變更列出請確認）；版號由上次 tag 到 HEAD 的 commit 判定（type 最高影響、0.x breaking 封頂 minor、雜訊警告、無法解析當 patch 並列出、使用者可覆寫）；更新日誌策展為使用者語氣的繁中條目（封存變更優先一變更一條、雜訊不進、技術名詞保留、不照抄主旨、三分組）；呈現草稿並在使用者點頭後才寫檔（固定順序：JSON prepend→Cargo.toml→tauri.conf.json→cargo metadata→`--write`→`node --test scripts/*.test.mjs` 與 `--check`；失敗停在該步不 commit）；提交、附註 tag 與原子 push（`chore(release): 版號升至 X.Y.Z`、`git tag -a vX.Y.Z -m vX.Y.Z`、`git push --atomic origin main vX.Y.Z`、失敗印重試指令、成功印 Release 工作流網址與三項收尾提醒）。驗證：內容審閱對照 release-skill 規格六條需求逐條有對應段落；`ls .claude/skills/release` 目錄名不以 `speclink-` 開頭；`speclink update` 後 `git status` 顯示該目錄未被動。 <!-- speclink-task:tsk_01M22EK286TXFQ1YEH6T25H2QZ -->
- [x] [M] 4.2 在乾淨 main 上執行 `/release` 走到「停一次」但不點頭：確認守門四項輸出、建議版號與理由、分組草稿；再於工作樹故意留一個未提交檔重跑，確認技能印出「工作樹不乾淨」並停止且未改任何檔。 <!-- speclink-task:tsk_01M22EK286KJXSXQF5T8X9JFMG -->

## 5. 詞彙與收尾

- [x] 5.1 `openspec/LANGUAGE.md` 新增詞條「更新日誌」（definition：每個 release 版本的白話更新內容，JSON 為真相、CHANGELOG.md 與 Release 說明為衍生，桌面設定頁按鈕與瀏覽模式標題；avoid：更新資訊、release notes、changelog（使用者可見文案中）；why：與「軟體更新」卡同族、「日誌」點明逐版紀錄；來源 release-changelog-whats-new，2026-09-09）。驗證：`node --test scripts/*.test.mjs` 全綠（vocabulary-guard 對 README 與 docs 面無新命中；有命中則同批改掉）。 <!-- speclink-task:tsk_01M22EK286XV0T7F1J2T2G38FZ -->
