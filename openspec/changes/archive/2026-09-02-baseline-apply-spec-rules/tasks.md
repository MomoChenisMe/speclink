## 1. 測試先行（Red）

- [x] 1.1 依「測試落點 render_golden」在 crates/speclink-core/tests/it/render_golden.rs 新增測試 baseline_skill_loads_workflow_config_and_applies_specs_rules：以 skill_for_both_tools("baseline-rules", "baseline") 讀 claude 與 codex 兩側生成檔，斷言 design Implementation Contract 的七個正向 needle（speclink workflow-config show --json、rules.specs、MUST honour every entry、none (no rules.specs configured)、Specs rules applied this run、checks structure only、never fall back to reading）存在、負向 needle（Read `openspec/config.yaml`）不存在；對應「盤點前取得 workflow config 並套用 specs 產出規則」的技能檔載明入口、規則屬 Agent 指令兩個 scenario。驗證：cargo test -p speclink-core --test it baseline_skill_ 此時失敗（Red），失敗訊息指出 needle 缺席。 <!-- speclink-task:tsk_01M1GK3ZKECZZ6JCWP9PZ6766H -->

## 2. 技能 asset（Green）

- [x] 2.1 依「設定入口沿用 workflow-config show」改寫 crates/speclink-core/assets/skills/baseline.md 的 Step 1：標題含 load the workflow config；程式碼區塊加入 speclink workflow-config show --json；刪除直讀 config.yaml 的句子；新增 context、specLocale（null＝英文、auto＝跟隨 locale、語系代碼＝該語言，結構標記與 SHALL/MUST 維持英文）、rules.specs 三欄位說明；新增指令失敗即回報並停止、never fall back to reading config.yaml by hand 的句子。驗證：對照 design 的 Implementation Contract 逐句核對；rg "workflow-config show --json" crates/speclink-core/assets/skills/baseline.md 命中。 <!-- speclink-task:tsk_01M1GK3ZKE2YWPD48NNG591PR0 -->
- [x] 2.2 依「揭露段固定格式」改寫 Step 3 與 Step 5：capability map 確認訊息 MUST 附揭露段（有規則時「Specs rules applied this run (from rules.specs, N entries):」加逐條編號原文；無規則時單行「Specs rules applied this run: none (no rules.specs configured)」）；Step 5 的報告項目新增同一段。對應「盤點前取得 workflow config 並套用 specs 產出規則」的設定了 rules.specs 時的套用與揭露、未設定時行為不變兩個 scenario。驗證：rg -c "Specs rules applied this run" crates/speclink-core/assets/skills/baseline.md 為 2 以上。 <!-- speclink-task:tsk_01M1GK3ZKEZ49H4JPMBQQ4AVGQ -->
- [x] 2.3 依「規則逐字套用不篩選」改寫 Step 4 的 Rules 與 Guardrails：新增「Honour every rules.specs entry — every spec you write MUST honour every entry verbatim; do not filter by whether it applies to a baseline」與「speclink validate checks structure only; these rules are instructions for what you write」；Guardrails 新增 Don't apply rules silently、Don't hand-read config.yaml 兩條；Step 2、Step 4 模板、既有 Guardrails 與 Next steps 逐字不動，維持「基準盤點的行為邊界」六項既有語意（只記現況、不改 code、不建 change、只補缺、確認後才寫、最後 strict validation）。驗證：git diff crates/speclink-core/assets/skills/baseline.md 只含上述段落；出口仍只有 propose 與 discuss 兩條。 <!-- speclink-task:tsk_01M1GK3ZKEJCDKW9FHZV0GW7CX -->
- [x] 2.4 測試轉綠：cargo test -p speclink-core --test it baseline_skill_ 通過（Green）；render_golden 五份快照此時預期不符，交由 3.2 再生。驗證：該測試 exit code 0。 <!-- speclink-task:tsk_01M1GK3ZKE8CYX2RS821VK90NJ -->

## 3. 版號與衍生物

- [x] 3.1 依「版號 v1.27.0 與衍生物再生」把 crates/speclink-core/src/init.rs 的 ASSET_VERSION 由 v1.26.1 改為 v1.27.0；CLI 的版本輸出顯示 engine v1.27.0。驗證：cargo run -p speclink-cli -- --version 輸出含 v1.27.0。 <!-- speclink-task:tsk_01M1GK3ZKEKK37QHD2YMBBEP26 -->
- [x] 3.2 再生五份 golden（crates/speclink-core/tests/golden/claude.snapshot.md、crates/speclink-core/tests/golden/codex.snapshot.md、crates/speclink-core/tests/golden/neutral-cli.snapshot.md、crates/speclink-core/tests/golden/neutral-tool-call.snapshot.md、crates/speclink-core/tests/golden/claude-worktree.snapshot.md）與 crates/speclink-core/tests/golden/assets.lock：先以 git status 確認樹上只有本 change 的異動，執行 UPDATE_GOLDEN=1 cargo test -p speclink-core --test it render_golden::，再執行 UPDATE_ASSETS_LOCK=1 同指令；對應「內嵌 speclink-baseline 技能的渲染與保護」的 golden 保護與其他技能只變版本戳兩個 scenario。驗證：git diff 五份 golden 只含 baseline 段落與版本戳；不帶環境變數重跑 cargo test -p speclink-core --test it 全綠。 <!-- speclink-task:tsk_01M1GK3ZKE35YESQAW5K2ZGMP7 -->

## 4. repo 生成物與文件

- [x] 4.1 以本次建置的 CLI 於 repo 根執行 cargo run -p speclink-cli -- update（不用 ~/.local/bin 的安裝版）：.claude/skills/speclink-baseline/SKILL.md 與 .agents/skills/speclink-baseline/SKILL.md 含 design 表列的全部正向 needle；.claude/skills/ 與 .agents/skills/ 全部 speclink-* SKILL.md 的 frontmatter 版本戳為 v1.27.0，其他技能內文不變。對應「內嵌 speclink-baseline 技能的渲染與保護」的 init 與 update 渲染技能 scenario。驗證：rg "workflow-config show --json" .claude/skills/speclink-baseline/SKILL.md .agents/skills/speclink-baseline/SKILL.md 各命中；rg -L "v1.27.0" .claude/skills/*/SKILL.md .agents/skills/*/SKILL.md 零檔；git status 盤點異動檔全部入列。 <!-- speclink-task:tsk_01M1GK3ZKENJGMEXHYB0RZEQ7E -->
- [x] 4.2 docs/workflow.md 與 docs/workflow.zh-TW.md 的 baseline 站 Input 行補上 workflow config：英文列 the workflow config from speclink workflow-config show --json (context, spec_locale, rules.specs)；中文列以 speclink workflow-config show --json 取得的 workflow config（專案說明、spec_locale 與 specs 產出規則 rules.specs）；兩語言同段對等，其他行不動。驗證：node --test scripts/vocabulary-guard.test.mjs 通過；rg "workflow-config show --json" docs/workflow.md docs/workflow.zh-TW.md 各命中一次。 <!-- speclink-task:tsk_01M1GK3ZKES45T3T65D95759PA -->

## 5. 收尾驗證

- [x] 5.1 「新 capability baseline-skill」的 delta 與整體規格通過驗證：speclink validate baseline-apply-spec-rules 與 speclink validate --specs --all --strict 皆 exit 0；cargo fmt --all -- --check 無差異；cargo test -p speclink-core --test it 全綠。驗證：四個指令的 exit code 均為 0，輸出摘錄入 change 紀錄。 <!-- speclink-task:tsk_01M1GK3ZKEWKRCHRPGG0K9JGXA -->
- [x] 5.2 收尾盤點：git status 列出的異動限 design Impact 清單（asset、crates/speclink-core/src/init.rs、crates/speclink-core/tests/it/render_golden.rs、五份 golden、assets.lock、docs/workflow 兩份、.claude 與 .agents 生成物、openspec/changes/baseline-apply-spec-rules/）；不使用 git add -A 或 git add .；不提交、不推送，交使用者檢查。驗證：git status --short 逐行對照 Impact 清單，無清單外檔案。 <!-- speclink-task:tsk_01M1GK3ZKE8QF12S5HC38CM7ZF -->
