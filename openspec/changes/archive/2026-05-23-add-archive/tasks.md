## 0. Spec ↔ task traceability checklist

- [x] 0.1 確認所有 spec requirement 名稱（archive-runner 11 條、state-machine ADDED 3 條 + MODIFIED 1 條）皆以子字串形式出現在後續至少一條 task 描述內；驗證方式：`spectra analyze add-archive` 無 Critical/Warning
- [x] 0.2 確認 design.md 所有 `###` 決策標題（Spec delta merge：整檔覆蓋 vs schema-aware diff、Lock acquisition：stub no-op vs 真 lock、State guard：兩條件 AND vs 完整 6-state check、`--no-validate` flag 行為：硬編 no-op vs 拒絕未實作、Archive 目錄命名：日期前綴 vs 純 change-id、State.db v4 migration：加 column vs 新表、Audit event：reuse `state_transition` row vs 新表、Observable behavior、CLI surface、JSON envelope shape、Provider trait surface、Acceptance criteria、Scope boundaries）皆在後續至少一條 task 描述內出現

## 1. Pre-apply 前置檢查與 slice 命名同步

- [x] 1.1 Apply 啟動時 verify `openspec/specs/state-machine/spec.md` 與 `openspec/specs/apply-task-ops/spec.md` 都已存在於 working tree（A3 已 archive 於 commit 3933dec）；缺失即 abort 並提示 user 跑 `/spectra-archive add-state-machine-and-apply`；驗證方式：在 `crates/runtime/src/dev_precheck.rs` 既有 helper 擴充 `precheck_a3_archived()` 於 fixture 用 with/without spec dir 兩種情境跑 unit test
- [x] 1.2 在 `doc/speclink-design.md` §1.1「Walking skeleton slice naming」表格新增 A4 列：`add-archive` / 「state.db v4 migration（`archived_at` 欄位）+ `archive.run` op + `ArchiveStore` trait + spec delta dumb merge + `.speclink/changes/archive/<YYYY-MM-DD>-<id>/` rename + `in_progress + all_tasks_done=1 → archived` transition + 2 新 error code (`change.tasks_incomplete` / `validation.archive_failed` 後者保留) + `archive.specs_skipped` warning carrier」；驗證方式：grep `A4 |` 在 design.md 出現一次、`add-archive` 字串出現於該表

## 2. Provider trait 與共用型別擴充（crates/provider）

- [x] 2.1 [P] 觸發 `rust-skills:m05-type-driven`；為 `ArchiveRequest { change_id, skip_specs, no_validate, yes }`、`ArchiveResult { change_id, state, merged_specs, archived_at, archive_dir }`、`MergedSpec { capability, lines_added, lines_removed }`、`StateTransitionReason` 既有 enum 新增 `ArchiveRun` + `ArchiveRunRevert` 兩個 variant 撰寫 serde 雙向 roundtrip 與 enum 列舉值斷言測試於 `crates/provider/src/types.rs::tests`（red）；驗證方式：`cargo test -p speclink_provider --lib types::tests` 跑紅；對應決策「Audit event：reuse `state_transition` row vs 新表」
- [x] 2.2 [P] 在 `crates/provider/src/types.rs` 實作 2.1 型別與 enum variant 並讓測試轉綠；`StateTransitionReason::ArchiveRun` 序列化字串為 `archive_run`、`StateTransitionReason::ArchiveRunRevert` 序列化為 `archive_run_revert`，對應 state-machine spec requirement「`StateTransitionReason` enum SHALL include `ArchiveRun`」（green）；驗證方式：2.1 測試全綠 + `cargo build -p speclink_provider`
- [x] 2.3 [P] 觸發 `rust-skills:m13-domain-error`；於 `crates/provider/src/error.rs::ProviderError` 新增 2 個 variant `ChangeTasksIncomplete` 與 `ValidationArchiveFailed`，並在 `codes` module 新增 `pub const CHANGE_TASKS_INCOMPLETE: &str = "change.tasks_incomplete"`、`pub const VALIDATION_ARCHIVE_FAILED: &str = "validation.archive_failed"`、`pub const ARCHIVE_SPECS_SKIPPED: &str = "archive.specs_skipped"` 三個 dot-separated 常量；對應 archive-runner spec requirement「New error codes SHALL be registered with stable exit codes」；驗證方式：`crates/provider/tests/error_codes.rs` 斷言 3 個常量字串與 ProviderError → code 對應正確（含 `ValidationArchiveFailed` 是 reserved 但 trait 表面必須存在）
- [x] 2.4 在 `crates/provider/src/lib.rs` 宣告 `ArchiveStore` trait（決策「Provider trait surface」），介面 method 為 `fn archive_change(&self, req: ArchiveRequest) -> Result<ArchiveResult, ProviderError>`；對應 archive-runner spec requirement「Provider trait SHALL expose `ArchiveStore` with a single `archive_change` method」；驗證方式：擴充 `crates/provider/tests/trait_surface.rs` 加 compile-only 測試斷言 trait method 簽章存在且不暴露 setter
- [x] 2.5 觸發 `rust-skills:m15-anti-pattern` + `rust-skills:coding-guidelines` 對 2.1–2.4 重構；驗證方式：`cargo fmt --check` + `cargo clippy -p speclink_provider -- -D warnings` 全綠

## 3. State.db v4 migration（crates/provider-local）

- [x] 3.1 寫 v4 migration integration test 在 `crates/provider-local/tests/migration_v4.rs`，使用 `tempfile` 建 empty db → apply v1 → v2 → v3 → v4 後斷言 `change` 表多出 `archived_at TIMESTAMP NULL` 欄位、`_migrations` 表多一筆 `version=4` row、既有 `change` 行 `archived_at` 為 NULL；同檔再加 idempotency 測（重跑 migration 不報錯、欄位不再被加）與 partial-rollback 測（注入 ALTER TABLE 失敗後 retry 結果一致）；對應 archive-runner spec requirement「state.db SHALL be upgraded to version 4 with `archived_at` column on the `change` table」（red）；對應決策「State.db v4 migration：加 column vs 新表」
- [x] 3.2 在 `crates/provider-local/src/state_db.rs::MIGRATIONS` 陣列追加 v4 entry，包含 `ALTER TABLE change ADD COLUMN archived_at TIMESTAMP NULL`，並插入 `_migrations` 對齊「v4 migration: archived_at column」決策（green）；驗證方式：3.1 全綠
- [x] 3.3 在 `crates/provider-local/src/state_db.rs` 新增 helper method `set_archived_at(change_id, archived_at_iso_8601)` 走單一 rusqlite tx 完成 `change.archived_at` set + `change.version` CAS bump，配合既有 `update_change_state_cas` 在同一個 outer tx 內被 `archive_change` 呼叫；驗證方式：unit test 覆蓋成功 commit、CAS 失敗、archived_at 寫入 NULL → 值 兩種轉換（red+green）
- [x] 3.4 將 `crates/provider-local/src/store.rs::LocalProjectStore::open_state_db()` 內既有 `db.migrate(3)` 升為 `db.migrate(4)`；新增「v3 binary refuses v4 db」regression test：用 v4 schema seed 後限制 `MIGRATIONS.len() == 3` 開檔須拋 `StateDbSchemaInvalid { found: 4, supported: 3 }`；驗證方式：`cargo test -p speclink_provider_local --test migration_v4`
- [x] 3.5 觸發 `rust-skills:m15-anti-pattern` 重構；驗證方式：`cargo build -p speclink_provider_local` + `cargo test -p speclink_provider_local` + `cargo fmt --check` + `cargo clippy -p speclink_provider_local -- -D warnings` 全綠

## 4. LocalArchiveStore 實作（crates/provider-local）

- [x] 4.1 在 `crates/provider-local/tests/archive_store.rs` 寫 5 條測試：(a) happy path single-tx commit + dir rename + spec merge 順序對齊「Spec merge SHALL happen after the directory rename」；(b) state guard：5 個非法 from_state 全 reject `state.transition_invalid`、`in_progress + all_tasks_done=0` reject `change.tasks_incomplete`；(c) same-day collision append suffix `-2` / `-3`；(d) `--skip-specs` 路徑：dir rename + state transition 完成但 `.speclink/specs/` byte-for-byte 不變、warnings 含 `archive.specs_skipped` + `capabilities_skipped` 排序；(e) 注入 `std::fs::rename` 失敗模擬 best-effort revert，斷言 `change.state` 回到 `in_progress`、`archived_at` 清為 NULL、補一筆 `reason=archive_run_revert` 的 `state_transition` row；對應 archive-runner spec requirement「`speclink archive` SHALL transition the change from `in_progress` to `archived` when all tasks are done」「Spec delta merge SHALL atomically overwrite the target capability spec for each capability under the change」「`--skip-specs` SHALL bypass merge while still transitioning state and emit an audit warning」「Filesystem rename SHALL happen after SQLite transaction commit; best-effort revert SHALL run if rename fails」「Spec merge SHALL happen after the directory rename」（red）；對應決策「State guard：兩條件 AND vs 完整 6-state check」「Archive 目錄命名：日期前綴 vs 純 change-id」
- [x] 4.2 在 `crates/provider-local/src/archive_store.rs` 實作 `LocalArchiveStore`，walking-skeleton path 流程：(1) 進 archive op 前 call stub helper `acquire_change_exclusive(change_id)` + `acquire_global_short()` 取得 no-op guard、對應決策「Lock acquisition：stub no-op vs 真 lock」；(2) 讀 change row + state guard 雙條件 check；(3) 開 SQLite tx：CAS update `change.state='archived'` + `change.version+=1` + insert `state_transition(reason='archive_run')` + `change.archived_at=now_utc()`；(4) commit tx；(5) 計算 target archive dir 名（含同日重名 suffix）後 atomic `fs::rename`；(6) rename 成功才走 spec merge（或 `--skip-specs` 跳過）；rename 失敗則開新 tx 跑 revert path 對齊「best-effort revert」要求（green）；驗證方式：4.1 全綠
- [x] 4.3 在 `crates/provider-local/src/archive_store.rs` 新增 `merge_spec_files(archive_dir, skip_specs)` helper：遍歷 `<archive_dir>/specs/<capability>/spec.md` 每個檔、若 `skip_specs=true` 收集 capability 名後返回；否則對每份檔做 `(target_path, source_bytes) → tempfile + rename + line count` 並回 `MergedSpec` array；對應決策「Spec delta merge：整檔覆蓋 vs schema-aware diff」；驗證方式：在 4.1 (a)+(d) 兩個 case 加細測驗 merge 不寫 spec 時 `.speclink/specs/` mtime 不變
- [x] 4.4 在 `crates/provider-local/src/archive_store.rs` 新增 `resolve_archive_dir(base, change_id, date)` helper：以 `<base>/archive/<YYYY-MM-DD>-<change_id>` 作起手、若存在則 `<base>/archive/<YYYY-MM-DD>-<change_id>-2`、`-3`、…、上限 100 次後回 `ProviderError::Internal { msg }`；驗證方式：在 4.1 (c) 用迴圈造 100 個既存 dir 斷言 fail-fast、用 1 個既存 dir 斷言取得 `-2`
- [x] 4.5 在 `crates/provider-local/src/lib.rs` 加 `pub use archive_store::LocalArchiveStore;`；驗證方式：`cargo build -p speclink_provider_local` + `cargo test -p speclink_provider_local` + `cargo fmt --check` + `cargo clippy -p speclink_provider_local -- -D warnings`
- [x] 4.6 觸發 `rust-skills:m15-anti-pattern` 重構（提取 `LockStubs` mod 含兩個 no-op acquire fn、把 atomic ordering 註解進 `archive_change` doc comment）；驗證方式：fmt + clippy + 4.1 仍綠

## 5. State machine 模組擴充（crates/runtime）

- [x] 5.1 [P] 在 `crates/runtime/tests/state_machine.rs`（A3 既有檔擴充）新增 transition-table 矩陣測試覆蓋 A4 新加 2 行：(a) `(in_progress, archived, archive_run)` 合法當且僅當 `all_tasks_done=1`、(b) `(archived, in_progress, archive_run_revert)` 僅限 best-effort revert path 走、其他 caller 一律 reject；同檔加「archive.run is the only writer of archived」測：mock 其他 caller 試圖 `transition_state(_, _, ChangeState::Archived, _)` 須 reject；對應 state-machine spec requirement「Transitions to `archived` SHALL be driven exclusively by `archive.run`」「State machine SHALL enforce the legal transition table」（red）
- [x] 5.2 [P] 在 `crates/runtime/src/state_machine.rs` 既有 transition table 加 `(InProgress, Archived, ArchiveRun)` 條件 `all_tasks_done=1` 與 `(Archived, InProgress, ArchiveRunRevert)` 兩列；新增 `ensure_archive_writer_is_archive_run(reason)` guard helper 用於 `transition_state` 入口（任何非 `ArchiveRun` reason 試圖寫 archived → reject）；對應決策「State guard：兩條件 AND vs 完整 6-state check」（green）；驗證方式：5.1 全綠
- [x] 5.3 [P] 在 `crates/runtime/src/error.rs::RuntimeError` 加 2 個 variant `ChangeTasksIncomplete` 與 `ValidationArchiveFailed`；擴充 `code()` / `exit_code()` / `retryable()` match arm（`change.tasks_incomplete`→2/`no`、`validation.archive_failed`→3/`no`）；對應 archive-runner spec requirement「New error codes SHALL be registered with stable exit codes」；驗證方式：`crates/runtime/tests/error_mapping.rs` 既有 snapshot 加新 row（red+green）+ `cargo insta review`
- [x] 5.4 觸發 `rust-skills:m15-anti-pattern` + `rust-skills:coding-guidelines` 重構；驗證方式：`cargo build -p speclink_runtime` + `cargo test -p speclink_runtime` + fmt + clippy

## 6. ArchiveOperations runtime ops（crates/runtime）

- [x] 6.1 在 `crates/runtime/tests/archive_ops.rs` 寫測試：(a) `run` happy path 把 ArchiveRequest 轉 ArchiveResult、回 JSON envelope `data` shape 對齊「JSON envelope shape」決策；(b) `--skip-specs` 路徑回 `merged_specs=[]` + `warnings` 含一筆 carrier 帶 `details.capabilities_skipped=[...]` 排序；(c) state guard 反射所有 `ProviderError::ChangeTasksIncomplete` / `StateTransitionInvalid` / `ChangeNotFound` 對應到 RuntimeError；(d) spec merge new capability dir 場景：`.speclink/specs/audit-log/` 不存在 → `merged_specs[0]={lines_added:N, lines_removed:0}`；(e) spec merge existing capability dir 場景：既有 90-line spec.md 被覆蓋 → `merged_specs[0]={lines_added:N, lines_removed:90}`；(f) empty specs 目錄 → `merged_specs=[]` 且 warnings 不含 `archive.specs_skipped`；對應 archive-runner spec requirement「Spec delta merge SHALL atomically overwrite the target capability spec for each capability under the change」「`--skip-specs` SHALL bypass merge while still transitioning state and emit an audit warning」（red）；對應決策「Observable behavior」「JSON envelope shape」
- [x] 6.2 在 `crates/runtime/src/archive_ops.rs` 實作 `ArchiveOperations<G>` runtime entry 含 `run` method；內部呼叫 `ArchiveStore::archive_change` 並把 `ProviderError` 一對一映射 `RuntimeError`；warning carrier 依「Each element of `warnings` array SHALL have shape `{ code, message, details? }`」契約組裝 `details.capabilities_skipped`；對應 archive-runner spec requirement「JSON envelope SHALL conform to the bootstrap / A2 / A3 contract」（green）；驗證方式：6.1 全綠
- [x] 6.3 在 `crates/runtime/src/lib.rs` 加 `pub mod archive_ops; pub use archive_ops::ArchiveOperations;`；驗證方式：`cargo build -p speclink_runtime` 通過
- [x] 6.4 觸發 `rust-skills:m15-anti-pattern` 重構（提取 warning carrier 建構 helper）；驗證方式：fmt + clippy + 6.1 全綠

## 7. CLI surface（crates/cli）

- [x] 7.1 觸發 `rust-skills:domain-cli` 取 clap derive 建議；在 `crates/cli/src/main.rs::Commands` enum 加 `Archive { change_id: String, #[arg(long)] skip_specs: bool, #[arg(long)] yes: bool, #[arg(long)] no_validate: bool, #[arg(long)] json: bool }` variant；`--help` 文字必須對 `--no-validate` 含「reserved for future analyze slice; currently no-op」字句、對 `--yes` 含「accepted for compatibility; archive does not prompt in this slice」字句；對齊「CLI surface」決策 + archive-runner spec requirement「`--no-validate` flag SHALL be accepted by the CLI but SHALL be a no-op in this slice」「`--yes` flag SHALL be accepted by the CLI; archive SHALL never prompt interactively in this slice」（red：clap derive snapshot 測試斷言 help text 含關鍵字）
- [x] 7.2 在 `crates/cli/src/commands/archive.rs` 新檔實作命令：呼叫 runtime `ArchiveOperations::run`；human-mode output 透過既有 `print_human_ok` 委派 `render_human`；`--json` mode 走 `print_json` 出 envelope 對齊 archive-runner spec requirement「JSON envelope SHALL conform to the bootstrap / A2 / A3 contract」（green）；驗證方式：7.1 snapshot 綠
- [x] 7.3 在 `crates/cli/src/commands/mod.rs` pub 新 `archive` module；`crates/cli/src/main.rs` 加 `Commands::Archive { .. } => commands::archive::run(...)` dispatch；驗證方式：`cargo build -p speclink_cli` + 一條 smoke test (`speclink archive --help` exit 0、含「reserved for future analyze slice」)
- [x] 7.4 擴充 `crates/cli/src/main.rs::hint_for` 2 個新 code 對應 archive-runner spec「New error codes SHALL be registered with stable exit codes」table：`change.tasks_incomplete` → `complete all tasks first with \`speclink task done <i> --change <id>\``、`validation.archive_failed` → `run \`speclink validate <id>\` first`；擴充 `crates/cli/src/output.rs::error_code_to_exit` 對應 mapping（`change.tasks_incomplete`→2、`validation.archive_failed`→3、`archive.specs_skipped` 不在此表 — 它是 warning carrier 不走 error path）；驗證方式：`crates/cli/tests/snapshots.rs` 既有 snapshot 加 2 個新 hint row 與 exit code row
- [x] 7.5 觸發 `rust-skills:m15-anti-pattern` + `rust-skills:coding-guidelines` 重構；驗證方式：`cargo build -p speclink_cli` + `cargo test -p speclink_cli` + fmt + clippy

## 8. CLI 整合測試（crates/cli/tests）

- [x] 8.1 [P] `crates/cli/tests/archive_walking_skeleton.rs`：實機跑「Walking-skeleton 端到端」全序列：`init → new change → 寫 proposal/tasks/spec 3 artifact 自動 transition to ready → apply start → task done × N（最後一個自動設 all_tasks_done） → archive`；archive 後 assert（a）`.speclink/changes/<id>/` 不存在；（b）`.speclink/changes/archive/<YYYY-MM-DD>-<id>/` 存在內容 1:1；（c）`.speclink/specs/<capability>/spec.md` 已寫入；（d）state.db `change.state='archived'` 與 `archived_at` 非 NULL；（e）`state_transition` 表多一筆 `reason='archive_run'`；對應 archive-runner spec requirement「`speclink archive` SHALL transition the change from `in_progress` to `archived` when all tasks are done」「JSON envelope SHALL conform to the bootstrap / A2 / A3 contract」+ design「Acceptance criteria」；對應決策「Observable behavior」
- [x] 8.2 [P] `crates/cli/tests/archive_state_guards.rs`：實機跑 6 條 reject 路徑：(a) `proposing` archive → `state.transition_invalid` exit 7；(b) `reviewing` archive → 同上（mock 用 state.db 直接寫 reviewing 注入）；(c) `ready` archive → 同上；(d) `code_reviewing` archive → 同上（mock 注入）；(e) `archived` archive 重複呼叫 → 同上；(f) `in_progress + all_tasks_done=0` archive → `change.tasks_incomplete` exit 2 + hint 含「`speclink task done`」；對應 archive-runner spec requirement「`speclink archive` SHALL transition the change from `in_progress` to `archived` when all tasks are done」（state guard matrix scenario）+ state-machine spec requirement「archive.run from code_reviewing is rejected in this slice」
- [x] 8.3 [P] `crates/cli/tests/archive_skip_specs.rs`：實機跑 `archive --skip-specs --json`：(a) 帶 2 個 capability 的 change → 回應 `data.merged_specs=[]` + `warnings` 唯一一筆 `code=archive.specs_skipped` + `details.capabilities_skipped=["audit-log","user-auth"]`（排序）；(b) 帶 0 capability 的 change → 同上但 `warnings=[]`（沒東西好 skip）；assert `.speclink/specs/` byte-for-byte 不變；對應 archive-runner spec requirement「`--skip-specs` SHALL bypass merge while still transitioning state and emit an audit warning」
- [x] 8.4 [P] `crates/cli/tests/archive_terminal_state.rs`：archive 一個 change 後再對該 change 呼叫 A3 既有 op，斷言 A3 spec 內既有但 A3 期間 unreachable 的 scenario 全部可達且行為對齊：(a) `apply.start` on archived → exit 0 + `data.message='Change is archived.'`；(b) `apply.pause` on archived → exit 7 `state.transition_invalid`；(c) `task.done 1` on archived → exit 7 `state.transition_invalid`；(d) `task.undo 1` on archived → exit 7 `state.transition_invalid`；對應 state-machine spec requirement「`archived` state SHALL be terminal — all subsequent `apply.*` and `task.*` operations SHALL be rejected or returned as hints without mutation」
- [x] 8.5 [P] `crates/cli/tests/json_envelope_v4.rs`：用 `insta::assert_snapshot!` 鎖死 `archive` op 的 success / `--skip-specs` success / 5 個 error path 的 JSON envelope；對應 archive-runner spec requirement「JSON envelope SHALL conform to the bootstrap / A2 / A3 contract」；首次 review 用 `cargo insta review`
- [x] 8.6 [P] `crates/cli/tests/human_output_v4.rs`：用 `insta::assert_snapshot!` 鎖死 `archive` 在 human-mode（無 `--json`）的成功輸出（含 `merged_specs` table 渲染）+ 1 條 error path 的 stderr；斷言 `--json` envelope 與 8.5 snapshot byte-for-byte 相符（cross-check `cli-human-output` capability 既有「renderer SHALL NOT alter `--json` envelope behavior」契約）
- [x] 8.7 觸發 `rust-skills:m15-anti-pattern` 重構共用 fixture（archive_e2e helper、archived_state injection helper）；驗證方式：`cargo test --workspace` + `cargo fmt --check --all` + `cargo clippy --workspace -- -D warnings` 全綠

## 9. 跨平台 readiness

- [x] 9.1 在 `crates/provider-local/tests/archive_store.rs` 加 cross-platform rename test：用 `tempfile` 在同一 mount 內驗證 `fs::rename` 對含子目錄的 change dir 成功；不模擬 cross-device EXDEV（本 slice 不支援、留 doctor slice）；對應 design Risks 表「Cross-device rename」row（mitigation：本 slice 不處理）；驗證方式：CI matrix（Linux/macOS/Windows × stable Rust）皆綠
- [x] 9.2 在 `crates/cli/tests/archive_walking_skeleton.rs` 加 Windows-specific atomic rename regression：archive 時用 Windows shared file 注入測試（讀取者持有 read lock），斷言 `fs::rename` 在 Windows 下處理 sharing violation 的行為（透過 retry 或回明確 error message，非 panic）；驗證方式：Windows CI job 該 test 綠或被 `#[cfg(windows)]` 標記後跳過

## 10. 文件與規格交叉檢核

- [x] 10.1 在 `README.md` 「Walking skeleton 進度」段（若存在則更新、不存在則新增）標 A4=`add-archive` 已落地、列舉 4-state walking-skeleton 主路徑 `proposing → ready → in_progress → archived` 已可端到端跑；驗證方式：grep `A4` 在 README 出現一次
- [x] 10.2 在 `doc/protocol/operations.md` `archive.run` 章節（line 3198）更新 `Errors` 表把本 slice 實作的 2 個 error code 從「reserved」字眼換成「implemented」；保留 `change.code_review_pending` / `validation.archive_failed` 為 reserved 不動；驗證方式：grep `archive.run` 章節含 `implemented` 字串
- [x] 10.3 跑 `cargo test --workspace` + `cargo fmt --check --all` + `cargo clippy --workspace --all-targets -- -D warnings` 全綠當作收尾；驗證方式：3 個 CI step 全綠 + `spectra validate add-archive` 通過
