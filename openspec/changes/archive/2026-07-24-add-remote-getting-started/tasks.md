## 1. Remote 教學契約（Red／Green／Refactor）

- [x] 1.1 RED — 依「Remote Getting Started 提供可重複的完整操作路徑」、「帳號、PAT 與 membership 的操作邊界明確」與「Remote 教學具雙語導流與可重複查核」新增 `scripts/remote-docs.test.mjs`，先斷言 `docs/remote-getting-started.zh-TW.md`／`docs/remote-getting-started.md` 存在且 H2 順序相同、包含 `/account`、POST `/account/tokens`、`/admin/users`、HTTP 405、membership、project-scoped URL、spec-only、checkout、offline、`npm run dev:reset`，並驗證 README／product-status 雙語入口與 `docs/server-deployment.zh-TW.md` 繁中入口；執行 `node --test scripts/remote-docs.test.mjs`，確認因教學與入口尚不存在而 RED。影響：`scripts/remote-docs.test.mjs`；`crates/speclink-core`／`crates/speclink-cli` 無修改。 <!-- speclink-task:tsk_01KY8Z0GXEXDX1VSC6F2YTK2BX -->
- [x] 1.2 GREEN — 新增 `docs/remote-getting-started.zh-TW.md` 與 `docs/remote-getting-started.md`，以相同 H2 順序交付從 `npm run dev:reset`／`npm run dev`、`/setup`、Admin membership、`/account` PAT、Desktop device login／PAT fallback、scope、spec-only／checkout、Remote CLI smoke test、持久化、失聯恢復到重置的完整路徑，並以症狀表說清 405、空 membership、401／重新認證、offline／stale、marker 衝突；執行 `node --test scripts/remote-docs.test.mjs`，確認核心內容測試轉 GREEN。影響：`docs/remote-getting-started.zh-TW.md`、`docs/remote-getting-started.md`；CLI 範例只使用 `crates/speclink-cli` 現有指令，不改輸出、`--json` 或 exit code。 <!-- speclink-task:tsk_01KY8Z0GXE41N0NW8S7ZJVW7BG -->
- [x] 1.3 REFACTOR — 在 `README.md`、`README.en.md`、`docs/product-status.zh-TW.md`、`docs/product-status.md` 與 `docs/server-deployment.zh-TW.md` 加入最短且不重複正文的同語言教學入口，並在 `docs/platform-architecture.zh-TW.md` 把「`/account/tokens` 建立 PAT」收斂為「瀏覽 `/account`，由表單 POST `/account/tokens`」；重跑 `node --test scripts/remote-docs.test.mjs`，確認導流、方法邊界與雙語結構仍 GREEN。影響：上述六份文件；平台架構的唯一目標架構與既有 Phase 順序不變。 <!-- speclink-task:tsk_01KY8Z0GXE2BP70HN7YCT7J459 -->

## 2. 文件驗證與範圍收束

- [x] 2.1 驗證操作教學只引用現行入口：由 `crates/speclink-server/src/app.rs` 比對 GET `/account`、POST `/account/tokens`、GET `/admin/users`，由 `speclink --help`、`speclink link --help`、`speclink auth login --help` 比對 CLI surface；`scripts/remote-docs.test.mjs` 必須同時檢查相對 Markdown 連結存在與禁止把 `/account/tokens` 描述成 GET 頁面，執行該測試及 `node --test "scripts/**/*.test.mjs"` 皆 exit 0。影響：`scripts/remote-docs.test.mjs`；不修改 Server／CLI runtime。 <!-- speclink-task:tsk_01KY8Z0GXE1R9JGWYPJHAFHE4G -->
- [x] 2.2 完成跨 artifact 與規格查核：執行 `speclink analyze add-remote-getting-started --json` 確認 Critical／Warning 為零、`speclink validate add-remote-getting-started` 通過，並人工核對 proposal 的 Non-Goals——不修改 setup 自動 membership、Server、Desktop、CLI、Protocol、認證、角色、Store 或 Phase 4 能力；確認 `git diff --check` 通過且既有 `remote-board-order` 未提交檔案未被本 change 改動。 <!-- speclink-task:tsk_01KY8Z0GXE9H7680X1GCAFD4VG -->
