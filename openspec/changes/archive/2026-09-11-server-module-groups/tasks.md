## 1. 守門先行（RED）

- [x] 1.1 （design D6 守門延伸；spec delivery-baseline Requirement「倉庫路徑守門」Scenario「殘留舊形伺服器模組或測試檔路徑被抓出」）在 scripts/release/delivery-gate.test.mjs 的路徑守門增 (d) 條：13 個將搬入 identity／admin／api／web 的 src 模組名（identity_sqlite、auth、device、setup、audit、backup、routes、read_api、verb、events、context、error、assets）與 44 個將搬入子目錄的測試檔舊名（含 admin_*、web_* 去前綴前的名字）視為舊形；根層 src 五檔＋四個主模組檔、tests/it 根層四檔＋common 不在清單；排除、@trace 跳過與 `path-guard:allow` 豁免規則沿用。驗證：`node --test scripts/release/delivery-gate.test.mjs` 在搬移前該條 RED 且輸出至少列出 apps/server-web/src/pages/admin/AuditPage.tsx 與 docs/product-status.md 的命中行；引用 src/app.rs 或 tests/it/e2e_cli.rs 的行不在命中清單。 <!-- speclink-task:tsk_01M260B9AQ5A87B410PVMP109V -->

## 2. src 四組

- [x] 2.1 （design D1 四組成員與根、D2 主模組檔＋同名資料夾、D3 identity_sqlite 收進 identity::sqlite、D5 相對路徑）以 git mv 把 identity_sqlite.rs 搬為 src/identity/sqlite.rs，auth／device／setup 搬入 src/identity/，audit／backup 搬入 src/admin/，routes／read_api／verb／events／context／error 搬入 src/api/，assets 搬入 src/web/；identity.rs 尾端宣告四個子模組並把 `pub use crate::identity_sqlite::IdentitySqlite` 改為 `pub use sqlite::IdentitySqlite`；admin.rs 宣告 audit／backup；web.rs 宣告 assets；新增 src/api.rs 只含六行 `pub mod`；lib.rs 改為五個根層 pub mod（admin、app、config、identity、state、web）＋私有 `mod api`＋四組 `pub use` 掛回 13 個舊名；admin/backup.rs 的 import 改 `crate::identity::IdentitySqlite`；web/assets.rs 的 include_bytes! 多一層 `../` 並更新同檔說明。驗證：`cargo build -p speclink-server` 綠；`cargo build --workspace --locked` 綠（desktop-tauri 與 remote 零改動）；`grep -rn 'identity_sqlite' crates apps` 只剩 config.rs 的測試函式名；`git status` 顯示 13 個模組為 rename；`npm run build -w apps/server-web` 後 `cargo build --release -p speclink-server` 綠（驗 include_bytes! 新路徑）。 <!-- speclink-task:tsk_01M260B9AQ69X7VSTXYK5V9BS7 -->
- [x] 2.2 （design D7 驗證）src 搬移後的行為回歸：`cargo test -p speclink-server` 全綠且 `--test it` 的測試數量與改動前相等（搬移前先記錄 `cargo test -p speclink-server --test it 2>&1 | grep 'test result'`）；`cargo test -p speclink-remote --test it` 綠（remote 測試以 speclink_server 路徑起 server）；`cargo test -p speclink-desktop-core` 綠。驗證：三個指令 exit code 0，測試數相等。 <!-- speclink-task:tsk_01M260B9AQ50RFSBCR8EYC8RKV -->

## 3. tests/it 鏡射四組

- [x] 3.1 （design D4 tests/it 鏡射四組）以 git mv 把 44 個測試檔搬入 tests/it/identity/（9 檔，檔名不變）、tests/it/admin/（14 檔，admin_* 去前綴）、tests/it/api/（15 檔）、tests/it/web/（6 檔，web_* 去前綴）；各目錄新增 mod.rs 逐行 `mod <檔>;`；main.rs 改為 `mod common; mod identity; mod admin; mod api; mod web; mod e2e_cli; mod phase2_chain; mod startup; mod serverfs_store;`；測試內 `use crate::common` 不動。驗證：`cargo test -p speclink-server --test it` 全綠且測試數量與 2.2 記錄相等；`cargo test -p speclink-server --test it admin` 與 `--test it web::setup` 過濾各命中該組原本的測試；`git status` 顯示 44 個測試檔為 rename；tests/pg 零改動。 <!-- speclink-task:tsk_01M260B9AQ2XTX15YTMVK00RDW -->

## 4. crate 外引用與收尾（GREEN）

- [x] 4.1 （design D5 相對路徑與 crate 外引用、D6、D7；spec delivery-baseline Requirement「倉庫路徑守門」）更新三處 crate 外引用：apps/server-web/src/pages/admin/AuditPage.tsx 的註解改 `crates/host/speclink-server/src/admin/audit.rs`；docs/product-status.md 與 docs/product-status.zh-TW.md 的測試檔連結改 admin/e2e.rs、admin/backup_e2e.rs、identity/device_e2e.rs（e2e_cli 與 phase2_chain 留根不變）。然後跑完整回歸：`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 全綠（1.1 的守門轉綠、docs-links 綠、remote-docs.test 讀 app.rs／web.rs 綠）；`npm test -w apps/server-web` 綠；`speclink validate server-module-groups` 綠；`git diff --stat` 不含 Cargo.lock、tests/pg、Dockerfile。 <!-- speclink-task:tsk_01M260B9AQVZTNAEKY6T4P8E72 -->
