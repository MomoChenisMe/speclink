## 1. 桌面讀取端：sources 錨定與 Requirement 段落級判定

- [x] 1.1 實作設計「錨定語法為 capability 名加井號加 Requirement 名，寫在既有 sources 欄」與 manual-pages 規格「frontmatter 六欄」的讀取面：`apps/desktop/core/src/manual.rs` 解析 `sources` 項時以第一個 `#` 切成 capability 與錨定，只對 capability 部分跑既有路徑守門（單一路徑段、無 `..`、無 `:`）；索引回傳的 `sources` 仍為 frontmatter 原字串陣列（含錨定原樣）；不合守門的項整項略去。驗證：`cargo test -p speclink-desktop-core manual` 新增測試斷言 `["desktop-app#看板與任務", "../evil#段", "a/b#段"]` 只保留第一項且原字串回傳，`sources` 欄位為字串陣列、`stale` 為布林（camelCase 不變）。 <!-- speclink-task:tsk_01M2764FDA8Q7B2P1R7KTRQ9B3 -->
- [x] 1.2 實作設計「過期判定改為逐項判定，錨定項只取該 Requirement 段落內的時戳」，落實 manual-pages 規格「過期判定基準」與 desktop-manual-page 規格「可能過期與未入冊的標示」的段落級判定：`apps/desktop/core/src/manual.rs` 新增依行首 `### Requirement:` 切段的函式（段自標題起至下一標題或檔尾；逐行去 `\r`），錨定項只取標題去頭尾空白後精確相等那段內的 `@trace updated`；無錨定項維持整份規格。驗證：`cargo test -p speclink-desktop-core manual` 測試按 manual-pages 契約 Example 判定表：頁 `generated: 2026-09-05`、`sources: ["desktop-app#桌面上的品質關卡"]`，該段 2026-08-14、他段 2026-09-10T17:15:24+08:00 → `stale` false；同頁 `sources: [desktop-app]` → true；`\r\n` 版本的規格文本結果相同。 <!-- speclink-task:tsk_01M2764FJXXPXAGXCNJPD73YP2 -->
- [x] 1.3 落實「過期判定基準」與「可能過期與未入冊的標示」的失效路徑：`apps/desktop/core/src/manual.rs` 錨定找不到相符標題、或錨定項的規格不存在時該項視為過期；無錨定項的規格不存在時維持不標記；頁內任一項過期即頁過期。驗證：`cargo test -p speclink-desktop-core manual` 測試 `["desktop-app#不存在的段"]` → `stale` true、`["not-exist#段"]` → true、`[not-exist]` → false、`["desktop-app#桌面上的品質關卡", x]` 且 x 整份晚於頁 → true。 <!-- speclink-task:tsk_01M2764FMKXT933H8TTZZXACQC -->
- [x] 1.4 實作設計「未入冊、出處行、來源消失都只看井號前的 capability 名」在讀取端的那一半：`apps/desktop/core/src/manual.rs` 的未入冊判定改以井號前的 capability 名比對（「可能過期與未入冊的標示」的未入冊句）。驗證：`cargo test -p speclink-desktop-core manual` 測試 capability `x` 只出現在某頁 `"x#某段"` 項時，`uncoveredNew` 不含 `x`；既有測試 `uncovered_new_requires_every_stamp_after_every_page_at_second_precision` 仍綠。 <!-- speclink-task:tsk_01M2764FP8WJPJEJHP1GTDTKVZ -->

## 2. 生成端：manual 技能 asset 改規則並進版

- [x] 2.1 把「過期判定基準」與「frontmatter 六欄」的錨定語意寫進技能，並落實設計「未入冊、出處行、來源消失都只看井號前的 capability 名」在生成端的那一半：`crates/engine/speclink-core/assets/skills/manual.md` Step 3 的 **stale page** 定義改為逐項判定（無錨定看整份規格；`capability#Requirement 名` 只看該 Requirement 段落的 `@trace updated`；錨定找不到即過期），**unlisted capability** 與 **orphan page** 註明只看井號前名稱；frontmatter 表的 `sources` 欄說明改為「`<capability>` 或 `"<capability>#<Requirement 名>"`（帶錨定一律雙引號）」並加一則帶錨定的範例；出處行規則註明只列 capability 名、去重。驗證：內容審閱——`speclink update` 後 `.claude/skills/speclink-manual/SKILL.md` 含上述三處新文字，且與 manual-pages delta 的 Example 判定表一致。 <!-- speclink-task:tsk_01M276HGX546B4JN7MVK2YT15G -->
- [x] 2.2 實作設計「內文逐位元相同的過期頁只推進 generated」與「生成端寫錨定的規則」，落實 manual-skill 規格「生成模式的輸出與報告」與 manual-pages 規格「重生時保留既有順序」：`crates/engine/speclink-core/assets/skills/manual.md` Step 4「Manual exists」的 byte-identical 規則改為「重生後除 `generated` 行外與磁碟相同 → 只把 `generated` 換成本次時戳、其餘位元不動（`sources` 保留原寫法）」，內文不同才整頁重寫；Step 5 寫頁規則加「新頁與整頁重寫的頁：只取材該 capability 部分 Requirement 時每條寫一項帶錨定、取材全部時寫裸名稱」；Step 6 摘要加「只換時戳：N 頁（<filenames>）」一列並把「未動」定義為未被判過期的頁。驗證：內容審閱——再生後的 `.claude/skills/speclink-manual/SKILL.md` 摘要範本含「只換時戳」列，且 Step 4 不再出現「do not rewrite」的逐位元不寫規則。 <!-- speclink-task:tsk_01M276HGZCHA62JGJ4RZGMHW88 -->
- [x] 2.3 實作設計「ASSET_VERSION 進版與衍生物同批再生」的第一半：`crates/engine/speclink-core/src/workspace/init.rs` 的 ASSET_VERSION 由 v1.33.0 進為 v1.34.0，並以 `UPDATE_GOLDEN=1 UPDATE_ASSETS_LOCK=1 cargo test -p speclink-core --test it render_golden::` 再生 `crates/engine/speclink-core/tests/golden/` 下五份 snapshot 與 assets.lock。驗證：`cargo test -p speclink-core --test it render_golden::` 綠；`git diff --stat crates/engine/speclink-core/tests/golden/` 只含版號行與 manual 技能內文的差異，其他技能內文無變動。 <!-- speclink-task:tsk_01M276HH1BAD68GSJGHC1WCA42 -->
- [x] 2.4 實作設計「ASSET_VERSION 進版與衍生物同批再生」的第二半：先 `cargo build -p speclink-cli` 再以新 binary 跑 `speclink update`，讓 `.claude/skills/speclink-manual/SKILL.md` 與 `.agents/` 下對應技能檔帶 v1.34.0 與新內文；再生出的其他技能檔只有版號行變動。驗證：`speclink --version` 顯示 v1.34.0；`git status --short` 盤點再生檔案並全部納入收尾 commit；`grep -c '只換時戳' .claude/skills/speclink-manual/SKILL.md` 至少 1。 <!-- speclink-task:tsk_01M276HH39ZJBMEEH5K0D8NAX2 -->

## 3. 品質站第 1 輪修正

- [x] 3.1 落實 desktop-manual-page 規格「內頁渲染與出處跳規格」的錨定出處與設計「未入冊、出處行、來源消失都只看井號前的 capability 名」的前端那一半：`packages/ui/src/components/ManualPage.tsx` 出處列取每項第一個 `#` 前的名稱後去重再比對正典。驗證：`npm test -w packages/ui -- manualPage` 新增測試「帶 Requirement 錨定的 sources」通過（恰一顆可點 `desktop-app`、文字無 `#`）。 <!-- speclink-task:tsk_01M278DJSMBEZP94TB50F8B80W -->
- [x] 3.2 實作設計「切段沿用引擎的 parse_canonical」與 `Source::parse` 邊界（空錨定視同裸名、兩半去空白），並把單項判定收進 `Source::is_stale`、快取改借用不 clone：`crates/engine/speclink-core/src/lifecycle/archive.rs` 的 `parse_canonical` 改 pub，`apps/desktop/core/src/manual.rs` 改呼叫它。驗證：`cargo test -p speclink-desktop-core manual` 新增測試 `empty_anchor_means_bare_capability_and_both_halves_are_trimmed` 通過且既有 24 個測試仍綠；`cargo test -p speclink-core --test it render_golden::` 綠。 <!-- speclink-task:tsk_01M278DJVJR1E5RZY3CS7GJG8Z -->
- [x] 3.3 落實 manual-skill 規格「生成模式的輸出與報告」與 manual-pages 規格「重生時保留既有順序」的補充：`crates/engine/speclink-core/assets/skills/manual.md` 把錨定語意集中為「Source anchors」一段並讓六處引用它、stamp-only 明寫「錨定失效者整頁重寫」、index／about 觸發加上 stamp-only；`openspec/LANGUAGE.md`「可能過期」詞條補錨定範圍。驗證：再生後 `.claude/skills/speclink-manual/SKILL.md` 含「Source anchors」且「before `#`」只出現在該段；golden 與 assets.lock 已同批再生；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 綠（詞彙守門）。 <!-- speclink-task:tsk_01M278DJXHV2CNF9KD6HBTFYEW -->

## 4. 收尾

- [x] 4.1 `openspec/changes/manual-stale-requirement-anchor/`：跑 `speclink validate manual-stale-requirement-anchor` 與 `speclink analyze manual-stale-requirement-anchor` 確認三份 delta 規格（「frontmatter 六欄」「過期判定基準」「重生時保留既有順序」「生成模式的輸出與報告」「可能過期與未入冊的標示」）與 tasks 無 Critical／Warning；跑 `cargo test -p speclink-desktop-core` 全套與 `node --test scripts/*.test.mjs scripts/*/*.test.mjs` 確認詞彙守門與連結檢查綠。驗證：三個指令皆 exit code 0。 <!-- speclink-task:tsk_01M276PKXYY2DEHYM146Q9R2TR -->
