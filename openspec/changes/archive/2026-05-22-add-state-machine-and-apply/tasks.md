## 0. Spec ↔ task traceability checklist

- [x] 0.1 確認所有 spec requirement 名稱（state-machine 9 條、apply-task-ops 8 條、change-store MODIFIED 2 條）皆以子字串形式出現在後續至少一條 task 描述內；analyzer 跑 `spectra analyze add-state-machine-and-apply` 無 Critical/Warning 視為達成
- [x] 0.2 確認 design.md 所有 `###` 決策標題（v3 migration、auto-transition trigger、walking-skeleton review-flag 預設值、task id 策略、StateMachineStore 拆 trait、並發控制 CAS、Observable behavior、CLI surface、Provider trait interface、auto-transition contract、`task.done` auto-trigger contract、walking-skeleton 端到端 acceptance、scope boundaries）皆在後續至少一條 task 描述內出現

## 1. Pre-apply 前置檢查與 slice 命名固化

- [x] 1.1 Apply skill 啟動時 verify `openspec/specs/change-store/spec.md` 已存在（A2 已 archive）；缺失即 abort 並提示 user 跑 `/spectra-archive add-change-and-artifact-io`；驗證方式：runtime helper `precheck_a2_archived()` 的 unit test 在 fixture 用 with/without spec dir 兩種情境
- [x] 1.2 在 `doc/speclink-design.md` §1（定位）之後新增「Walking skeleton slice naming」一節，凍住 A1=`add-project-bootstrap` / A2=`add-change-and-artifact-io` / A3=`add-state-machine-and-apply` 對應表；驗證方式：grep 該標題在 design.md 出現一次、各 slice id 字串出現於表內

## 2. Provider trait 與共用型別擴充（crates/provider）

- [x] 2.1 [P] 觸發 `rust-skills:m05-type-driven` 取 idiomatic type 建議；為 `ChangeState`（6 variant proposing/reviewing/ready/in_progress/code_reviewing/archived）、`StateTransitionReason`（apply_start / apply_pause / task_done_auto / task_undo_revert / artifact_dag_complete / review_approved_artifact / review_rejected_code / archive_run）、`Actor { agent_host, os_user, host_id }`、`TransitionRequest`、`ChangeStateView` 寫 serde 雙向 roundtrip 與 enum 列舉值斷言測試在 `crates/provider/src/types.rs::tests`（red）；驗證方式：`cargo test -p speclink_provider --lib types::tests` 跑紅
- [x] 2.2 [P] 在 `crates/provider/src/types.rs` 實作上述型別並讓 2.1 測試轉綠；ChangeState 必須涵蓋「six legal states」並支援 `FromStr` / `Display` 對齊 `state.invalid_value` 錯誤路徑（green）；驗證方式：2.1 測試全綠 + `cargo build -p speclink_provider`
- [x] 2.3 [P] 觸發 `rust-skills:m13-domain-error`；於 `crates/provider/src/error.rs` 既有 `ProviderError` enum 新增 5 個 variant（`StateInvalidValue` / `StateTransitionInvalid { from, to }` / `StateVersionConflict { current_version }` / `StateDbSchemaInvalid { found, supported }` / `ChangeDagIncomplete { missing }`），並在 `codes` module 新增 `pub const STATE_INVALID_VALUE` / `STATE_TRANSITION_INVALID` / `STATE_VERSION_CONFLICT` / `STATE_DB_SCHEMA_INVALID` / `CHANGE_DAG_INCOMPLETE` 5 個 dot-separated 錯誤碼常量，對齊 design.md 「新 error codes SHALL be registered with stable exit codes」契約；驗證方式：`crates/provider/tests/error_codes.rs` 斷言 5 個常量字串與 ProviderError → code 對應正確
- [x] 2.4 在 `crates/provider/src/lib.rs` 宣告 `StateMachineStore` trait（決策「`StateMachineStore` 拆成獨立 trait（不併入 `ChangeStore`）」），介面 method 為 `get_change_state` / `transition_state` / `set_actor` / `set_all_tasks_done`，所有寫入 method 帶 `expected_version` 參數對應 `change.version` 作為 the compare-and-swap token；驗證方式：撰寫只測「trait 簽章存在 + ChangeStore 不暴露 state setter」compile-only 測試於 `crates/provider/tests/trait_surface.rs`，跑 `cargo test -p speclink_provider --test trait_surface`
- [x] 2.5 觸發 `rust-skills:m15-anti-pattern` + `rust-skills:coding-guidelines` 對 2.1–2.4 重構（abstract const naming、enum 排序、doc comment 繁中）；驗證方式：`cargo fmt --check` + `cargo clippy -p speclink_provider -- -D warnings` 全綠

## 3. State.db v3 migration（crates/provider-local）

- [x] 3.1 寫 v3 migration integration test 在 `crates/provider-local/tests/migration_v3.rs`，使用 `tempfile` 建 empty db → apply v1 → v2 → v3 後斷言 `change` 表多出 `actor_json TEXT NULL` 與 `all_tasks_done INTEGER NOT NULL DEFAULT 0`、新表 `state_transition` 含全部欄位（`transition_id` PK / `change_id` FK / `from_state` / `to_state` / `actor_json` / `transitioned_at` / `reason`）與 `idx_state_transition_change_time(change_id, transitioned_at DESC)` index；同檔再加 idempotency 測（重跑 migration 不報錯）與 partial-rollback 測（注入失敗後 retry 結果一致）；對應 spec requirement「State.db schema MUST be upgraded to version 3 with `actor_json` column, `all_tasks_done` column, and `state_transition` table」（red）；**暫停等 user 確認 migration SQL 與向下相容策略**
- [x] 3.2 在 `crates/provider-local/src/state_db.rs` 的 `MIGRATIONS` 陣列追加 v3 entry，包含 ALTER TABLE + CREATE TABLE + CREATE INDEX，並插入 `_migrations` 對齊「State.db v3 migration：alter `change` 表 + 新增 `state_transition` 表」決策（green）；驗證方式：3.1 全綠
- [x] 3.3 觸發 `rust-skills:m06-error-handling`；在 `crates/provider-local/src/state_db.rs` 新增 helper method `update_change_state_cas(change_id, expected_version, new_state, audit_row)` 在單一 rusqlite tx 內完成 change row update + state_transition insert，CAS 失敗回 `ProviderError::StateVersionConflict`；另加 `set_actor` / `set_all_tasks_done` 同樣 CAS-based helper，對齊「並發控制：`change.version` CAS + 單 SQLite tx，不接 file lock」決策；驗證方式：unit test 覆蓋成功 commit、CAS 失敗、audit insert 失敗 rollback 三條路徑（red+green）
- [x] 3.4 將 `crates/provider-local/src/store.rs::LocalProjectStore::open_state_db()` 內既有 `db.migrate(2)` 升為 `db.migrate(3)`；新增「v2 binary refuses v3 db」regression test：用 v3 schema seed 後限制 `MIGRATIONS.len() == 2` 開檔須拋 `StateDbSchemaInvalid { found: 3, supported: 2 }`；對應 spec 內「v2 binary refuses to open a v3 database」scenario；驗證方式：`cargo test -p speclink_provider_local --test migration_v3`
- [x] 3.5 觸發 `rust-skills:m15-anti-pattern` 重構 3.1–3.4；驗證方式：`cargo build -p speclink_provider_local` + `cargo test -p speclink_provider_local` + `cargo fmt --check` + `cargo clippy -p speclink_provider_local -- -D warnings` 全綠

## 4. LocalStateMachineStore 實作（crates/provider-local）

- [x] 4.1 在 `crates/provider-local/tests/state_machine_store.rs` 寫測試覆蓋 atomic single-tx（state update + audit insert 同時 commit）、CAS mismatch 回 `state.version_conflict`、audit insert 失敗 rollback state；對應 spec requirement「State mutation SHALL be atomic with audit insert via a single SQLite transaction」與「`change.version` SHALL serve as the compare-and-swap token for all state-machine mutations」（red）
- [x] 4.2 在 `crates/provider-local/src/state_machine_store.rs` 實作 `LocalStateMachineStore` 滿足 `StateMachineStore` trait；所有 method 透過 3.3 helper 走單 SQLite tx；對齊 design.md 「Provider trait interface」決策（green）；驗證方式：4.1 全綠
- [x] 4.3 修改 `crates/provider-local/src/change_store.rs::LocalChangeStore::create` 內既有硬編字串 `"proposing"` 改為使用 `ChangeState::Proposing.to_string()`，把 baseline「Change state in slice A SHALL be the literal `proposing`」requirement 升級為「Change state SHALL be one of the six lifecycle values defined by the state-machine capability」（A3 lifecycle 演進）；同時對 baseline「Change row Etag (the `version` column) SHALL start at 1 on creation」requirement 補上 A3 演進：保留 `version=1` 初值、新增 `Change row \`version\` column SHALL serve as the compare-and-swap token for all state-machine mutations` 契約；新增 unit test 斷言（a）建立後 `change.version` 等於 1；（b）任何後續 state-machine mutation（透過 4.2 `LocalStateMachineStore`）正確自增 `version` 1；（c）`ChangeStore` trait 表面無 `version` setter 暴露給呼叫端；驗證方式：A2 既有 `change_crud` 測試保持綠（regression: 行為等價）、新測 cas matrix 綠
- [x] 4.4 在 `crates/provider-local/src/lib.rs` 加 `pub use state_machine_store::LocalStateMachineStore;` 暴露給上層 runtime；驗證方式：`cargo build -p speclink_provider_local` + `cargo test -p speclink_provider_local` + `cargo fmt --check` + `cargo clippy -p speclink_provider_local -- -D warnings`

## 5. Runtime state_machine 模組（crates/runtime）

- [x] 5.1 [P] 在 `crates/runtime/tests/state_machine.rs` 寫 transition-table 矩陣測試（合法/非法兩種）涵蓋所有 6 個 source state × 9 個目標 transition 共 30+ 配對；另寫 `ReviewPolicy::walking_skeleton()` 行為測（proposing 完成 DAG → ready、in_progress all-done → all_tasks_done flag 而非 code_reviewing），對應 spec requirement「State machine SHALL enforce the legal transition table」「Change lifecycle SHALL define exactly six legal states」「Walking-skeleton mode SHALL hard-code both review flags to `false`」與決策「Walking-skeleton review-flag 預設值：硬編 `false`，不讀 config」（red）
- [x] 5.2 [P] 在 `crates/runtime/src/state_machine.rs` 實作模組：硬編 transition table（含 reason code 對應）、`ReviewPolicy::walking_skeleton()` 建構子、Actor 推導 helper（讀 `SPECLINK_AGENT_HOST` env / `whoami` crate / hostname，fallback 字串 `cli` / `unknown` / `unknown`），對應 spec requirement「Actor SHALL be resolved by fallback chain when `--actor` flag is omitted」（green）；驗證方式：5.1 全綠
- [x] 5.3 [P] 在 `crates/runtime/src/error.rs::RuntimeError` 加 5 個新 variant 映射 §2.3 的 5 個 ProviderError；擴充 `code()` / `exit_code()` match arm（`state.invalid_value`→1、`state.transition_invalid`→7、`state.version_conflict`→7、`state.db.schema_invalid`→1、`change.dag_incomplete`→2），對應 spec requirement「New error codes SHALL be registered with stable exit codes」；驗證方式：`crates/runtime/tests/error_mapping.rs` snapshot 測試（red+green）+ `cargo insta review`
- [x] 5.4 觸發 `rust-skills:m15-anti-pattern` + `rust-skills:coding-guidelines` 重構 5.1–5.3；驗證方式：`cargo build -p speclink_runtime` + `cargo test -p speclink_runtime` + `cargo fmt --check` + `cargo clippy -p speclink_runtime -- -D warnings`

## 6. ApplyOperations runtime ops（crates/runtime）

- [x] 6.1 在 `crates/runtime/tests/apply_ops.rs` 寫 5×2 矩陣測試：`apply.start` 對 5 個 source state 行為（proposing→error、reviewing→error、ready→transition+assign actor、in_progress→no-op+reassign、code_reviewing→hint message、archived→hint message）+ `apply.pause` 對 5 個 source state 行為（proposing→error、reviewing→error、ready→no-op、in_progress→transition+clear actor、code_reviewing→error、archived→error），對應 spec requirement「`speclink apply start` SHALL implement the ensure-actor semantics defined by design.md §6.2」與「`speclink apply pause` SHALL implement symmetric idempotency against `apply.start`」（red）
- [x] 6.2 在 `crates/runtime/src/apply_ops.rs` 實作 `ApplyOperations<G>` runtime entry 含 `start` / `pause`；start 內呼叫 5.2 actor 推導 helper 與 4.2 `StateMachineStore`；對齊 operations.md `apply.start` Provider method `update_change_state` 契約（green）；驗證方式：6.1 全綠
- [x] 6.3 觸發 `rust-skills:m15-anti-pattern` 重構；驗證方式：`cargo test -p speclink_runtime --test apply_ops` + fmt + clippy

## 7. TaskOperations runtime ops（crates/runtime）

- [x] 7.1 在 `crates/runtime/tests/task_ops.rs` 寫測試：tasks.md checkbox parser regex 矩陣（正例：頂層 / 縮排 / 多空白；負例：大寫 `[X]`、非 list 行、bullet 不是 checkbox）、1-based index 邊界（0、1、N、N+1）、`task.done` idempotency（已 done 再呼叫 no-op）、`task.done` 完成最後一個 task 在 walking-skeleton 下設 `all_tasks_done=1`+state 不變、`task.undo` 從 `code_reviewing` 先 transition `in_progress` 再 unmark；對應 spec requirement「`speclink task list` SHALL enumerate checkbox lines from tasks.md by 1-based index」「`speclink task done` SHALL mark exactly one checkbox by 1-based index and auto-trigger when all tasks complete」「`speclink task undo` SHALL unmark exactly one checkbox by 1-based index and revert auto-trigger when needed」「The transition `code_reviewing → in_progress` triggered by `task.undo` SHALL precede the unmark」與決策「Task id 策略：1-based 行內順序 index，不引入 marker」與「`task.done` auto-trigger contract」（red）
- [x] 7.2 在 `crates/runtime/src/task_ops.rs` 實作 `TaskOperations<G>` runtime entry 含 `list` / `done` / `undo` + tasks.md parser；done/undo 寫檔用 A2 既有 tempfile-then-rename atomic sequence；`task.undo` 在 `code_reviewing` state 下與 `all_tasks_done` clear、tasks.md 寫回必須同 SQLite tx commit（green）；驗證方式：7.1 全綠
- [x] 7.3 觸發 `rust-skills:m15-anti-pattern` 重構 parser（regex 一次編譯、line iterator）；驗證方式：`cargo test -p speclink_runtime --test task_ops` + fmt + clippy

## 8. ArtifactOperations 內嵌 auto-transition hook（crates/runtime）

- [x] 8.1 在 `crates/runtime/tests/artifact_ops.rs`（A2 既有檔擴充）新增 hook 行為測：(a) DAG incomplete 時 hook no-op、state 不變、warnings 無 `state_transitioned`；(b) DAG complete + state == `proposing` 時 hook transition `proposing → ready`、warnings 含 `state_transitioned`；(c) DAG complete 但 state ∈ {ready, in_progress, code_reviewing, archived} 時 hook no-op；對應 spec requirement「Forward state transitions from `proposing` SHALL be triggered automatically by the `artifact.write` DAG evaluator」與決策「Auto-transition trigger：`artifact.write` hook 而非顯式 `change.advance` CLI」「Auto-transition contract（`artifact.write` hook）」（red）
- [x] 8.2 在 `crates/runtime/src/artifact_ops.rs::ArtifactOperations::write` 既有「atomic rename 成功之後 / 回 OK 之前」處插入 DAG evaluator：讀 change current state → 若非 proposing 跳過 → 否則檢查 `proposal.md` + `tasks.md` + `specs/*/spec.md` ≥ 1 是否齊全 → 齊全則呼叫 `StateMachineStore::transition_state` 走 walking-skeleton path 推到 `ready`、warnings 追加 `state_transitioned` warning（green）；驗證方式：8.1 全綠 + A2 既有 artifact_io / etag_concurrency 測試保持綠
- [x] 8.3 觸發 `rust-skills:m15-anti-pattern` 重構 evaluator 函式（提取為 helper 避免 write 路徑變肥）；驗證方式：`cargo test -p speclink_runtime` + fmt + clippy

## 9. CLI surface（crates/cli）

- [x] 9.1 觸發 `rust-skills:domain-cli` 取 clap derive 建議；在 `crates/cli/src/main.rs` 加 `Apply` 與 `Task` 兩個 parent subcommand variant + child variant；在 `crates/cli/src/commands/mod.rs` pub 5 個新 module；`task done` 與 `task undo` 的 `--help` 文字必須包含「task indices are derived from current document order」字句對應 spec requirement「Task index stability contract SHALL be explicit in the spec and warned to users」；對齊 design 決策「CLI surface」（red：clap derive snapshot 測試斷言 help text 含關鍵字）
- [x] 9.2 在 `crates/cli/src/commands/` 新增 5 個檔（`apply_start.rs` / `apply_pause.rs` / `task_list.rs` / `task_done.rs` / `task_undo.rs`），分別呼叫 runtime `ApplyOperations` 或 `TaskOperations` 對應 method；human-mode output 一律呼叫 `crates/cli/src/main.rs::print_human_ok` thin wrapper（內部委派 `speclink_cli::human::render_human`），不直接 println JSON 字串；`--json` mode 呼叫既有 `print_json` 出 envelope，遵守 `cli-human-output` capability 的「renderer SHALL NOT alter `--json` envelope behavior」契約（green）；驗證方式：9.1 snapshot 綠
- [x] 9.3 擴充 `crates/cli/src/main.rs::hint_for` 5 個新 code 對應 spec「Error code mapping is exhaustive」table；擴充 `crates/cli/src/output.rs::error_code_to_exit` 5 個新 mapping（state.invalid_value→1、state.transition_invalid→7、state.version_conflict→7、state.db.schema_invalid→1、state.dag_incomplete→2）；驗證方式：`crates/cli/tests/snapshots.rs` 擴充 `insta::assert_snapshot!` 覆蓋新 hint + exit code 對照表（red+green）
- [x] 9.4 觸發 `rust-skills:m15-anti-pattern` + `rust-skills:coding-guidelines` 重構；驗證方式：`cargo build -p speclink_cli` + `cargo test -p speclink_cli` + fmt + clippy

## 10. CLI 整合測試（crates/cli/tests）

- [x] 10.1 [P] `crates/cli/tests/apply_lifecycle.rs`：實機跑 `init → new change → 寫 3 artifact 自動 transition → apply start → apply pause → apply start (idempotent reassign actor)` 序列，assert state 與 actor 變化對齊 spec apply scenarios；JSON envelope shape 與 operations.md `apply.start` example 對齊
- [x] 10.2 [P] `crates/cli/tests/task_workflow.rs`：實機跑 task list → task done × N（含 idempotent 二次 done） → task undo（含 idempotent 二次 undo + revert from code_reviewing 路徑透過 mock state 直接寫 `code_reviewing` 注入測試）→ all_tasks_done flag transitions；對應 spec apply-task-ops 所有 task.* requirement
- [x] 10.3 `crates/cli/tests/state_machine_e2e.rs`：實機跑 design.md 「Walking-skeleton 端到端 acceptance」全 11 步序列；assert 每步 state、actor、warning、`all_tasks_done` 對齊 design contract 「Observable behavior」；對應 spec requirement「Forward state transitions from `proposing` SHALL be triggered automatically by the `artifact.write` DAG evaluator」端到端驗證
- [x] 10.4 [P] `crates/cli/tests/json_envelope_v3.rs`：用 `insta::assert_snapshot!` 鎖死 5 個新 op 的 success / error JSON envelope；對應 spec requirement「All five CLI commands SHALL emit JSON envelopes compatible with the bootstrap and A2 contract」；首次 review 用 `cargo insta review`
- [x] 10.4a [P] `crates/cli/tests/human_output_v3.rs`：用 `insta::assert_snapshot!` 鎖死 5 個新 op 的 human-mode（無 `--json`）輸出 + 各自 1 條典型 error path stderr；斷言 (a) 通過 `render_human` pipeline（無生 JSON-stringified 字串）、(b) `--json` 一旦帶上 envelope 不變（與 10.4 snapshot byte-for-byte 相符）；對應 `cli-human-output` capability 三條 requirement「Human-mode CLI output SHALL recursively pretty-print the `data` payload」「The renderer SHALL NOT alter `--json` envelope behavior」「The renderer SHALL leave stderr error and hint output untouched」cross-check
- [x] 10.5 觸發 `rust-skills:m15-anti-pattern` 重構共用 helper（fixtures、actor stub）；驗證方式：`cargo test --workspace` + `cargo fmt --check --all` + `cargo clippy --workspace -- -D warnings` 全綠

## 11. 跨平台 readiness

- [x] 11.1 在 `crates/runtime/tests/actor_resolution.rs` 寫 actor 推導 cross-platform test：mock env `SPECLINK_AGENT_HOST` 與不存在情境、`whoami` lookup 成功與失敗 fallback `unknown`、`hostname` lookup 成功與失敗 fallback `unknown`；對應 spec requirement「Actor SHALL be resolved by fallback chain when `--actor` flag is omitted」；驗證方式：CI matrix（Linux/macOS/Windows × stable Rust）皆綠
- [x] 11.2 在 `crates/cli/tests/task_workflow.rs` 加 Windows-specific atomic rename regression test（用 `tempfile` + `std::fs::rename` 對 tasks.md 寫入，斷言 Windows ARG_LOCK / sharing violation 不會發生）；驗證方式：Windows CI job 該 test 綠

## 12. 文件與規格交叉檢核

- [x] 12.1 更新 `README.md`：在既有「AI workflow stage 指令」清單加入 `apply start / pause / task list / done / undo` 與一段 walking-skeleton 4-state mode 範例；驗證方式：`grep -E "apply start|task done" README.md` 命中、人工 review 範例可貼即跑
- [x] 12.2 在 `doc/speclink-design.md` 既有 §6.2 / §6.3 / §16.7 內容**不動**的前提下，於 §6.2 表結尾加一行 footnote 指向「slice A3=`add-state-machine-and-apply` 落實 6-state transition table」；§16.7 表結尾同樣加 footnote 指向 A3；驗證方式：grep 兩處 `A3=` footnote 命中
- [x] 12.3 對 `doc/protocol/operations.md` 的 `apply.start` / `apply.pause` / `task.done` 三條 op spec 與本 slice 實作做 prose review，輸出 review 報告至 PR description（不修 operations.md 內容）；驗證方式：PR description 內含 3 條 op 的「contract aligned / drift detected」逐條對照
- [x] 12.4 跑 `spectra analyze add-state-machine-and-apply` 與 `spectra validate add-state-machine-and-apply` 全綠；對應 task 0.1 / 0.2 traceability checklist 自動驗證
