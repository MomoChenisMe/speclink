## 1. 技能正典字面

- [x] 1.1 （design D1 第六訊號與准入測試的字面；spec improve-skill Requirement「掃描精髓段逐字保留」）在 crates/speclink-core/assets/skills/improve.md 的 Step 3：引言與清單標題由 five 改 six，第 5 條之後新增第 6 條「Grouping you can only guess from file names」，deletion test 段落之後新增「The sixth signal has its own admission criterion」段（讀者預測測試＋三條件＋「只搬家不算」＋同碰訊號 1 時 deletion test 先判），「a list of twelve」句與 Step 4 `strongly recommended` 定義改認准入判準，其餘段落逐字不動。驗證：對該檔執行 grep 斷言「The six friction signals」出現 2 次、「five friction」出現 0 次、「reader-prediction test」出現至少 1 次；`git diff` 只含 Step 3 的新增行與 five→six 兩處改字。 <!-- speclink-task:tsk_01M259S4Q1RSTQF3087QJ19ZW1 -->
- [x] 1.2 （design D2 深度四問對應版、D3 Guardrails；spec improve-skill Requirement「candidates 以討論記錄承載」）在同一檔的 Step 5 interface depth check 條目之後新增「For a sixth-signal candidate these four questions replace the four above」與「Two practices ride along with a sixth-signal candidate」兩條（Grouping source／Path stability／Path hazards／Reader-prediction test 四問＋測試鏡射搬移與切刀依路徑相依、不用 worktree 平行兩條做法），Guardrails 的 Deletion test 一條改為「An admission criterion gates every candidate — the deletion test for the first five signals, the reader-prediction test for the sixth, and moving complexity around never counts」。驗證：grep 斷言「Grouping source」「Path stability」「Path hazards」「An admission criterion gates every candidate」各出現 1 次；discussion-hold-until-release 落地後的扇出段落逐字不動（`git diff` 不含該段）。 <!-- speclink-task:tsk_01M259S4Q19F58SA4RBW6KTZS6 -->

## 2. 版號三連動與再生

- [x] 2.1 （design D4 版號三連動與排程）確認 discussion-hold-until-release 已封存並提交（`git log --oneline -5` 含其封存 commit、工作樹無其 golden 改動）後，將 crates/speclink-core/src/init.rs 的 `ASSET_VERSION` 由 v1.32.0 bump 為 v1.33.0；執行 `UPDATE_GOLDEN=1 cargo test -p speclink-core --test it render_golden` 與 `UPDATE_ASSETS_LOCK=1 cargo test -p speclink-core --test it render_golden` 再生四份快照與 assets.lock。驗證：不帶環境變數再跑 `cargo test -p speclink-core --test it render_golden` 綠；四份快照的 `git diff` 只含 Step 3／Step 5／Guardrails 的新增行與版號行，assets.lock 只含 improve 與版號相關項。 <!-- speclink-task:tsk_01M259S4Q1AFXG73H37VNS2NR0 -->
- [x] 2.2 （design D4）執行 `cargo build -p speclink-cli` 後以 target/debug/speclink 執行 `speclink update`，再生 repo 內 .claude/skills 與 .agents/skills 共 37 份 SKILL.md。驗證：.claude/skills/speclink-improve/SKILL.md 與 .agents/skills/speclink-improve/SKILL.md 各含「The six friction signals」「reader-prediction test」「Grouping source」；`grep -rl 'v1.33.0' .claude/skills .agents/skills | wc -l` 為 37；其餘 35 份的 `git diff` 只含版號行。 <!-- speclink-task:tsk_01M259S4Q1VHXQ3XYKB72YXFY0 -->

## 3. 規格對照與收尾

- [x] 3.1 （design D5 規格 delta 形狀；spec improve-skill Scenario「六條 friction 訊號逐條在列」「第六訊號帶讀者預測測試」「第六訊號的深度四問對應版」）逐條對照兩份渲染實例：六條訊號逐條在列且 deletion test 限定前五條、准入測試三條件在列、深度四問對應版與兩條做法在列、Guardrails 註明第六訊號改答讀者預測測試；並確認 discussion-hold-until-release 落地後的扇出段落與 Scenario「分期立案帶 --hold」的字面仍相符。驗證：`speclink validate improve-layout-signal` 綠；`speclink analyze improve-layout-signal` 零 CRITICAL／WARNING；`node --test scripts/*.test.mjs` 綠（vocabulary-guard 掃 crates/speclink-core/assets/skills 面）。 <!-- speclink-task:tsk_01M259S4Q1BKHDXK178Q75M1RZ -->
