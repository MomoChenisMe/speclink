## 1. 基準

- [x] 1.1 建立回歸基準：改碼前跑 `cargo test -p speclink-core review::`、`cargo test -p speclink-core verify::` 記下測試數（預期 51／35），並跑 `cargo test -p speclink-core --test it render_golden::`、`cargo test -p speclink-cli --test it`、`cargo test -p speclink-desktop-core` 確認全綠；基準本身若紅，先停下回報。驗證：上列指令 exit code 0，測試數已記錄。路徑：crates/speclink-core、crates/speclink-cli/tests/it、apps/desktop/core <!-- speclink-task:tsk_01M23EBH0M0V1G8TEY7HPTP16H -->

## 2. anchors 對映單源（design D1 ChangeMeta::anchors 以 StationId 窮舉對映）

- [x] 2.1 【Red】在 crates/speclink-core/src/model.rs 測試模組寫 D1 ChangeMeta::anchors 以 StationId 窮舉對映的測試：只帶 `reviewed_*` 的 meta，`anchors(&REVIEW)` 取到 at／tasks_total／scope 三欄、`anchors(&VERIFY)` 三欄皆空（`is_stamped` 為 false）；兩站都帶章時各取各的、互不遮蔽。驗證：`cargo test -p speclink-core model::tests::anchors_` 失敗於方法不存在。 <!-- speclink-task:tsk_01M23EBH0MFTZ65XC3K0XH9KG5 -->
- [x] 2.2 【Green】在 crates/speclink-core/src/station.rs 新增 `StationId { Review, Verify }` 與 `Station.id` 欄位，把 `REVIEW_DOC`／`VERIFY_DOC` 與兩個站常數 `REVIEW`／`VERIFY` 定義進 station.rs（欄位值逐字沿 review.rs／verify.rs 的 STATION）；在 model.rs 實作 `ChangeMeta::anchors(&self, st: &Station) -> StampAnchors<'_>`（對 `st.id` 窮舉 match）。此步 review.rs／verify.rs 仍在、暫時同時存在兩份常數，編譯須通過。驗證：`cargo test -p speclink-core model::tests::anchors_` 全綠。路徑：crates/speclink-core/src/station.rs、crates/speclink-core/src/model.rs <!-- speclink-task:tsk_01M23EBH0MF0Y2VHAD0N8SNE58 -->

## 3. freshness 改吃 meta（design D2 station::freshness 直接吃 meta）

- [x] 3.1 【Red】在 station.rs 測試模組寫 D2 station::freshness 直接吃 meta 的測試：`freshness(&REVIEW, &meta, &counts, &rf)` 對無章 meta 回 Unknown、章齊備且錨符回 Fresh、任務錨破回 Stale，且 verify 章的存在不影響 review 站判定。驗證：`cargo test -p speclink-core station::tests::freshness_takes_meta` 失敗於簽名不符。 <!-- speclink-task:tsk_01M23EBH0MEY6KRTS1YCRA91G1 -->
- [x] 3.2 【Green】`station::freshness` 改簽名為 `(st, meta, counts, read_file)`，判定本體留在函式內、開頭改由 `meta.anchors(st)` 取錨（不另切私有轉接層）；`archive.rs` 的 `guard_stale_stamps` 兩臂手寫對映改為 `[&REVIEW, &VERIFY]` 迭代加 `change.meta.anchors(st)`；review.rs／verify.rs 的門面 `freshness` 暫改為轉發到新簽名（下一組刪除）。可觀察行為：archive 章失效守門既有測試斷言不改全綠。驗證：`cargo test -p speclink-core station::tests::freshness_takes_meta`、`cargo test -p speclink-core archive::tests::` 全綠；`grep -n 'reviewed_at.as_deref()' crates/speclink-core/src/archive.rs` 零命中。路徑：crates/speclink-core/src/station.rs、crates/speclink-core/src/archive.rs <!-- speclink-task:tsk_01M23EBH0MC774Q1ARVN9W3SJD -->

## 4. 門面刪除與呼叫端改寫（design D3 常數與門面搬進 station.rs、兩檔刪除；D4 呼叫端改寫清單）

- [x] 4.1 先做測試搬家（design D5 測試搬家：兩個子模組、呼叫加站別、斷言不動）：把 review.rs 的 51 條與 verify.rs 的 35 條測試連同各自 fixture 原封搬進 station.rs `mod tests` 底下的 `mod review`／`mod verify` 子模組，呼叫機械改寫為帶 `&REVIEW`／`&VERIFY`（`add_round`、`stamp`、`stamp_with_scope`、`discard`、`freshness`、`station::show(&STATION, …)`→`show(&REVIEW, …)`），斷言字串、fixture、測試名一字不動；review.rs／verify.rs 的測試模組刪除。驗證：`cargo test -p speclink-core station::tests::review::` 通過 51 條、`cargo test -p speclink-core station::tests::verify::` 通過 35 條；`git diff --color-moved=dimmed-zebra` 檢視斷言行皆為搬移。路徑：crates/speclink-core/src/station.rs、crates/speclink-core/src/review.rs、crates/speclink-core/src/verify.rs <!-- speclink-task:tsk_01M23EBH0M6EWG7XN2HCB2EGYR -->
- [x] 4.2 依 D4 呼叫端改寫清單改寫 speclink-core 內的呼叫端：command/mod.rs 的 8 個 Review／Verify 臂改 `crate::station::xxx(&crate::station::REVIEW|VERIFY, …)`、`classify` 改 downcast `crate::station::NotFound`、測試的 `REVIEW_DOC`／`content_fingerprint` 改路徑；archive.rs 的 `guard_open_tickets` 與測試常數改路徑。然後執行 D3 常數與門面搬進 station.rs、兩檔刪除：刪除 crates/speclink-core/src/review.rs 與 verify.rs，lib.rs 移除兩個 `pub mod`。可觀察行為：review／verify／archive 動詞的訊息與 `--json` 不變。驗證：`cargo test -p speclink-core` 全綠；`test ! -e crates/speclink-core/src/review.rs && test ! -e crates/speclink-core/src/verify.rs`；`grep -rn 'crate::review::\|crate::verify::' crates/speclink-core/src` 零命中。路徑：crates/speclink-core/src/command/mod.rs、crates/speclink-core/src/archive.rs、crates/speclink-core/src/lib.rs <!-- speclink-task:tsk_01M23EBH0MYFH935JJS1HE6HSH -->
- [x] 4.3 依 D4 呼叫端改寫清單改寫 workspace 內的其他消費者：crates/speclink-cli/src/verbs/station.rs 的 `REVIEW_CLI`／`VERIFY_CLI` 改指 `core::station::REVIEW`／`VERIFY`；apps/desktop/core/src/query.rs 的兩個 DOC 常數與兩處 `freshness(&REVIEW|VERIFY, &c.meta, &counts, &read_file)`、測試的 `content_fingerprint`／`content_fingerprint_bytes` 改路徑；cache.rs 兩個 DOC 常數；verbs.rs 兩處 `station::discard(&REVIEW|VERIFY, store, change)`。可觀察行為：CLI review／verify 家族與 desktop 的 `reviewStatus`／`verifyStatus`／`reviewedNotPassed`／`verifiedNotPassed` 值不變。驗證：`cargo test -p speclink-cli --test it`、`cargo test -p speclink-desktop-core` 全綠；`grep -rn 'core::review::\|core::verify::\|speclink_core::review::\|speclink_core::verify::' --include='*.rs' crates apps` 零命中。路徑：crates/speclink-cli/src/verbs/station.rs、apps/desktop/core/src/query.rs、apps/desktop/core/src/cache.rs、apps/desktop/core/src/verbs.rs <!-- speclink-task:tsk_01M23EBH0MFTHJQS0VZ2AQ62FC -->

## 5. 收尾

- [x] 5.1 全面驗收：`cargo test -p speclink-core`、`cargo test -p speclink-core --test it`（含 `render_golden::` 零 diff）、`cargo test -p speclink-cli --test it`、`cargo test -p speclink-desktop-core` 全綠；`grep -n 'reviewed_at.as_deref()\|verified_at.as_deref()' crates/speclink-core/src/*.rs` 非測試碼只命中 model.rs 的 `anchors` 一處；station.rs 頂部與 `Station` 文件註解不再提「review.rs 薄實例」。驗證：上列指令 exit code 0、grep 結果如述。路徑：crates/speclink-core/src/station.rs <!-- speclink-task:tsk_01M23EBH0MC7F51FZTWX2MWCV6 -->
- [x] 5.2 `cargo clippy -p speclink-core -p speclink-cli -p speclink-desktop-core --all-targets` 在本 change 動到的檔案上，命中集合改前後相同（repo 基準本來就有存量紅燈且 CI 不跑 clippy，不以零警告為判準）、刪檔後不留孤兒 `use`；跑 `node --test scripts/*.test.mjs` 全綠。驗證：clippy 命中∩diff 檔案的集合以 git stash 前後比對相同；node 指令 exit code 0。路徑：crates/、apps/desktop/core、scripts/ <!-- speclink-task:tsk_01M23EBH0M09DPRPJAY0DD2JV0 -->
