## 1. 引擎：requirement 級重疊自 delta 內文解析，重用封存合併閘的解析函式

- [x] 1.1 在 crates/engine/speclink-core/src/lifecycle/plan.rs 新增 delta_touches(store, change)：對 delta_capabilities 的每個 capability 以 read_artifact 讀 delta 文字，經 archive 模組的 delta_operations（合併閘讀 delta 的同一套：換行正規化、parse_delta、rename_pairs、略過標題式 RENAMED；合併閘的 capability_violations 也改經它讀）取 ADDED／MODIFIED／REMOVED 的 requirement 名與 RENAMED 的 FROM、TO 名（兩端分開記，輸出皆為 RENAMED；操作以 DeltaOp::parse 對應），回傳 (capability, touch, requirement) 三元組；delta 讀不到或空白回空清單。驗證：plan.rs 單元測試 delta_touches_lists_every_operation_including_rename_from_and_to 與 delta_touches_empty_delta_is_zero_touch，`cargo test -p speclink-core plan::`。（規格：執行順序的基底與拓樸修正） <!-- speclink-task:tsk_01M38JDHAYC5CJAVV9E3VV80Q0 -->
- [x] 1.2 在 plan.rs 定義 RequirementOverlap 結構（serde rename 為 change、capability、requirement、ownOperation、otherOperation、conflict）與配對規則：每個觸碰有角色（MODIFIED 修改；REMOVED 與改名的舊名拿走；ADDED 與改名的新名帶進），角色先後依名稱此刻在不在正式規格（archive 模組的 canonical_names；在：修改 → 拿走 → 帶進，不在：帶進 → 修改 → 拿走）；角色不同為重疊且角色在前者先封存；雙方皆修改為重疊、先後照封存序；雙方皆拿走或皆帶進為 conflict。DeltaOp 另有 as_str／parse 供 wire 以字串搬運。驗證：單元測試 requirement_overlap_pairs_modified_with_modified、a_take_away_pairs_by_the_role_order、added_pairs_with_modified_as_overlap、added_with_added_is_conflict、a_rename_target_pairs_like_an_added_requirement、a_name_the_canon_carries_is_taken_away_before_it_is_brought_back、a_use_archives_before_a_take_away、taking_the_same_name_away_twice_is_a_conflict、names_brought_in_both_ways_list_each_other、same_capability_different_requirement_is_not_overlap、delta_op_strings_match_the_json_values，`cargo test -p speclink-core plan::`。（規格：執行順序的基底與拓樸修正） <!-- speclink-task:tsk_01M38JDHAYB13NHV4538FAMHR6 -->

## 2. 引擎：波次公式只看宣告前置，重疊改列 archiveAfter

- [x] 2.1 改寫 plan.rs 的 place 與 compute_from：波次 = 1 + 作用中前置最大波次；blockedBy 只含前置；archiveAfter 以一對 change 整體判定：由角色推出的方向優先（我方等對方時列對方；對方等我方時不列），雙方皆修改的重疊照封存序（配置順序經「等」修正的穩定拓樸排序）、在前者列入，conflict 不列也不改變其餘先後；PlanChange 追加 requirement_overlap 與 archive_after 欄位（serde rename requirementOverlap、archiveAfter），overlaps 欄位原樣保留。驗證：單元測試 no_depends_means_wave_one_regardless_of_overlap、requirement_overlap_sets_archive_after_not_wave、added_side_archives_first_regardless_of_basis、conflict_not_in_archive_after、in_progress_overlap_partner_archives_first、a_name_brought_in_orders_the_pair_over_the_placement、archive_order_never_runs_in_a_circle_through_placement_ordered_pairs、a_conflict_leaves_the_order_of_another_shared_name_to_the_placement，`cargo test -p speclink-core plan::`。（規格：執行順序的基底與拓樸修正） <!-- speclink-task:tsk_01M38JDHAYSWM7AX0H95JAZNVH -->
- [x] 2.2 新增 crate 內的 compute_strict（command 層新增 Command::PlanStrict 呼叫它，Command::Plan 維持 ranks 單欄；compute 與 compute_with_ranks 的簽名不變）：strict 時目錄級重疊推波次並併入 blockedBy，缺 rank 者只依 created 排，與本變更前輸出逐位元一致；requirementOverlap 與 archiveAfter 照常計算。驗證：單元測試 strict_overlap_reproduces_capability_level_waves 以既有 basis_order 測試資料斷言舊波次與舊 blockedBy、strict_overlap_keeps_the_created_order_for_the_unranked 斷言 task 數不同時仍照 created，`cargo test -p speclink-core plan::`；command 測試 plan_strict_overlap_reaches_the_engine。（規格：執行順序的基底與拓樸修正） <!-- speclink-task:tsk_01M38JDHAY5AS3TVJTSY2TZKSM -->
- [x] 2.3 crates/host/speclink-server/src/api/routes.rs 的 Command::Plan 維持原形（strict 另立 Command::PlanStrict，不必補欄位）；apps/desktop/core/src/query.rs 的 plan 注入測試改為新語意（只共用 capability 的夥伴同為第 1 波、blockedBy 空，overlaps 仍列目錄級）；整個 workspace 編譯通過。驗證：`cargo build --workspace`、`cargo test -p speclink-desktop-core query::`。 <!-- speclink-task:tsk_01M38JDHAYASY3WE5V4W6GJM0R -->

## 3. 引擎：缺 rank 的 tie-break 改為被依賴數、task 總數、created

- [x] 3.1 plan.rs 的 Entry 加 dependents（被依賴數，作用中可配置者 depends_on 含它的去重個數）與 task_total（tasks.md 經 tasks 模組 parse 的 task 個數；無 tasks.md 或其中沒有任何 task 時為 None，於同層殿後），基底序改為 Entry::basis_cmp 的比較：階段 → 有 rank 者依 rank → 缺 rank 者依被依賴數多、task 總數少 → created（缺者殿後）→ 名字；有 rank 者不受新鍵影響。plan.rs 另公開 basis_order(store)，apps/desktop/core/src/query.rs 的 board_sorted_changes 改用它排序（刪去排序鍵複本，壞 meta 卡以名稱序接在後面），plan 成環時看板退回序同樣照新鍵；plan.rs 另公開 board_order(store)（看板顯示序：配置順序，成環時退回基底序，move_rank 的欄序取自同一個函式），query.rs 的 board_order 改照它排。驗證：單元測試 unranked_sort_by_dependents_then_task_total_then_created、missing_tasks_sorts_last_among_unranked、tasks_md_without_a_task_sorts_last_like_a_missing_one、board_order_is_the_placement_or_the_basis_order_on_a_cycle、ranked_changes_ignore_new_keys，既有 basis_order_* 測試改資料後仍綠，`cargo test -p speclink-core plan::`；query.rs 測試 cycle_fallback_follows_the_engine_basis_order，`cargo test -p speclink-desktop-core query::`。（規格：執行順序的基底與拓樸修正、看板卡片順序以 board_rank 欄位為真相） <!-- speclink-task:tsk_01M38JDHAY1HJAMP3HDA5T40ZN -->
- [x] 3.2 rank 移動的依賴檢查（check_ranks；move_rank 在寫入前以補章加新 rank 的整張表呼叫它）沿用新排序重算同欄順序，檢查規則不變；它原本的單一 rank 入口 check_rank_move 只剩桌面拖排一個呼叫端，拖排改呼叫 move_rank 後一併刪除，既有測試改測 check_ranks（跨前置拒絕、跨依賴者拒絕、重疊夥伴翻轉允許、只看同欄、不寫入、容忍未知名稱）。驗證：`cargo test -p speclink-core plan::` 內 rank_move_check_ 開頭的測試綠。 <!-- speclink-task:tsk_01M38JDHAY8KK3QNJKMBD65YZ4 -->

## 4. 引擎與 CLI：排序鍵演算搬進 speclink-core，看板拖排與 change rank 共用同一步

- [x] 4.1 把 apps/desktop/core/src/rank.rs 的 midpoint、spread、neighbor_midpoint、ranked_in_order 與其測試整檔搬到 crates/engine/speclink-core/src/lifecycle/rank.rs，於 lifecycle 模組宣告並由 crate 根（crates/engine/speclink-core/src/lib.rs）re-export 為 `speclink_core::rank`；刪除 apps/desktop/core/src/rank.rs 與 apps/desktop/core/src/lib.rs 的模組宣告，apps/desktop/core/src/manage.rs 與 apps/desktop/src-tauri/src/remote.rs 改為直接引用 `speclink_core::rank`。驗證：`cargo test -p speclink-core rank::`、`cargo test -p speclink-desktop-core` 與 `cargo test -p speclink-desktop` 皆綠。 <!-- speclink-task:tsk_01M38JDHAYJ3B3T08WBSH26E7R -->
- [x] 4.2 plan.rs 實作 set_rank 與 move_rank：set_rank 守門依序（name、再 anchor 須作用中且 metadata 可解析、兩者不相同、同階段、name 無 rank 或帶 force）後，以錨點與 Side（Before／After）在欄內找出前後鄰居，照 move_rank 的做法寫入（兩者共用 move_start 查 name、取 rank 表與可配置清單）；move_rank 是看板拖排與 change rank 共用的一步——欄序為 plan 配置投影到該欄（成環時為基底序、只含 metadata 有效者、被移動者除外），欄內缺 rank、rank 序與顯示序不一致（ranked_in_order 為假）或 rank 不合生成格式（generated_shape：合法 board_rank 且不以 a 結尾；只在本機寫入時檢查，remote 拖排共用的 ranked_in_order 不看格式）時以 spread 按顯示序算補章，neighbor_midpoint 算新 rank；寫入前檢查要寫的 change 皆有 .openspec.yaml、以新 rank 表跑依賴檢查，任一不過即零寫入；寫入經呼叫端傳入的 write 逐 change 落在各自的副本，順序為補章逐一、最後寫 name，中途失敗停止回報且已寫保留。apps/desktop/core/src/manage.rs 的看板拖排改呼叫 move_rank（刪去自己那份補章與中點，被拒時零寫入）。驗證：單元測試 a_hand_edited_rank_gets_the_column_restamped_before_the_move、rank.rs 的 generated_shape_accepts_only_keys_the_midpoint_can_use、rank_before_unranked_anchor_stamps_column_then_writes、rank_refuses_when_already_ranked_without_force、rank_force_overwrites、rank_refuses_across_declared_dependency、rank_refuses_different_stage、rank_refuses_self_anchor、move_rank_puts_the_change_between_the_given_neighbors、move_rank_leaves_the_moved_change_out_of_the_stamp_decision、move_rank_refusal_writes_nothing_even_when_the_column_needs_stamps、move_rank_refuses_an_unknown_or_corrupt_change，`cargo test -p speclink-core plan::`；manage.rs 測試 reorder_refusal_writes_nothing_even_when_the_column_needs_stamps，`cargo test -p speclink-desktop-core manage::`。（規格：change rank 動詞寫入看板順序鍵、欄內存在缺 rank 卡時整欄補章） <!-- speclink-task:tsk_01M38JDHAY0BMZ1P0YC4BACATD -->
- [x] 4.3 crates/adapters/speclink-cli/src/verbs/plan.rs 的 ChangeCommands 加 Rank 子指令：`speclink change rank <name> (--before <other> | --after <other>) [--force] [--json]`，--before 與 --after 以 clap 互斥且必擇一；fs 臂直接呼叫引擎 set_rank——讀取走 worktree overlay（與 plan 同一視角），寫入經同檔私有的 home_store_for 逐 change 落在各自的副本（副本已不含該 change 時退回主 checkout，與 overlay 讀取同一個存在判定；change depends 也改用它，單元測試 home_store_follows_the_copy_that_still_holds_the_change）；成功 stdout 一行 `✓ <name> ranked before <other>`（或 after），--json 輸出 { "change", "anchor", "position" } 三鍵不含 rank 值；remote 模式由 crates/adapters/speclink-cli/src/main.rs 的 dispatch 明確拒絕、stderr 說明順序由 board resource 決定、非零 exit、不發請求。驗證：crates/adapters/speclink-cli/tests/it/plan_verbs.rs 的 change_rank_before_writes_board_rank_and_reorders_plan、change_rank_json_shape_has_no_rank_value、change_rank_refuses_already_ranked_and_force_overwrites、change_rank_refuses_dependency_cross、change_rank_refuses_different_stage、change_rank_remote_mode_refuses_without_request、change_rank_refuses_a_change_without_metadata_before_any_write，crates/adapters/speclink-cli/tests/it/worktree_overlay.rs 的 change_rank_writes_the_worktree_copy_of_a_mapped_change、change_rank_sees_a_change_created_after_the_worktree_branched、change_rank_stamps_each_change_in_its_own_copy，`cargo test -p speclink-cli --test it plan_verbs`、`cargo test -p speclink-cli --test it worktree_overlay`。（規格：change rank 動詞寫入看板順序鍵） <!-- speclink-task:tsk_01M38JDHAYGRBMW6DKDHD9VP5M -->

## 5. CLI：plan 輸出追加欄位，既有欄位保留

- [x] 5.1 speclink plan 加 `--strict-overlap` 旗標並傳入引擎；`--json` 的 changes 每項為九鍵（name、wave、stage、dependsOn、overlaps、blockedBy、ready、requirementOverlap、archiveAfter），requirementOverlap 每項六鍵 camelCase。remote 模式帶此旗標時由 crates/adapters/speclink-cli/src/main.rs 的 dispatch 以固定訊息拒絕、不發任何請求；新增 specs/verb-contract delta 把它列為兩模式輸出的第 6 項明文分歧，並修改「模式分岔的單點宣告」把它與 change rank 列為 Dual 家族內的 FsOnly 子情形，docs/verb-contract.md 與 docs/verb-contract.zh-TW.md 同步。驗證：plan_verbs.rs 新增 plan_json_has_nine_keys_and_requirement_overlap_shape（斷言鍵名與型別）、plan_strict_overlap_reproduces_old_waves，remote_plan.rs 新增 remote_plan_refuses_strict_overlap_without_a_request，`cargo test -p speclink-cli --test it plan_verbs`、`cargo test -p speclink-cli --test it remote_plan`。（規格：plan 動詞輸出波次與阻擋清單、動詞人眼輸出的兩模式同形、模式分岔的單點宣告） <!-- speclink-task:tsk_01M38JDHAY9AQKY8PGF51J6ZW4 -->
- [x] 5.2 人眼輸出每個 change 行尾依序附 ` — blocked by: …`、` — archive after: …`、` — conflicts with: …`，空者不印，--no-color 無 ANSI。驗證：plan_verbs.rs 新增 plan_human_output_orders_three_suffixes（斷言 `• e [proposed] — blocked by: a — archive after: b — conflicts with: c` 形狀；行首沿用既有 `• <名稱> [<階段>]`）與既有人眼測試更新，`cargo test -p speclink-cli --test it plan_verbs`。（規格：plan 動詞輸出波次與阻擋清單） <!-- speclink-task:tsk_01M38JDHAYEZMPMJ2P3FVS98NM -->

## 6. 技能資產以有無 board_rank 為 rank 代寫分界

- [x] 6.1 crates/engine/speclink-core/assets/skills/propose.md 的 Pending-change landscape check：硬信號段改為「requirement 級重疊由 plan 依名稱此刻在不在正式規格排出先後、算成 archiveAfter，兩個變更都帶進（新增或改名成）或都拿走（移除或改名掉）同名 requirement 為 conflict；代理人不判定重疊，conflict 時向使用者提其中一邊要改」；depends 落檔後加插隊判定段（提案中、task 數少且無人依賴而急 → `speclink change rank <本變更> --before <第一個比它大的提案中變更>`；拒絕時口頭建議不加 --force）；plan 呈現段加 archiveAfter 的封存順序句。驗證：內容審閱——再生後的 .claude/skills/speclink-propose/SKILL.md 含 `change rank`、`archiveAfter`、`conflict` 字面且不含「delta capability overlap」；Example 第 3 列（add-a 已有 board_rank）由 CLI 測試 spec_example_soft_dependency_declared_after_propose 走一遍，`cargo test -p speclink-cli --test it plan_verbs`。（規格：收尾盤點提案中變更的執行順序） <!-- speclink-task:tsk_01M38JDHAYZS6ZVBV6DVN3XCJ7 -->
- [x] 6.2 crates/engine/speclink-core/assets/skills/ingest.md 收尾段同步：硬信號字面與插隊判定段皆與 propose 逐字一致（render_golden 的 propose_and_ingest_skills_share_the_landscape_wording 鎖住）；rank 段以階段分岔（提案中照 propose 的插隊判定、已有 rank 時口頭建議、進行中／已就緒不執行）。驗證：內容審閱——再生後的 speclink-ingest SKILL.md 含分岔三句，且落檔段與 propose 的同一段逐字一致（diff 比對）；Example 第 5 列（add-a 已有 board_rank）由 CLI 測試 spec_example_ingest_shrunk_change_jumps_the_queue 走一遍。（規格：ingest 收尾重判本變更的軟依賴） <!-- speclink-task:tsk_01M38JDHAYHFDZRHG8476Z4SYS -->
- [x] 6.3 crates/engine/speclink-core/assets/skills/archive.md：封存前提示改讀 archiveAfter（附「它們封存後重讀同名 requirement 區塊、對照正式規格重寫再封存」）、conflict 以白話提改掉其中一邊（兩邊都新增或改名成同名、或都移除或改名掉同名，後封存者封存失敗）、blockedBy 另提宣告前置未封存、封存前記下 archiveAfter 含目標的其他 change 並於封存成功後提醒它們先對照正式規格重寫（走 ingest）再封存，刪去「動到同一份規格」與任何「跑 drift 即可」字面；未指名候選改標 archiveAfter（空「可封存」、非空「等 X 封存」）與 blockedBy 的「前置 X 未封存」。驗證：內容審閱——再生後的 speclink-archive SKILL.md 含 `archive after`／`archiveAfter`、`可封存`、`等 <name> 封存` 字面，不含「跑 drift」。（規格：archive 技能以 plan 建議封存順序、未指名時的候選清單依 plan 順序） <!-- speclink-task:tsk_01M38JDHAY57VXMK1J8ZAP7BVT -->
- [x] 6.4 crates/engine/speclink-core/assets/skills/commit.md 的先封存子流程：封存前提示與 archive.md 同一套字面（archiveAfter、conflict、blockedBy 三段，皆僅建議），封存後的「排在後面的 change 先重寫」提醒也與 archive.md 同一段（render_golden 的 archive_and_commit_skills_share_the_plan_hand_off_wording 鎖住）。驗證：內容審閱——再生後的 speclink-commit SKILL.md 含三段字面且與 archive 的提示句一致；render_golden 的 commit 測試釘住「對照正式規格重寫」並檢查不含「跑 drift」「對照正典」「合併閘」。（規格：先封存子流程的順序提示與收尾提醒） <!-- speclink-task:tsk_01M38JDHAYNC122AJHYF4M2FR1 -->
- [x] 6.5 刪去 crates/engine/speclink-core/assets/skills/apply.md 與 apply-worktree-pre.md 的「a blocker that comes from delta-capability overlap keeps its place until it lands」子句（blockedBy 已不含重疊者）；bump crates/engine/speclink-core/src/workspace/init.rs 的 ASSET_VERSION，以 UPDATE_ASSETS_LOCK 再生 crates/engine/speclink-core/tests/golden/assets.lock，更新五份 golden 快照（claude、codex、claude-worktree、neutral-cli、neutral-tool-call），`cargo build -p speclink-cli` 後執行 `speclink update` 再生 .claude 與 .agents 的 SKILL.md（再生檔不進 evidence，收尾以 git status 盤點）。驗證：`cargo test -p speclink-core --test it render_golden::` 綠，`speclink --version` 顯示新 ASSET_VERSION，再生後的 speclink-apply 與 speclink-apply-with-worktree SKILL.md 不含「delta-capability overlap」。 <!-- speclink-task:tsk_01M38JDHAY6P3JNBTAC81WK12W -->

## 7. wire 帶上兩個新欄位（自第二刀提前）

- [x] 7.1 crates/protocol/speclink-protocol/src/query.rs 的 PlanChangeEntry 追加 `requirement_overlap: Vec<PlanRequirementOverlap>` 與 `archive_after: Vec<String>`（皆 serde default、camelCase），新增 PlanRequirementOverlap 六欄型別（兩個操作以字串搬運），JSON Schema 匯出隨型別更新；序列化九鍵、缺兩新鍵反序列化為空陣列。驗證：query.rs 單元測試 plan_response_deserializes_the_plan_payload、plan_change_entry_reads_a_missing_array_as_empty、plan_response_serializes_the_cli_plan_json_keys、plan_dtos_export_json_schema，`cargo test -p speclink-protocol`（規格：plan 回應 payload）。 <!-- speclink-task:tsk_01M38JQ5S6A1QJFWRHTF8GW5MX -->
- [x] 7.2 crates/protocol/speclink-remote/src/convert.rs 的 plan_report 逐欄搬運 requirementOverlap 與 archiveAfter 到引擎的 PlanChange（移除空值填入），操作字串以引擎的 DeltaOp::parse 轉回、不認得即回錯誤；既有 stage／wave 成員失敗行為不變。驗證：convert.rs 測試 plan_report_round_trips_the_engine_plan_through_the_wire（資料含新欄位）、plan_report_carries_requirement_overlap_and_archive_after、plan_report_refuses_a_delta_operation_it_does_not_know；crates/protocol/speclink-remote/tests/it/typed_client.rs 的 plan_gets_the_typed_plan 斷言新欄位，`cargo test -p speclink-remote`（規格：plan 回應 payload）。 <!-- speclink-task:tsk_01M38JQ5S6Y9VXXP8W7ST1EDRY -->
- [x] 7.3 crates/host/speclink-server/src/api/routes.rs 的 plan handler 從引擎報告逐欄映射新兩欄到 DTO；apps/desktop/src-tauri/src/remote.rs 的測試替身補兩欄。驗證：crates/host/speclink-server/tests/it/api/plan_api.rs 新增 plan_entry_carries_requirement_overlap_and_archive_after 與 plan_same_capability_different_requirement_has_empty_archive_after，`cargo test -p speclink-server --test it plan_api`；`cargo test -p speclink-desktop` 綠（規格：plan 唯讀衍生查詢端點）。 <!-- speclink-task:tsk_01M38JQ5S6E8P5N8HY9500F5F9 -->
- [x] 7.4 CLI remote 模式 `speclink plan --json` 與 fs 模式對同一內容九鍵同形、逐位元一致，含 requirement 級重疊與 archiveAfter。驗證：crates/adapters/speclink-cli/tests/it/remote_plan.rs 的 remote_plan_prints_the_fs_output_for_the_same_content 以兩者 MODIFIED 同一 requirement 的內容斷言九鍵與 `archive after:` 尾註，`cargo test -p speclink-cli --test it remote_plan`（規格：plan 回應 payload）。 <!-- speclink-task:tsk_01M38JQ5S61Q809PP2F1704TNH -->

## 8. 收尾驗證

- [x] 8.1 全面回歸：`cargo test -p speclink-core`、`cargo test -p speclink-cli --test it`、`cargo test -p speclink-desktop-core`、`cargo test -p speclink-desktop`、`cargo test -p speclink-protocol`、`cargo test -p speclink-remote`、`cargo test -p speclink-host`、`cargo test -p speclink-server --test it plan_api`，並以 `speclink plan --json` 對本 repo 實跑一次確認 changes 每項九鍵、無依賴者皆第 1 波；最後執行 `node --test scripts/*.test.mjs scripts/*/*.test.mjs` 確認詞彙守門與連結檢查綠。 <!-- speclink-task:tsk_01M38JDHAYZP53905R2ZRSBRSK -->
