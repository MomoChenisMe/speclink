<!-- 純重構：每組先以「鎖定測試」釘住現行行為（TDD 的 Regression Test Workflow——測試一寫即綠即為基準），再改結構；新增的型別走 RED → GREEN。四份 golden 是唯一裁判，任何一份變紅只修程式、不再生 golden。 -->

## 1. skills.rs 單一 render 與代換表（先做，golden 為裁判）

- [x] 1.1 鎖定測試：在 `crates/speclink-core/src/skills.rs` 的 tests 新增三個 target（Claude 的 fork 技能 drift、Codex 的 apply、Custom 描述子 invocation 為 cli 與 tool-call）經 `render_skill_file_for` 的輸出各以恰一個 `\n` 結尾、frontmatter 版號行等於 `ASSET_VERSION`、Claude fork 技能含「## Claude fork context」前言而 Codex 與 Custom 不含、Custom 含「## Invocation」前言。驗證：`cargo test -p speclink-core skills::` 全綠（鎖定基準）。 <!-- speclink-task:tsk_01M1X7AARTDH27P0FPFT580NZX -->
- [x] 1.2 依 design「skills.rs 單一 render 與代換表」把 `render_skill_file`／`render_skill_file_custom` 合成 `render_skill_file_for` 一支、`substitute`／`substitute_neutral` 合成一張由 `RenderTarget` 填值的代換表（`spec_dir_slash`、`plan_dir`、`tool_name`、`slash_replacement`、`drop_plan_mode_lines`、`neutralize_skill_refs`），`substitute(body, tool, spec_dir)` 保留為 Builtin 公開便利入口；刪 `substitute_neutral`、`render_skill_file`、`render_skill_file_custom`，`crates/speclink-core/tests/it/render_golden.rs` 的呼叫維持可編譯。契約：三種 target 的輸出位元級不變。驗證：`cargo test -p speclink-core --test it render_golden::` 全綠，`git status` 顯示 `crates/speclink-core/tests/golden/` 與 `assets.lock` 零變動；`cargo test -p speclink-core skills::` 全綠；`grep -rn "substitute_neutral\|render_skill_file_custom\|fn render_skill_file(" crates apps` 零命中。 <!-- speclink-task:tsk_01M1X7AARTPFZD1SQ3H4VXXEFQ -->

## 2. `.speclink.yaml` 讀改寫收進 config.rs

- [x] 2.1 鎖定測試：在 `crates/speclink-core/src/config.rs` tests 新增 remote section 讀改寫的三個案例——檔案不存在時 `write_remote_section` 建檔且只含 remote 鍵；檔案含 `spec_dir`、描述子與未知頂層鍵時寫入／移除 remote section 後其餘鍵逐字保留；檔案內容非 mapping 時以 `invalid .speclink.yaml` 開頭的單行錯誤失敗且檔案不變；`remove_remote_section` 對無 remote 鍵的檔回 `Ok(false)` 且檔案位元級不變。驗證：先對 `init::` 版本跑綠（基準），搬家後對 `config::` 版本跑綠。 <!-- speclink-task:tsk_01M1X7AART0CEXWQRP72M1VSSZ -->
- [x] 2.2 依 design「`.speclink.yaml` 讀改寫收進 config.rs」把 `write_remote_section`／`remove_remote_section` 搬到 `crates/speclink-core/src/config.rs`，共用 `parse_yaml_mapping`，刪 `crates/speclink-core/src/init.rs` 的 `load_app_yaml_doc`；`init_remote` 內部、`crates/speclink-cli/src/verbs/connection.rs`、`apps/desktop/src-tauri/src/connections.rs`、`apps/desktop/src-tauri/src/remote.rs` 改 use 路徑。契約：CLI `speclink connection` 動詞與 desktop 綁定的檔案結果與錯誤字面不變。驗證：`cargo test -p speclink-core config::` 全綠；`cargo test -p speclink-cli --test it connection` 全綠；`grep -rn "load_app_yaml_doc\|init::write_remote_section\|init::remove_remote_section" crates apps` 零命中。 <!-- speclink-task:tsk_01M1X7AARTV63F0AFWKW8NTKRQ -->

## 3. ToolSelection 單一解析

- [x] 3.1 RED：在 `crates/speclink-core/src/init.rs` tests 新增 `ToolSelection::resolve` 四種輸入——純內建 `[claude, codex, claude]` 得 `builtins=[Claude, Codex]`、`legacy_fallback=false`；混一個合法描述子得 `customs` 一筆、`descriptor_error=None`；壞描述子（缺 `skills_dir`）得 `descriptor_error=Some(單行訊息)` 且 `builtins` 仍解析；空清單且 `.claude` 目錄存在得 `builtins=[Claude]`、`legacy_fallback=true`，無 `.claude` 目錄得空 `builtins`；未知名 `cursor` 進 `notes` 且訊息字面與今天 `update` 相同（`unknown tool 'cursor' in .speclink.yaml tools list (supported: claude, codex)`）。另測 `ToolSelection::builtins_only(&[Codex])` 得 `builtins=[Codex]`、無描述子。驗證：`cargo test -p speclink-core init::tests::tool_selection` 編譯失敗或紅燈。 <!-- speclink-task:tsk_01M1X7AART8ZS9M84V5J0KVWND -->
- [x] 3.2 GREEN：依 design「ToolSelection 單一解析，描述子錯誤延後裁決」實作 `ToolSelection` 與兩個建構子（`resolve`、`builtins_only`），描述子重名同樣落 `descriptor_error`。驗證：3.1 的測試全綠；既有 55 個 init.rs 測試不動仍綠（`cargo test -p speclink-core init::`）。 <!-- speclink-task:tsk_01M1X7AARTPGG9RE3F3KJB25N8 -->
- [x] 3.3 `apps/desktop/src-tauri/src/connections.rs` 的 checkout 預選改讀 `ToolSelection::resolve(root, &app).builtins`，`legacy_fallback` 為真、或解析後 `builtins` 為空（清單只有描述子／未知名）時改用 `detect_footprint_tools`（與今天「picked 為空就回退」等價：只依 footprint、不補 Claude），刪自寫的 tools 解析迴圈。契約：`inspect_checkout` 的 IPC 回傳 `tools` 陣列內容與順序不變。驗證：`cargo test -p speclink-desktop connections::` 中 `inspect_without_a_tools_list_preselects_only_actual_footprints`、`inspect_reports_only_builtins_from_a_mixed_tools_list` 全綠（此 crate 測試前先備妥 desktop sidecar 與 server-web dist）。 <!-- speclink-task:tsk_01M1X7AARTDXAWXTQDGQ6E9WFX -->

## 4. managed_skills 與 SyncPlan

- [x] 4.1 RED：在 `crates/speclink-core/src/init.rs` tests 新增——`managed_skills(Builtin(Claude), worktree_on=true, "openspec")` 的目錄名集合等於 registry 全集加 `speclink-` 前綴；`Builtin(Codex)` 與 `Custom` 等於 `for_codex` 子集；`worktree_on=false` 時兩者都不含 `speclink-apply-with-worktree` 與 `speclink-worktree-merge`；每筆內容等於 `render_skill_file_for` 同參數輸出。`SyncPlan::resolve` 對 `builtins_only(&[Codex])` 得一個 Builtin(Codex) target、`skills_root` 為 `<root>/.agents/skills`、`deselected_builtins=[Claude]`；對 `legacy_fallback` 選集得 `deselected_builtins` 為空；含描述子時多一個 Custom target 且 `skills_root` 為描述子的 `skills_dir`；`guard` 檢查的目錄集合等於各 target 的 `skills_root`（以「只有一個描述子目錄領先版本」的工作區斷言 guard 拒絕且訊息含兩個版號）。驗證：`cargo test -p speclink-core init::tests::managed_skills` 與 `init::tests::sync_plan` 紅燈。 <!-- speclink-task:tsk_01M1X7AART6PA9G2HGJRX1SSD9 -->
- [x] 4.2 GREEN：依 design「managed_skills 為受管技能集合唯一擁有者」與「SyncPlan 為唯一 adapter：resolve、guard、apply、diff」實作 `managed_skills`、`SyncTarget`／`SyncTargetKind`、`SyncPlan::resolve`（`worktree_skills_enabled` 只在此呼叫一次）、`guard`、`differing_files`（路徑以 `/` 組專案根相對字串、換行正規化）、`write_skills`。驗證：4.1 全綠；`grep -n "worktree_skills_enabled(" crates/speclink-core/src/init.rs` 除定義外只剩 `SyncPlan::resolve` 一個呼叫點。 <!-- speclink-task:tsk_01M1X7AARTHKN1JDXWQ3MH9VP1 -->
- [x] 4.3 依 design「apply 寫入順序與失敗後的可觀察狀態」實作 `SyncPlan::apply`（剝除 → 逐 target 寫檔與清孤兒 → 下架內建 prune → 自訂足跡 prune 與 save），`update` 改為 load AppConfig → `ToolSelection::resolve` → `descriptor_error` 即 `Err` 零寫入 → `SyncPlan::resolve` → guard（`allow_downgrade` 跳過）→ apply；刪 `generate_tool`、`generate_custom`、`expected_skill_dirs`、`skip_gated_skill`。契約：`UpdateOutcome` 的 `updated` 順序固定 claude、codex、描述子；`speclink update` 的 stdout／stderr 文字不變。驗證：`cargo test -p speclink-core init::` 全綠（含 `update_prunes_renamed_skill_directory`、`update_keeps_user_skill_directories_without_prefix`、`generation_omits_worktree_skills_when_the_policy_is_false`、`the_gate_applies_to_codex_and_custom_descriptors_alike`、`update_strips_a_descriptors_legacy_marker`、`dropping_the_deprecated_instructions_file_field_does_not_prune_the_descriptor`、`the_guard_covers_the_legacy_fallback_without_a_tools_list`、`the_guard_covers_custom_descriptor_skill_files`）；`cargo test -p speclink-cli --test it update` 全綠。 <!-- speclink-task:tsk_01M1X7AART6XER2XSZN5WJ99B4 -->
- [x] 4.4 `probe_assets` 改走計畫：load AppConfig 失敗回 `unknown` → `ToolSelection::resolve` → `SyncPlan::resolve` → 對 Builtin target 逐一 `probe_skills_dir` 組 `ToolAssetState` → 聚合 → 非 Current 時 `differing_files` 只取 Builtin；刪 `differing_managed_files`。契約：`AssetProbe` 的 `--json`／IPC 欄位（`status`、`currentVersion`、`tools[].tool/workspaceVersion/stale/newer/missing`、`differingFiles`）與值不變，描述子錯誤不影響探測結果。驗證：`cargo test -p speclink-core init::tests::probe` 與 `init::tests::skill_probe` 全綠；`cargo test -p speclink-desktop-core project::` 的 `probe_assets_*` 全綠。 <!-- speclink-task:tsk_01M1X7AARTZXSWRPBMKGQN3NSY -->
- [x] 4.5 依 design「init 在本刀只換寫入來源，不改流程」改 `init`／`init_remote`／`reconcile_builtin_tools`：init 兩支以 `ToolSelection::builtins_only` 建計畫、`plan.guard()` 取代 `skill_targets`＋`refuse_downgrade`、`plan.write_skills(root, force)` 取代 `generate_tool`，`workspace_init` 內容併入兩支本體；reconcile 改為改寫 tools 文字 → 以改寫後文字解析 AppConfig 建選集與計畫 → guard → 寫設定檔 → apply（不再重載原檔抽描述子、不再二次守門）；刪 `skill_targets`。契約：`speclink init`／`init --store remote`／`init --force` 的檔案結果、stdout 與守門訊息不變；reconcile 拒絕時連設定檔都不寫。驗證：`cargo test -p speclink-core init::` 全綠（含 `init_force_refuses_a_workspace_that_leads_the_engine`、`reconcile_refuses_a_leading_workspace_before_touching_the_config`、`reconcile_matches_init_output_for_the_same_selection`、`reconcile_bad_config_fails_loud_with_zero_writes`、`adopt_twice_with_same_tools_is_idempotent`）；`cargo test -p speclink-cli --test it init` 全綠；`cargo test -p speclink-desktop connections::` 的 `bind_switches_claude_to_codex_preserving_user_text_and_descriptors`、`desktop_bind_and_cli_remote_init_produce_isomorphic_artifacts`、`bind_retry_with_the_same_selection_is_byte_identical` 全綠。 <!-- speclink-task:tsk_01M1X7AARTM29D28ZSRENYKZ1R -->

## 5. 收尾

- [x] 5.1 對新增的公開介面（`ToolSelection`、`SyncPlan`、`config::write_remote_section`／`remove_remote_section`）套用 audit 的 sharp-edges 檢查表（`speclink instructions --skill audit`）：描述子 `skills_dir` 逸出專案根仍由 `is_project_relative` 在 `validate()` 擋下、`SyncPlan::apply` 的清孤兒只刪 `speclink-` 前綴目錄、guard 除 `allow_downgrade` 外無其他繞過口；把結論以三行註解記在 `SyncPlan` 型別文件註解。驗證：`grep -rn "generate_tool\|generate_custom\|expected_skill_dirs\|differing_managed_files\|skill_targets\|load_app_yaml_doc" crates apps` 零命中；`cargo clippy -p speclink-core` 無新警告。 <!-- speclink-task:tsk_01M1X7AARTBH604HP10DQF7RWM -->
- [x] 5.2 受影響面全跑並確認零 golden 變動：`cargo test -p speclink-core`、`cargo test -p speclink-core --test it render_golden::`、`cargo test -p speclink-cli --test it`、`cargo test -p speclink-desktop-core`、`cargo test -p speclink-desktop`（先備妥 sidecar 與 server-web dist）、`node --test scripts/*.test.mjs`；`git status --porcelain crates/speclink-core/tests/golden` 為空、`ASSET_VERSION` 未變。 <!-- speclink-task:tsk_01M1X7AARTZ5601DBB9EGT5RGV -->
