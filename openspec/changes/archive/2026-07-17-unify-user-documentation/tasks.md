## 1. 現況證據與產品狀態

- [x] 1.1 【紅】建立「D2 — 能力狀態使用證據分級而非檔案存在推論」與「產品狀態以證據分類目前與規劃能力」的查核基線：以 `speclink --help`、相關子指令 `--help`、`.agents/skills/*/SKILL.md`、`crates/speclink-core/assets/skills/`、Cargo workspace members、現行 tests／操作文件與正典 specs 交叉列出 Local CLI／skills、Local Desktop、Node SDK、Server／Store drivers、Desktop Remote Workspace、Agent／Protocol、營運能力的可操作入口與限制；確認現況至少能重現中英文 README 的 Server／Remote 狀態落後、getting-started 引用不存在 verify skill、optional design 被寫成固定產物三項失敗，並保存命令輸出摘要作為後續文件判定依據。驗證：證據表每項具兩個獨立來源或一個端到端來源，且上述三項基線均可觀察為失敗；此 task 只讀 `crates/speclink-cli/src/main.rs`、`crates/speclink-core/assets/skills/` 與其他實作來源，不修改 crates。 <!-- speclink-task:tsk_01KXQMC438MT53XK7KM5C7ZMZJ -->
- [x] 1.2 【綠】建立 `docs/product-status.zh-TW.md` 與 `docs/product-status.md`，使每個產品能力列都有 Available／Partial／Planned／Deprecated（繁中對應可用／部分可用／規劃中／已棄用）、使用者入口、1.1 的證據、限制／下一步與最後查核日期；清楚標示 legacy remote 路徑與缺失的進階 contract 文件，且不把 crate 或 spec 單獨存在寫成已完整交付。驗證：逐列人工對照 1.1 證據，兩版矩陣列集合與狀態一一相等，所有實際 Markdown 連結目標存在。 <!-- speclink-task:tsk_01KXQMC439XHQXJDBW5QFX5S7Z -->
- [x] 1.3 【重構】縮減產品狀態與 README／架構文件的重複敘述：product-status 保留完整證據與限制，其他入口只保留摘要及連結；重新執行 `speclink --help`、workspace members 與相關 skill inventory 抽查每個 Available 列，確認無未註明限制的誤報，且此整理不修改 `crates/speclink-core`、`crates/speclink-cli` 或任何 runtime 檔案。 <!-- speclink-task:tsk_01KXQMC4392RHYJP3YQWD19YTT -->

## 2. 完整工作流正典

- [x] 2.1 【紅】為「D3 — 完整工作流以主幹加決策分支呈現」「完整工作流指南說明用途與使用時機」與「討論結論後的轉出與併入分流完整」建立缺口基線：將 `.agents/skills/` 與 `crates/speclink-core/assets/skills/` 的 onboard、discuss、propose、apply、ingest、drift、analyze／validate、audit、commit、archive 規則對照現行 getting-started，確認 promote 對 propose、link→ingest→seal、直接封存 discussion、續作前 drift、utility skill 定位與 verify 限制均未完整呈現。驗證：缺口清單逐項指向現行 CLI help 或 skill 段落，且不得以歷史 change 作唯一證據。 <!-- speclink-task:tsk_01KXQMC4396DAXRGX1Z4V38373 -->
- [x] 2.2 【綠】建立 `docs/workflow.zh-TW.md` 與 `docs/workflow.md`：先呈現主幹與必經／條件式／utility 分類，再對每階段固定提供目的、使用與跳過時機、輸入、產物、Claude slash command、Codex `$skill`、底層 CLI／Host、完成判準、下一步與恢復方式；加入 discussion 結論後四路決策表，明示 `discuss promote` 只產生骨架、直接 propose 才完成必要 artifacts、既有 change 走 link→ingest→seal。驗證：2.1 缺口清單全部有對應章節或決策列，命令名稱可由目前 help／skills 觀察，兩版 H2 順序與決策表列相同。 <!-- speclink-task:tsk_01KXQMC43923ZE3JXKGZNSF6TM -->
- [x] 2.3 【重構】以意圖導向導覽精簡 workflow：讓「需求是否明確、是否已有 change、是否閒置、是否改需求、是否只求理解」五個問題可直接導向唯一推薦入口，避免重複 CLI reference；以 `rg` 確認繁中散文使用「轉為變更」「已轉出變更」「封存」「輪」「背景」，並人工走讀一條 discuss→propose→apply→archive 與一條 link→ingest→seal 路徑，確認每步的完成判準與下一步閉合。 <!-- speclink-task:tsk_01KXQMC439WRQG7KDPFK66EM0T -->

## 3. 快速入口與雙語鏡像

- [x] 3.1 【紅】建立「D1 — 以漸進揭露分配文件責任」「使用者文件採漸進揭露與單一責任」與「Getting Started 僅使用已驗證入口」的入口查核基線：將 README 現有首段的品牌圖片、SDD Engine 標語、繁體中文／English 切換、Rust SDD 引擎定位、共同 change／artifact／task／verify／archive 語意、Local Repo／Remote Store 雙路徑、Spectra App 2.3.1 相容基線、目前狀態與路線圖連結記為保留基線，而非待刪除的重複內容；另檢查文件地圖、getting-started 的 skill／CLI、固定四 artifacts 敘述與複雜分支深度。驗證：既有中英文 README 的結構與狀態需要同步校正、`speclink-verify` 無對應生成 skill、Claude／Codex 語法未區分、optional design 被寫成固定產物，並以 `speclink validate --help`、`speclink analyze --help`、`speclink archive --help` 與生成 skill 清單保存正確替代入口。 <!-- speclink-task:tsk_01KXQMC43962KTVH8NA5X0NTNH -->
- [x] 3.2 【綠】重整 `README.md` 與既有 `README.en.md`：保留或僅微調 3.1 所列首段，兩版皆呈現專案目的、起源與 Local Repo／Remote Store 雙路徑；其後提供最短 SDD 心智模型、由 product-status 摘出的當前能力、Local Repo 開始入口與完整文件地圖，所有詳細流程、完整狀態矩陣、架構、設定、SDK 與 Server 營運內容導向責任文件。驗證：品牌圖片仍可載入，標語與 Spectra App 2.3.1／parity／golden tests 相容基線存在，語言切換連結雙向可用，兩種部署路徑皆有定義，已實作與分階段建置內容可逐列追到 product-status，舊 remote REST v1 明確不是目標架構，路線圖連結有效，兩版 H2 集合與順序一致，且不複製完整狀態矩陣或架構章節。 <!-- speclink-task:tsk_01KXQMC439KMZN8MTBBWNZWC0E -->
- [x] 3.3 【綠】重寫 `docs/getting-started.zh-TW.md` 與 `docs/getting-started.md` 為可複製的 Local Repo 第一輪：分別展示 Claude、Codex 與直接 CLI 呼叫層級，使用現存 propose／apply／validate／analyze／archive 入口，說明 design 是條件式 artifact，將 discuss 與其他分流導向 workflow，並把不存在的 verify skill 改成誠實的目前限制與 product-status 連結。驗證：依文件逐條對照 `speclink init --help`、`speclink status --help`、`speclink validate --help`、`speclink analyze --help`、`speclink archive --help` 與 skill inventory，無不存在的子指令、旗標或 skill。 <!-- speclink-task:tsk_01KXQMC439ZZRR4J7RPAGNT5QC -->
- [x] 3.4 【重構】落實「D4 — 中英文鏡像與正典詞彙共同維護」與「中英文文件保持結構與事實對等」：對 `README.md`／`README.en.md`、getting-started、workflow、product-status 四組文件比較 H2 集合與順序、決策／狀態表列、命令與交叉連結；修正所有事實漂移，並以 `rg -n '促轉|已促轉|歸檔'` 檢查繁中使用者散文，僅允許歷史引用或必要 code span。驗證：四組結構比較皆相等、語言切換不遺失任何流程分支或狀態限制、avoid 詞彙零非例外命中。 <!-- speclink-task:tsk_01KXQMC4391AYK9RQ9BD32Y8NV -->

## 4. 目前與目標邊界及專案 context

- [x] 4.1 落實「目標架構與目前狀態維持清楚邊界」：在 `docs/platform-architecture.zh-TW.md` 與 `docs/implementation-refactor-roadmap.zh-TW.md` 開頭補 product-status／workflow 入口與查核日期說明，前者仍明示唯一目標架構，後者仍明示其下執行伴隨；SHALL 不搬動既有章節、Phase 或 Gate。驗證：以變更前後 H2 清單比較確認章節次序逐字相同，並人工確認 product-status 能反向連回兩份目標文件。 <!-- speclink-task:tsk_01KXQMC439QB729Y8KC70M00FK -->
- [x] 4.2 更新 `openspec/config.yaml` 既有 `context` 散文以反映目前 Cargo workspace、Host／Store／Server／Desktop／Protocol 與網路／async 現況，移除只有 core／cli 與無外部服務的過時描述；不改 `schema`、`locale`、`spec_locale`、`tdd`、`audit`、`rules` 或任何設定解析。驗證：以 YAML parse 或 `speclink instructions proposal --change unify-user-documentation --json` 確認 context 可讀且新敘述出現在 payload，並以 diff 確認 policy 欄位與 rules 逐位元未變；`crates/speclink-core` 與 `crates/speclink-cli` 零修改。 <!-- speclink-task:tsk_01KXQMC439K6WQSP6WYBFSACFN -->

## 5. 文件準確性與交付驗證

- [x] 5.1 執行「D5 — 將文件準確性驗證收斂成可重複清單」與「文件準確性具可重複驗證清單」：以 shell 擷取本 change 觸及 Markdown 的相對連結並逐一 `test -e`，以 `rg '^## '` 比較四組中英文文件 H2 次序，以矩陣第一欄比較 product-status 列集合，以 CLI help／`.agents/skills/`／`crates/speclink-core/assets/skills/` 驗證 getting-started 與 workflow 的可用入口，以 `rg` 檢查正典詞彙；刻意延後的 `docs/verb-contract.md` SHALL 只以無連結缺口文字出現。驗證：全部查核 exit code 0，且不存在空白 placeholder、失效連結、無證據狀態列或未註明狀態的規劃入口。 <!-- speclink-task:tsk_01KXQMC43959DB54N9CG1MJNQA -->
- [x] 5.2 完成 scope 與 artifact 回歸：檢視 git diff 只含 proposal Impact 列出的使用者文件、`openspec/config.yaml` 與本 change artifacts，確認套用前既有的其他工作區修改未被觸碰；執行 `speclink analyze unify-user-documentation --json` 無 Critical／Warning、`speclink validate unify-user-documentation` 通過，並人工逐項對照 user-documentation spec 八個 Requirement 與所有 Scenario 均有可觀察文件證據。 <!-- speclink-task:tsk_01KXQMC439ZE91KV0VGNEZXCFQ -->
