## 1. 基準

- [x] 1.1 建立回歸基準：改碼前跑 `cargo test -p speclink-core`、`cargo test -p speclink-core --test it render_golden::`、`cargo test -p speclink-cli --test it archive`、`cargo test -p speclink-cli --test it manual_task_gates`、`cargo test -p speclink-desktop-core` 確認全綠；基準本身若紅，先停下回報。驗證：上列指令 exit code 0。路徑：crates/speclink-core、crates/speclink-cli/tests/it、apps/desktop/core <!-- speclink-task:tsk_01M24GR699KEM3824VKWYAK2RT -->

## 2. 代勾搬進 archive()（design D1 代勾寫入落在計畫階段之後、提交階段的第一個效果）

- [x] 2.1 【Red】在 crates/speclink-core/src/archive.rs 測試模組寫 D1 代勾寫入落在計畫階段之後、提交階段的第一個效果的測試：帶 `mark_tasks_complete: true` 且 tasks 未全勾的 change，(a) 結構 validate 失敗（例如 proposal.md 缺 `## Why`）時 `archive()` 回 `Err` 含 `Validation failed` 且 tasks.md 逐位元不變、`artifact_writes` 為 0；(b) 同日撞名（先放一份同 dated_name 的封存）時回 `Err` 含 `already exists` 且 tasks.md 逐位元不變；(c) 全部守門通過時封存成功、封存後的 tasks.md 全部已勾（含 `* [ ]` 條列與 tab 分隔）。既有測試 `mark_tasks_complete_flag_passes_the_gate_without_pre_write` 改名為 `mark_tasks_complete_flag_checks_every_task_inside_archive`、斷言改為封存後 tasks.md 已全勾。驗證：`cargo test -p speclink-core archive::tests::mark_tasks_complete` 中 (a)(b)(c) 與改名的測試失敗。 <!-- speclink-task:tsk_01M24GR699DE4ZPV4ACDG1CMN0 -->
- [x] 2.2 【Green】把 `run_archive` 的代勾段（四條 `replace`）逐字搬進 `archive()`，位置在 `merge_refusal` 判定之後、snapshot 迴圈之前；`run_archive` 縮為 `resolve_change` → `host_workspace` → `archive()`（design D2 run_archive 縮為 resolve_change 加 archive()），刪除其中 `guard_linked_worktree`／`guard_meta`／`guard_open_tickets`／`merge_violations`＋`merge_refusal`／`guard_stale_stamps` 與代勾段。可觀察行為：command 層既有封存測試（壞 meta 兩值零寫入、未結工單拒絕零寫入、`change-archived` 事件）斷言不改全綠。驗證：`cargo test -p speclink-core archive::tests::mark_tasks_complete`、`cargo test -p speclink-core command::tests::archive` 全綠；`grep -n '"- \[ \] "' crates/speclink-core/src/command/mod.rs` 零命中。路徑：crates/speclink-core/src/archive.rs、crates/speclink-core/src/command/mod.rs <!-- speclink-task:tsk_01M24GR699QN1TXF8DMA9NF2VT -->

## 3. 守門縮回私有（design D3 guard 函式縮回 archive.rs 私有）

- [x] 3.1 依 D3 guard 函式縮回 archive.rs 私有：`guard_linked_worktree`、`guard_open_tickets`、`guard_stale_stamps`、`merge_refusal` 改為私有 `fn`，`merge_violations` 維持 `pub`；刪除三處「runtime 先喚一次、archive 內再守一次」的註解，改為一句守門只在此處。可觀察行為：無；validate.rs 與 drift.rs 仍經 `merge_violations` 共用過期判定。驗證：`cargo build -p speclink-core -p speclink-cli` 通過；`grep -n 'pub(crate) fn guard_\|pub(crate) fn merge_refusal' crates/speclink-core/src/archive.rs` 零命中；`grep -n 'guard_linked_worktree\|guard_open_tickets\|guard_stale_stamps\|merge_refusal' crates/speclink-core/src/command/mod.rs` 零命中。路徑：crates/speclink-core/src/archive.rs <!-- speclink-task:tsk_01M24GR699K2QFNRASMSQHVVK3 -->

## 4. bulk 改吃 skip_reason（design D4 bulk 改吃 skip_reason 唯讀投影、字串與優先序留在 bulk）

- [x] 4.1 【Red】在 archive.rs 測試模組寫 D4 bulk 改吃 skip_reason 唯讀投影、字串與優先序留在 bulk 的測試：過期 delta＋結構不合法＋任務未完成三條同時成立的 change，`skip_reason(store, &change, &opts)` 回 `MergeRefused`；`skip_specs: true` 時回 `StructuralInvalid`、再加 `no_validate: true` 時回 `TasksIncomplete { complete, total }`、三旗標齊帶時回 `None`；全就緒的 change 回 `None`；呼叫過程 `meta_writes` 與 `artifact_writes` 皆為 0。驗證：`cargo test -p speclink-core archive::tests::skip_reason` 失敗於函式不存在。（review 回合：原設計回 `Vec`、bulk 取 `.first()`，依 CLAUDE.md「不加沒被要求的彈性」改為 `Option` 短路。） <!-- speclink-task:tsk_01M24GR699230ZYSP0XB5M287X -->
- [x] 4.2 【Green】實作 `SkipReason` 與 `skip_reason`，任務條件抽成私有 `incomplete_tasks(store, name) -> Option<TaskShortfall>`（私有具名結構）供 `archive()` 的任務守門與 `skip_reason` 共用；bulk（crates/speclink-cli/src/verbs/lifecycle.rs 的 `cmd_archive_bulk`）改為以 `From<&ArchiveArgs>` 組一次 `ArchiveOptions`、對每個 change 取 `skip_reason(...)` 渲染既有三段字串（含 Purpose 點名段）後 `continue`，刪除自算的 `merge_violations`／`validate_change_structural`／`tasks::progress` 三段。可觀察行為：`speclink archive --all` 的 `Skipped:` 行、`archive would refuse`、`## Purpose` 點名、`validation failed`、`tasks incomplete (c/t)`、中止三段報告與結尾統計逐字不變。驗證：`cargo test -p speclink-core archive::tests::skip_reason` 全綠；`cargo test -p speclink-cli --test it archive_merge_gate`、`cargo test -p speclink-cli --test it archive_readiness_gate` 全綠；`grep -n 'merge_violations\|validate_change_structural\|tasks::progress' crates/speclink-cli/src/verbs/lifecycle.rs` 零命中。路徑：crates/speclink-core/src/archive.rs、crates/speclink-cli/src/verbs/lifecycle.rs <!-- speclink-task:tsk_01M24GR699PHDGPJR1YEZ7Y597 -->

## 5. 收尾（design D5 零行為變化的驗證面）

- [x] 5.1 依 D5 零行為變化的驗證面做全面驗收：`cargo test -p speclink-core`、`cargo test -p speclink-core --test it`（含 `render_golden::` 零 diff）、`cargo test -p speclink-cli --test it`（archive_readiness_gate、archive_merge_gate、manual_task_gates、archive_evidence_gate 四檔全綠且斷言未改）、`cargo test -p speclink-desktop-core`、`cargo test -p speclink-server archive` 全綠；`git diff` 確認除 `mark_tasks_complete_flag_passes_the_gate_without_pre_write` 一條改名改斷言外，既有測試斷言字串未改。驗證：上列指令 exit code 0。 <!-- speclink-task:tsk_01M24GR699AAMSR5KXJK4VK761 -->
- [x] 5.2 `cargo clippy -p speclink-core -p speclink-cli --all-targets -- -D warnings` 零警告（刪掉守門鏈後不留孤兒 import，例如 lifecycle.rs 的 `schema` 變數）；跑 `node --test scripts/*.test.mjs` 全綠。驗證：兩條指令 exit code 0。路徑：crates/speclink-core、crates/speclink-cli、scripts/ <!-- speclink-task:tsk_01M24GR699ACKXQ70XY60QQ45H -->
