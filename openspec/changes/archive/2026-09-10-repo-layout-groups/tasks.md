## 1. 路徑守門先行（RED）

- [x] 1.1 （design D6 「無殘留舊路徑」靜態守門；spec delivery-baseline Requirement「倉庫路徑守門」Scenario「殘留舊形 crate 路徑被抓出」「安裝腳本的根路徑不算舊形」「歷史記錄與 @trace 區塊不受檢」）在 scripts/delivery-gate.test.mjs 新增一條 test：遞迴走訪 repo 文字檔（排除 .git、node_modules、target、dist、.speclink、.dev、openspec/changes、openspec/discussions、CHANGELOG.md；跳過 `<!-- @trace` 到 `-->` 之間的行與帶 `path-guard:allow` 標記的行），斷言零命中舊形 `crates/speclink-<name>`（下一段不是 engine／host／store／protocol／adapters）與 `scripts/<已搬檔名>`（清單為五組全部檔名，不含 install.sh／install.ps1／install.test.mjs），命中時列出檔案與行號；(b) 的檔名清單自 `scripts/<group>/` 讀出，不寫死字面。正典規格逐字引用舊形路徑的三行（ADDED 需求與兩條 scenario）加 `path-guard:allow` 標記。驗證：`node --test scripts/delivery-gate.test.mjs` 在搬移前該條 RED 且輸出列出至少 README.md 與 .github/workflows/ci.yml 的命中行；其餘既有案例維持綠。 <!-- speclink-task:tsk_01M25AJARHYSM27BYRCH6N8MP5 -->

## 2. crates 五組

- [x] 2.1 （design D1 五組 crates 逐字對齊 §4）以 git mv 把 12 個 crate 目錄搬到 crates/engine/（speclink-core、speclink-fs）、crates/host/（speclink-host、speclink-server）、crates/store/（speclink-store、speclink-store-fs、speclink-store-sqlite、speclink-store-postgres）、crates/protocol/（speclink-protocol、speclink-remote）、crates/adapters/（speclink-cli、speclink-node）；改 Cargo.toml 的 workspace members 與 14 份 Cargo.toml 的 44 條 path 相依（同組 `../<name>`、跨組 `../../<group>/<name>`、apps/desktop 兩份 `../../../crates/<group>/<name>`）。驗證：`cargo build --workspace --locked` 綠；`git diff --exit-code Cargo.lock` 零差異；`git status` 顯示 12 個目錄為 rename；`cargo test -p speclink-core --test it render_golden` 綠（asset 與 include_str! 相對路徑不受影響）。 <!-- speclink-task:tsk_01M25AJARH5M3TZTM7BR6WKP6Z -->
- [x] 2.2 （design D1、D7 驗證與 CI）更新 crate 路徑的 CI 與部署引用：.github/workflows/ci.yml 的 docker smoke 改 `-f crates/host/speclink-server/Dockerfile`；node-sdk.yml 的 8 處 `crates/speclink-node` 改 `crates/adapters/speclink-node`；release.yml 的 Dockerfile `file:` 與三處註解；deploy/docker-compose.yml 與 docker-compose.postgres.yml 的 `dockerfile:`；crates/host/speclink-server/Dockerfile 的兩行註解；scripts/delivery-gate.test.mjs 讀 Dockerfile 的兩處 `read('crates/host/speclink-server/Dockerfile')`。驗證：`node --test scripts/delivery-gate.test.mjs` 除 1.1 那條外全綠（Dockerfile 與 workflow 斷言以新路徑通過）；本機有 docker 時 `docker build -f crates/host/speclink-server/Dockerfile .` 可建，否則以 ci.yml 的 docker smoke 於 PR 驗證。 <!-- speclink-task:tsk_01M25AJARHCYR9AFXA8HCK4YFY -->
- [x] 2.3 （design D1、D5 正典規格散文的路徑字串直接改寫）更新 crate 路徑的其餘引用：scripts（desktop-install.mjs 與 desktop-install.test.mjs 讀 init.rs、vocabulary-guard.test.mjs 的 SURFACE_DIRS、remote-docs.test.mjs 讀 app.rs／web.rs、npm-engine-package.mjs 註解）；package.json 的 test:all 三處 `npm --prefix crates/adapters/speclink-node`、.gitignore 的 `/crates/adapters/speclink-node/target/`、.dockerignore 首行註解、packages/ui/src/tasks.ts 與 packages/ui/src/__tests__/taskList.test.tsx 的同構註解、packages/ui/src/__tests__/discussionDrawer.test.tsx 兩處 readFileSync 讀 crates/engine/speclink-core/src/discuss.rs（搬完不改會紅）；程式碼註解與訊息字串（apps/server-web/src/pages/admin/AuditPage.tsx、crates/adapters/speclink-cli/tests/it/init_tools.rs、crates/engine/speclink-core/src/drift.rs、crates/engine/speclink-core/tests/it/render_golden.rs 的 bump 提示、crates/adapters/speclink-node/.cargo/config.toml、crates/host/speclink-server/src/assets.rs、crates/adapters/speclink-node/package.json 註解）；README.md 與 README.en.md 的 `cargo install --path crates/adapters/speclink-cli`；docs 11 檔散文；正典規格散文 25 處（archive-skill、baseline-skill、client-protocol、commit-skill、config-skill、delivery-baseline、discuss-skill、drift-computation、host-runtime、improve-skill、manual-skill、node-sdk-release、propose-skill、reference-server、store-abstraction、trace-skill、ui-copy-vocabulary、workflow-schemas）只換路徑字串、不動需求名／scenario 名／@trace 區塊。驗證：`npm test -w packages/ui` 綠（discussionDrawer 實讀 discuss.rs）；`speclink validate --specs` 全綠；`node --test scripts/docs/docs-links.test.mjs scripts/docs/docs-parity.test.mjs scripts/docs/vocabulary-guard.test.mjs scripts/docs/remote-docs.test.mjs` 綠（此時 docs 測試已在 3.1 搬入子目錄則用新路徑，否則用根路徑）；`grep -rn 'crates/speclink-' --include='*.md' openspec/specs | grep -v '^\s*- '` 的命中全部落在 @trace 區塊內（以 1.1 斷言的 @trace 跳過規則複核）。 <!-- speclink-task:tsk_01M25AJARHGE2W5X2B1J03FEBB -->
- [x] 2.4 （design D7）node crate 搬移後的 napi 建置與 SDK 測試：`npm --prefix crates/adapters/speclink-node run build` 成功產出 binding，`npm --prefix crates/adapters/speclink-node test` 綠；`cargo test -p speclink-desktop-core` 綠（apps/desktop/core 的 path 相依）。驗證：三個指令 exit code 0。 <!-- speclink-task:tsk_01M25AJARHVF4TXQ5RMKHPNMF7 -->

## 3. scripts 五組與 glob

- [x] 3.1 （design D2 五組 scripts，安裝腳本留根，測試跟腳本走）以 git mv 把腳本與同名測試搬入 scripts/release/、scripts/npm/、scripts/desktop/、scripts/docs/、scripts/dev/（清單如 proposal），install.sh／install.ps1／install.test.mjs 留根；改各檔的 `root` 推導為 `path.resolve(dirname, '../..')`、docs-screenshots.mjs 的 import 改 `../dev/cli.mjs`、desktop-install.mjs 呼叫 `scripts/desktop/desktop-sidecar.mjs`、四支以子行程起腳本的測試（homebrew-formula、release-latest-json、signing-gate）改新路徑、用法註解與錯誤訊息內的路徑（desktop-install、desktop-sidecar、docs-links、docs-screenshots、dev.mjs、dev.test.mjs、homebrew-formula、release-latest-json、release-notes-render、signing-gate）同步；delivery-gate.test 與 signing-gate.test 對 workflow 指令字串的斷言改新路徑。驗證：對每支搬移後的 .mjs 執行 `node --check`；`node scripts/dev/cli.mjs --version`（或其 `--help`）可跑；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 除 1.1 那條外全綠。 <!-- speclink-task:tsk_01M25AJARHDB158RV6BSQ9FA1D -->
- [x] 3.2 （design D2、D3 scripts 測試 glob 改兩個明確樣式；spec delivery-baseline Scenario「主 CI 以兩個明確樣式跑 scripts 測試面」）更新 scripts 路徑的呼叫端：package.json 的 dev／dev:server／dev:desktop／dev:reset／cli 五條；apps/desktop/package.json 的 predev；packages/server-npm/package.json 註解；.github/workflows/ci.yml（:95／:135 sidecar 改 scripts/desktop/desktop-sidecar.mjs；:69 測試步驟改 `node --test scripts/*.test.mjs scripts/*/*.test.mjs`，維持 shell: bash）；node-sdk.yml 的 npm-engine-package 路徑；release.yml 的 release-notes-render（含錯誤訊息）、signing-gate、desktop-sidecar、release-latest-json、release-notes、homebrew-formula、npm-server-package 與註解；.claude/skills/release/SKILL.md 四行；openspec/config.yaml 的 tasks 規則必跑指令改為兩樣式；delivery-gate.test 的「ci.yml 跑 scripts 測試面」斷言改認新字面。驗證：`npm run cli -- --version` 與 `npm run dev -- --help`（若 dev.mjs 無 --help 則 `node --check scripts/dev/dev.mjs`）可跑；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 除 1.1 那條外全綠；`speclink workflow-config show` 顯示新規則字面。 <!-- speclink-task:tsk_01M25AJARHBQSA2C7WGPD00HQQ -->

## 4. docs 與根目錄

- [x] 4.1 （design D4 docs/design 與 prompt.md）git mv docs/platform-architecture.zh-TW.md 與 docs/implementation-refactor-roadmap.zh-TW.md 至 docs/design/；git rm prompt.md；openspec/config.yaml 專案說明的藍圖路徑改 docs/design/platform-architecture.zh-TW.md；docs 內對這兩檔的相對連結逐一校正（兩檔互連同目錄不變，其他 docs 若有連結改 design/ 前綴）。驗證：`node --test scripts/docs/docs-links.test.mjs scripts/docs/docs-parity.test.mjs` 綠；`ls prompt.md` 不存在；`speclink instructions proposal --change repo-layout-groups --json` 的 context 含新路徑。 <!-- speclink-task:tsk_01M25AJARHG3EJAN2MN16780FS -->

## 5. 收尾（GREEN）

- [x] 5.1 （design D6、D7；spec delivery-baseline Requirement「倉庫路徑守門」）1.1 的守門斷言轉綠：`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 全綠（含守門條與 ci.yml glob 斷言）；`cargo build --workspace --locked` 綠且 `git diff --exit-code Cargo.lock` 零差異；`cargo test -p speclink-core --test it render_golden` 綠；`speclink validate --specs` 綠；`speclink validate repo-layout-groups` 綠；`git status` 中 crates 與 scripts 的搬移皆為 rename（相似度 100%）、無殘留空目錄。 <!-- speclink-task:tsk_01M25AJARHQMZ2J9B7AMJ1KX9Q -->
