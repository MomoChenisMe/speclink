## 1. 引擎搜尋（speclink-core）

- [x] 1.1 依 design「D1 搜尋演算法落在 speclink-core 的 discuss 模組，不加 Store 方法」與「D2 命中範圍限定 topic、slug 與四種決定行，以固定標記辨識」，在 crates/speclink-core/src/discuss.rs 新增 search 函式：以既有 list_live_discussions 與 list_archived_discussions 取得全文，對 topic、slug、各輪 `**Ruled out**:` 行與 Conclusion 區的 `**Decision**:`、`**Rejected alternatives**:`、`**Deferred**:` 行做不分大小寫子字串比對，多關鍵字任一命中即算；輪號取該行上方最近的 `### Round N` 標題；缺輪標題或 Conclusion 區的記錄仍以 topic 與 slug 參與比對；空關鍵字清單以錯誤中止。驗證：同檔 `#[cfg(test)]` 新增 topic 命中、slug 命中、ruled-out 命中帶正確輪號、conclusion 三種行命中、不分大小寫、多關鍵字 OR、Evidence 行不命中、缺區段不失敗、空關鍵字錯誤九個案例，`cargo test -p speclink-core --lib discuss` 綠。 <!-- speclink-task:tsk_01M1PE8J3J7J69J0RD2A7NXGHV -->
- [x] 1.2 依 design「D3 命中結果重用 DiscussionInfo，排序 topic／slug 命中優先」定義命中型別：每筆為既有 DiscussionInfo 欄位加 matches 陣列，每個 match 序列化為 `kind`、`where`、`text` 三個 camelCase 欄位（where 值為 frontmatter、round-N 或 conclusion）；排序為 topic 或 slug 命中者排前，兩群內各依 created 由新到舊、同日依 slug 字典序，同一記錄的 matches 依文件順序（crates/speclink-core/src/discuss.rs）。驗證：同檔單元測試以四筆記錄（topic、slug、conclusion、round 各一，created 交錯）斷言輸出順位與 spec 的排序表一致，並斷言 serde_json 序列化的鍵名為 kind、where、text；`cargo test -p speclink-core --lib discuss` 綠。 <!-- speclink-task:tsk_01M1PE8J3JZDHHZBVZM6K6Y6ET -->
- [x] 1.3 依 design「D4 命令層新增 DiscussSearch，server 端點以單一 q 參數空白切詞」的命令層部分，在 crates/speclink-core/src/command/mod.rs 新增 DiscussSearch 命令與對應結果，滿足 spec「動詞覆蓋與跨入口一致性」：列入查詢動詞覆蓋、不發領域事件、既有 DiscussList 與 DiscussShow 的結果與事件行為不變。驗證：command 模組既有測試加一案例斷言執行 DiscussSearch 後領域事件為空且回傳命中清單；`cargo test -p speclink-core --lib command` 綠。 <!-- speclink-task:tsk_01M1PE8J3JFNJAYA5A1ZE0ZDNT -->

## 2. CLI 子指令（speclink-cli）

- [x] 2.1 依 design「D5 CLI 子指令形狀與輸出」在 crates/speclink-cli/src/verbs/discuss.rs 新增 `speclink discuss search <關鍵字>... [--json] [--no-color]`，本機模式經命令層執行，滿足 spec「討論定案以 search 動詞可查」：人眼零命中印 `No discussions match "<關鍵字>".`、有命中以 `Discussions matching "<關鍵字>":` 起頭、每筆一行加縮排 match 行、--no-color 無 ANSI；--json 輸出 `{ "hits": [...] }`；成功與零命中 exit 0；未帶關鍵字由 clap 拒絕、非零 exit、stderr 印用法、stdout 無輸出。驗證：新增 crates/speclink-cli/tests/it/discuss_search.rs（並登錄至 crates/speclink-cli/tests/it/main.rs），案例涵蓋人眼有命中格式、--no-color 無 ANSI、--json payload（hits 陣列、每筆含 slug／topic／status／rounds／created／path／archived 與 matches，match 含 kind／where／text）、零命中人眼與 --json、未帶關鍵字非零 exit；`cargo test -p speclink-cli --test it discuss_search` 綠。 <!-- speclink-task:tsk_01M1PE8J3J9QVA381B4GYRJRZ7 -->
- [x] 2.2 既有 discuss list、discuss list --archived 與 discuss show 的人眼與 --json 輸出逐位元不變（crates/speclink-cli/src/verbs/discuss.rs 不動既有 renderer）。驗證：`cargo test -p speclink-cli --test it discuss` 既有 discuss_slug、discuss_kind、discuss_seal、discuss_content_guard、discuss_conclude_auto_archive、discuss_promote_snapshot 全綠。 <!-- speclink-task:tsk_01M1PE8J3J7XVBX5W9EQDSKMWQ -->

## 3. Protocol、server 與 remote

- [x] 3.1 在 crates/speclink-protocol/src/query.rs 新增搜尋回應型別，滿足 spec「討論搜尋回應 payload」：頂層 hits 陣列，每筆含與討論資訊 payload 相同欄位（含 promotedTo、選填 concluded）加 matches 陣列（kind、where、text 字串欄位），camelCase 與 JsonSchema 派生。驗證：同檔 `#[cfg(test)]` 加兩案例——完整 JSON 反序列化欄位值一致、缺 createdBy／kind／concluded 鍵且 promotedTo 空陣列時仍成功；`cargo test -p speclink-protocol` 綠。 <!-- speclink-task:tsk_01M1PE8J3JGRSFR6TS2AA5MP4J -->
- [x] 3.2 依 design「D4 命令層新增 DiscussSearch，server 端點以單一 q 參數空白切詞」的 server 部分與「D7 不重用桌面全域搜尋端點」，在 crates/speclink-server/src/app.rs 註冊 GET /discussions/search、crates/speclink-server/src/routes.rs 實作 handler（q 空白切詞後執行同一命令；q 缺席或全空白回 400 invalid_argument；reader role 可呼叫），滿足 spec「討論定案搜尋端點」，既有 GET /search 不動。驗證：crates/speclink-server/tests/it/discussion_routes.rs 新增在途與封存各一命中且順序正確、q 缺席 400、q 全空白 400、reader role 200 四案例，既有 /search 測試不變；`cargo test -p speclink-server --test it discussion_routes` 綠。 <!-- speclink-task:tsk_01M1PE8J3JCDPDG5YZTDFRDNV9 -->
- [x] 3.3 在 crates/speclink-remote/src/client.rs 新增 typed 搜尋方法，以關鍵字空白接起放入 q 呼叫 GET /discussions/search，回應反序列化為 3.1 的型別，不走 raw JSON。驗證：crates/speclink-remote/tests/it/typed_client.rs 新增案例斷言請求路徑與 q 值為 `golden sse`、回應為 typed 型別且欄位值一致；`cargo test -p speclink-remote --test it typed_client` 綠。 <!-- speclink-task:tsk_01M1PE8J3J66XZ88R184Y4ESGZ -->
- [x] 3.4 在 crates/speclink-cli/src/verbs/discuss.rs 的 remote 分支接上 3.3 的方法，以既有 to_discussion_info 轉回引擎型別後共用 2.1 的 renderer，使 remote 模式的人眼與 --json 與本機同形（path 缺席與 promotedTo、concluded 增欄沿用 list 既定分歧）。驗證：crates/speclink-cli/tests/it/discuss_search.rs 沿 remote_verb_parity.rs 的 mock server 模式加一案例，斷言 remote 模式 --json 的 hits 順序、slug 與 matches 與本機相同；`cargo test -p speclink-cli --test it discuss_search` 綠。 <!-- speclink-task:tsk_01M1PE8J3J7Y55HP9RP3FJ6EPQ -->

## 4. 技能文字與 asset 三連動

- [x] 4.1 依 design「D6 技能文字：discuss 偵察加舊討論查核，improve 防重提改用 search」改寫 crates/speclink-core/assets/skills/discuss.md，滿足 spec「開場舊討論查核與第四類對照」：Step 2 漏斗改為「正典 → 舊討論查核 → 程式碼」，舊討論查核指示以題目關鍵字加正典轉譯英文詞執行 `speclink discuss search ... --json`、命中決定行全列、整份 Conclusion 以 `speclink discuss show` 最多讀 3 份且 topic 命中優先、不依 kind 過濾；Presenting assumptions 的對照表加第四列「舊討論已定案」（曾否決附理由且重開須說明理由已失效、曾延後、已落地）並重申不得擋下方向；Context 規定加 `Prior discussions: <slug 清單>`、零命中寫 none。驗證：內容審閱逐條對照 spec 三個 scenario 的 THEN；4.3 的 golden 測試綠。 <!-- speclink-task:tsk_01M1PE8J3JANE3A785J4292JKB -->
- [x] 4.2 改寫 crates/speclink-core/assets/skills/improve.md 的 Step 1，滿足 spec「improve 技能以六步骨架渲染至兩工具」：以 `speclink discuss search <範圍關鍵字>... --json` 加 `speclink list --json` 取代 `speclink discuss list --archived --json` 後逐筆 show，並規定同範圍的舊 improve 記錄（kind 為 improve）排前閱讀；六步骨架、僅使用者發起與不得實作的限定不變。驗證：內容審閱確認技能檔不再含 `discuss list --archived` 指示、含 search 指示與 improve 記錄排前規則；4.3 的 golden 測試綠。 <!-- speclink-task:tsk_01M1PE8J3JMHJNNCJWTFJD6FSG -->
- [x] 4.3 crates/speclink-core/src/init.rs 的 ASSET_VERSION 由 v1.27.0 升至 v1.29.0，再生 crates/speclink-core/tests/golden/ 下五份快照（claude、claude-worktree、codex、neutral-cli、neutral-tool-call）與 assets.lock，使渲染產物含 4.1 與 4.2 的新內容。驗證：`cargo test -p speclink-core --test it render_golden` 綠；在本 repo 執行 `speclink update` 後以 git status 盤點再生的 .claude/skills 與 .codex 技能檔（32 份 SKILL.md）一併納入同批 commit。 <!-- speclink-task:tsk_01M1PE8J3J37S7SVDHVXGVK450 -->

## 5. 收尾

- [x] 5.1 端到端確認：在本 repo 執行 `speclink discuss search drawer` 與 `speclink discuss search drawer --json`，人眼輸出列出 spec-archive-drawer-ux 等封存記錄的 ruled-out 命中行且 exit 0，--json 的 hits 每筆含 matches；`speclink validate discuss-search-recall` 通過；`cargo test -p speclink-core`、`cargo test -p speclink-cli --test it discuss_search`、`node --test scripts/*.test.mjs` 全綠。 <!-- speclink-task:tsk_01M1PE8J3J43RPD6VSRYCBHJ9K -->
