> 本 change 零程式碼邏輯、零 delta：只改字。判準見 design D1（指 `openspec/specs/` 合併後的規格集合才改成「正式規格」，別義的「正典」不動），標題改名清單見 design D3，封存區一律不動（design D4）。每個批次做完立刻跑該批的驗證，不累積到最後。grep 一律排除 openspec/changes/archive、openspec/discussions/archive、node_modules、target、dist。

## 1. 詞條與使用者可見面（design D2、D7）

- [x] 1.1 `openspec/LANGUAGE.md` 詞彙末尾新增「正式規格」詞條，字面依 design D2（definition 點名對照詞「delta 規格」；avoid 為「正典規格、正典 spec、正典 specs、規格正典、正典（指這個規格集合時）」；why 含 2026-09-12 裁定與不改的別義）。驗證（TDD 紅燈）：`speclink language show` 列出該詞條；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 的詞彙守門轉紅，命中清單恰為 packages/ui/src/i18n.tsx 兩行、README.md 四行與 docs 十份 markdown 的複合寫法，沒有任何光桿「正典」被命中（證明語境限定生效） <!-- speclink-task:tsk_01M2B1ZH5YD5N3G1RAY7HQW9VW -->
- [x] 1.2 桌面 zh-TW 規格頁標題顯示「正式規格」、空狀態顯示「此專案尚無正式規格」；英文版 "Canonical specs" 不變。先改 packages/ui/src/__tests__/specList.test.tsx 的斷言為「此專案尚無正式規格」使 `npm test -w packages/ui` 轉紅，再改 packages/ui/src/i18n.tsx 的 specs.heading 與 specs.empty 兩條轉綠。驗證：`npm test -w packages/ui` 綠；grep packages/ui/src/i18n.tsx「正典」零命中 <!-- speclink-task:tsk_01M2B1ZH5YYV6RTR04QXRMYTM1 -->
- [x] 1.3 README.md 與 docs 十份（docs/getting-started.zh-TW.md、docs/workflow.zh-TW.md、docs/product-status.zh-TW.md、docs/roadmap.zh-TW.md、docs/sdk-node.zh-TW.md、docs/verb-contract.zh-TW.md、docs/configuration.zh-TW.md、docs/server-deployment.zh-TW.md、docs/design/implementation-refactor-roadmap.zh-TW.md、docs/design/platform-architecture.zh-TW.md）的複合寫法全改，指 specs 這一側的光桿「正典」依 design D1 的 docs 處置逐句改（README「共用同一份規格正典」→「共用同一份正式規格」；platform-architecture 只改文件狀態欄直接指規格檔的那一句，「遠端正典」不動）；句子保持通順、不重寫段落。驗證：`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 全綠（詞彙守門歸零、中英 H2 序列與截圖集合不變）；grep README.md 與 docs/「正典規格」「正典 spec」「規格正典」零命中 <!-- speclink-task:tsk_01M2B1ZH5YCJV1SNW9S4XC76Z8 -->

## 2. 正式規格散文直編（design D1、D3）

- [x] 2.1 批次一、delta 對照最密集的四份全改：openspec/specs/archive-merge/spec.md、openspec/specs/spec-validation/spec.md、openspec/specs/capability-naming-guard/spec.md、openspec/specs/change-analysis/spec.md——複合寫法與全部光桿「正典」改「正式規格」，含 design D3 表列的七個標題（validate --specs 驗證正式規格、snapshot 先於正式規格寫入、既有正式規格 Purpose 不受 delta 影響、MODIFIED 需求不在正式規格、無正式規格時每 capability 一筆、命中正式規格名稱照常放行，以及 archive-merge 內文）；每條需求的 SHALL 與 scenario 的 WHEN／THEN 結構不變、需求數與 scenario 數不變。驗證：`speclink validate --specs --strict` 綠；grep 這四份「正典」零命中；git diff 只有名詞替換、無結構行增減 <!-- speclink-task:tsk_01M2B1ZH5YR8ME8V3GMS93C9VT -->
- [x] 2.2 批次二、其餘全改的八份：openspec/specs/desktop-manual-page/spec.md、openspec/specs/manual-pages/spec.md、openspec/specs/manual-skill/spec.md、openspec/specs/context-projection/spec.md（含標題「投影含正式規格與 delta specs」）、openspec/specs/verify-evidence/spec.md、openspec/specs/drift-computation/spec.md、openspec/specs/change-lifecycle/spec.md、openspec/specs/task-identity/spec.md——複合寫法與光桿全改。驗證：`speclink validate --specs --strict` 綠；grep 這八份「正典」零命中；需求數與 scenario 數不變 <!-- speclink-task:tsk_01M2B1ZH5Y0KCS9WZB7DC53J4N -->
- [x] 2.3 批次三、部分改的三份依 design D1 逐句：openspec/specs/desktop-app/spec.md（兩個標題「選定 spec 以抽屜顯示其正式規格內容」「展開卡片顯示正式規格全文」與正典內容／正典全文／正典 Purpose 改；五處「正典詞」不動）、openspec/specs/user-documentation/spec.md（「遠端的正典在 Store」「讀者不需翻正典」「與正典一致」「server-identity 正典」改；「使用流程正典」「能力狀態正典」「正典詞彙」「工作流正典」「安全邊界的正典」不動）、openspec/specs/discuss-skill/spec.md（四個標題「正式規格先行的漏斗偵察」「正式規格接地與三分對照」「需求與正式規格衝突時列為假設」「正式規格零命中時流程照舊」與內文改；「正典詞彙轉譯」不動）。驗證：`speclink validate --specs --strict` 綠；grep 這三份剩餘「正典」逐行屬 D1 不改清單（desktop-app 五行、user-documentation 六行、discuss-skill 一行） <!-- speclink-task:tsk_01M2B1ZH5YY8MY995W2NG71DKC -->
- [x] 2.4 批次四、只改複合寫法的十份：openspec/specs/delivery-baseline/spec.md、openspec/specs/phase2-acceptance/spec.md、openspec/specs/remote-workspace-data/spec.md、openspec/specs/propose-skill/spec.md、openspec/specs/archive-skill/spec.md、openspec/specs/server-context-api/spec.md、openspec/specs/server-read-api/spec.md（兩個標題「正式規格內文可讀」「讀取存在的正式規格」）、openspec/specs/trace-skill/spec.md、openspec/specs/trace-verb/spec.md、openspec/specs/verb-contract/spec.md；這十份的光桿「正典」（路徑慣例、server 查詢結果、資料面、正典化）一字不動。驗證：`speclink validate --specs --strict` 綠；grep 這十份「正典規格」「正典 spec」零命中；design D1「完全不動」清單的 15 份規格 git diff 為空 <!-- speclink-task:tsk_01M2B1ZH5YWYBN8RVKG2XPQ5TS -->

## 3. 手冊直接改字（design D5）

- [x] 3.1 openspec/manual/ 十六頁（about、analyze、archive、baseline、data-layout、desktop-browse、desktop-manual、discuss、drift-ingest、index、manual、migrate、propose、remote-context、remote-overview、trace）依 design D1 同一判準直接改字，含兩個段落標題「delta 能合併進正式規格」（archive 頁）與「正式規格是證據，不是裁決」（discuss 頁）；frontmatter 只改 archive 頁 keywords 裡的舊詞「正典規格」（它就是要改名的詞本身），其餘五欄與 `generated` 一字不動；policy-config 與 desktop-quality 兩頁不動。驗證：grep openspec/manual/「正典規格」零命中；剩餘「正典」只在 policy-config「正典內容」、desktop-quality「正典詞」、about 的「正典檔」「工作流正典」「正典詞彙」；git diff 的 frontmatter 變動只有那一行 keywords <!-- speclink-task:tsk_01M2B1ZH5Y77BYESA3F32P4KGT -->

## 4. 程式碼註解與測試字串（design D6）

- [x] 4.1 crates 十六檔只改兩類文字——複合寫法（正典規格／正典 spec(s)／正典 spec.md）與引用 design D3 改名標題的註解：crates/engine/speclink-core/src/lifecycle/capname.rs、crates/engine/speclink-core/src/lifecycle/model.rs、crates/engine/speclink-core/src/lifecycle/trace.rs、crates/engine/speclink-core/src/lifecycle/newcmd.rs（「命中正式規格名稱照常放行」）、crates/engine/speclink-core/src/lifecycle/archive/tests.rs（兩個標題引用）、crates/engine/speclink-core/src/command/tests.rs、crates/engine/speclink-core/src/quality/validate.rs、crates/engine/speclink-core/src/quality/validate/tests.rs、crates/engine/speclink-core/src/quality/analyzer/tests.rs（兩個標題引用）、crates/adapters/speclink-cli/src/verbs/checks.rs、crates/adapters/speclink-cli/tests/it/manual_pages_dir_ignored.rs、crates/adapters/speclink-cli/tests/it/new_artifact.rs、crates/adapters/speclink-cli/tests/it/remote_read_path.rs、crates/adapters/speclink-cli/tests/it/trace.rs、crates/adapters/speclink-cli/tests/it/validate_specs.rs、crates/host/speclink-server/tests/it/phase2_chain.rs；光桿「正典」註解不動（審查站點名的同句混用除外，一併改齊）、無任何識別符或邏輯變動。驗證：`cargo test -p speclink-core --test it render_golden::` 綠（golden 未變）；`cargo test -p speclink-core --lib` 綠；`cargo test -p speclink-cli --test it -- validate_specs new_artifact trace manual_pages_dir_ignored remote_read_path` 綠；grep 這十六檔「正典規格」「正典 spec」零命中 <!-- speclink-task:tsk_01M2B1ZH5Y1KM1AF0HP088DPYC -->
- [x] 4.2 apps 與 packages 十三檔同 D6 兩類：apps/desktop/core/src/manual.rs、apps/desktop/core/src/query.rs、apps/desktop/src-tauri/src/lib.rs、apps/desktop/src-tauri/src/remote.rs、apps/desktop/src-tauri/tests/it/remote_data.rs、apps/desktop/src/__tests__/App.test.tsx（「選定 spec 以抽屜顯示其正式規格內容」）、packages/ui/src/adapter.ts、packages/ui/src/components/SpecDrawer.tsx、packages/ui/src/components/SpecList.tsx、packages/ui/src/trace.ts、packages/ui/src/__tests__/delta.test.ts、packages/ui/src/__tests__/richDrawer.test.tsx、packages/ui/src/__tests__/trace.test.ts，以及 scripts/release/delivery-gate.test.mjs 的一行註解。驗證：`cargo test -p speclink-desktop-core` 綠；`cargo check -p speclink-desktop --tests` 通過（src-tauri 只動註解，不跑需 sidecar 的整合測試）；`npm test -w packages/ui` 綠；`npm test -w apps/desktop` 綠；grep 這十四檔「正典規格」「正典 spec」零命中 <!-- speclink-task:tsk_01M2B1ZH5Y9Z9DYG3SXG7HYHKG -->

## 5. 驗收（design D7）

- [x] [M] 5.1 使用者抽審改動最多的兩份規格 openspec/specs/archive-merge/spec.md 與 openspec/specs/discuss-skill/spec.md、手冊 openspec/manual/archive.md、以及 README.md 改寫的五句；讀起來不通順或判準用錯的句子提出後逐句修正，修正後重跑該批驗證。抽審通過即驗收完成 <!-- speclink-task:tsk_01M2B1ZH5YS9Q64SPZGKQTBVCH -->
- [x] 5.2 全量驗收五道全綠：`node --test scripts/*.test.mjs scripts/*/*.test.mjs`（詞彙守門與 docs 對等）；`speclink validate --specs --strict`；`npm test -w packages/ui`；`cargo test -p speclink-core --test it render_golden::`；活的檔案 grep「正典規格」「正典 spec」「規格正典」零命中，剩餘「正典」命中逐一對照 design D1 不改清單並把對照結果列在 PR 說明。封存區、CHANGELOG.md、apps/desktop/src/release-notes/release-notes.json、openspec/config.yaml、技能資產 git diff 為空（design D4） <!-- speclink-task:tsk_01M2B1ZH5YKSNZSQY0GS9EEPED -->
