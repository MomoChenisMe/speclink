## 1. 政策欄位（config worktree）

- [x] 1.1 worktree 政策欄位進入四層解析（對應需求：工作流政策的正典歸屬與四層解析順序）：`crates/speclink-core/src/config.rs` 的政策欄位結構與環境覆寫增加 worktree（可空布林，內建預設 false；環境變數 SPECLINK_WORKTREE 沿用既有布林解析，非法值落到下一層；.speclink.yaml 的 worktree 鍵不生效且無 deprecation 警告——不進舊鍵相容清單）。先以 #[cfg(test)] 落測試再實作：涵蓋「config.yaml 值生效」「SPECLINK_WORKTREE 覆寫」「非法環境值落下一層」「.speclink.yaml 鍵不生效」四情境。驗證：cargo test -p speclink-core 通過。 <!-- speclink-task:tsk_01KZ5KCS34TEYAR0BF5QYCHH6M -->
- [x] 1.2 workflow-config 動詞受理 worktree 鍵（對應需求：工作流政策的正典歸屬與四層解析順序）：`crates/speclink-cli/src/commands.rs` 的正典鍵清單增 worktree，speclink workflow-config set worktree true|false 寫入 openspec/config.yaml（走既有 yaml_scalar 跳脫路徑），非法值以非零 exit code 失敗且檔案不動；workflow-config show 人眼輸出與 --json 呈現 worktree 欄位（camelCase，布林）。先落 CLI 整合測試（crates/speclink-cli/tests/ 既有 workflow-config 測試檔）再實作：斷言 set→show 往返、--json payload 含 "worktree": true、非法值報錯。驗證：cargo test -p speclink-cli 對應測試通過，既有 workflow-config 測試不因新欄位而破（show 輸出屬刻意變更者同批更新斷言）。 <!-- speclink-task:tsk_01KZ5KCS34PWH6N6NYQ0183N5C -->

## 2. 引擎聚合讀（worktree overlay）

- [x] 2.1 host worktree discovery（對應需求：worktree discovery 與映射慣例）：新增 `crates/speclink-host/src/worktree.rs`（`crates/speclink-host/src/lib.rs` 掛模組），對 git worktree list --porcelain 輸出解析出 linked worktree 的絕對路徑與分支，依三條件（分支 speclink/<change名>、同名未封存 change 存在、worktree 內 openspec/changes/<change名>/ 可讀）產出 change→{path, branch} 映射；detached HEAD 與 prunable 條目略過；git 失敗回空映射不報錯。先以 #[cfg(test)] 落 porcelain 解析與條件判定測試（正常、detached、prunable、不合慣例、change 缺席）再實作。驗證：cargo test -p speclink-host 通過。 <!-- speclink-task:tsk_01KZ5KCS34EDBFYX2V5X0A7EM2 -->
- [x] 2.2 host Store 裝飾器（對應需求：worktree discovery 與映射慣例；list 的 worktree 觀察面輸出）：`crates/speclink-host/src/worktree.rs` 增加包住既有 Store 的讀取重導向裝飾器——映射中 change 的 read_artifact／read_change_meta／find_change／updated_at_secs 改讀 worktree 副本，未映射 change 與所有寫入原樣透傳主副本。先落單元測試（重導向命中、未映射透傳、寫入落主副本）再實作。驗證：cargo test -p speclink-host 通過。 <!-- speclink-task:tsk_01KZ5KCS34HDSJK40R92GWB93F -->
- [x] 2.3 core listing 的 facts 變體與 worktree 欄位（對應需求：list 的 worktree 觀察面輸出）：`crates/speclink-core/src/listing.rs` 的 list JSON 條目增可空 worktree 物件欄位（path、branch，缺席不序列化），並提供帶 facts 的組裝變體；既有組裝函式簽名與行為不動。先落測試再實作：斷言「facts 空表時變體輸出與既有組裝位元級相同」與「有 facts 時該條目含 camelCase 的 path／branch 欄位」。驗證：cargo test -p speclink-core 通過，server 與 remote parity 呼叫端零改動仍編譯通過。 <!-- speclink-task:tsk_01KZ5KCS34D4T9EFTQMDJBFYC8 -->
- [x] 2.4 CLI list 組裝與人眼標示（對應需求：worktree discovery 與映射慣例；list 的 worktree 觀察面輸出）：`crates/speclink-cli/src/commands.rs` 的 speclink list——僅 local 主 checkout（workspace 根 .git 為目錄）且有效 worktree 政策為 true 時執行 discovery、以裝飾器包 Store、facts 進組裝變體；映射成立的 change 人眼行尾追加固定字面「 [worktree]」（--no-color 同字面）；政策關閉或非主 checkout 時完全不 spawn git、輸出位元級不變。驗證：任務 2.6 的整合測試涵蓋，且既有 list 相關 CLI 測試全數通過。 <!-- speclink-task:tsk_01KZ5KCS34JQD2WYC41R23EXDS -->
- [x] 2.5 位元級回歸釘子（對應需求：list 的 worktree 觀察面輸出）：list 的人眼輸出由 CLI 直接 println 產出，`crates/speclink-core/tests/golden` 僅收技能渲染快照，故無 list 人眼 fixture 家族可加。改以三顆既有測試釘死位元級相容：core 的「facts 空表輸出與既有組裝位元級相同」、CLI 的「政策關閉時人眼與 --json 皆與無 worktree 情境逐位元一致」、CLI 的「標示字面 [worktree] 釘死且 --no-color 同字面」。既有技能渲染 golden 全數不動。驗證：cargo test -p speclink-core --test it render_golden:: 通過（既有 golden 未動）＋ cargo test -p speclink-cli --test it worktree_overlay 通過。 <!-- speclink-task:tsk_01KZ5KCS34JZ495PJ0EYCXCHBA -->
- [x] 2.6 CLI 端到端整合測試（對應需求：worktree discovery 與映射慣例；list 的 worktree 觀察面輸出）：crates/speclink-cli/tests/ 新增 worktree overlay 測試（真實 git repo ＋ git worktree add）——情境：映射成立時 --json 含 worktree 欄位且 completedTasks 反映 worktree 副本勾選、人眼含「 [worktree]」、git worktree remove 後輸出還原、政策關閉時位元級不變、於 linked worktree 內執行不套用、分支不合慣例略過。路徑斷言不得假設分隔符（Windows 相容，tempdir 路徑先正規化）。驗證：cargo test -p speclink-cli 對應測試於 macOS 本機通過。 <!-- speclink-task:tsk_01KZ5KCS346T3P44ZGACGTT9KS -->
- [x] 2.7 契約文件同步（對應需求：動詞契約的涵蓋面與 payload 形狀）：docs/verb-contract.md 增列 list --json 的可空 worktree 欄位（僅 fs 模式主 checkout 且政策開啟時出現、remote 恆缺席）。驗證：內容審閱——欄位名、型別、出現條件與 spec delta 一致。 <!-- speclink-task:tsk_01KZ5KCS34R2J52XVXKDGK8H1W -->

## 3. 技能（apply-with-worktree ＋ worktree-merge）

- [x] 3.1 apply-with-worktree 模板資產（對應需求：apply-with-worktree 技能的前置指示；apply-with-worktree 技能的收尾指示）：新增 `crates/speclink-core/assets/skills/apply-worktree-pre.md`（讀取有效 worktree 政策、非 true 即拒跑並指示 workflow-config set worktree true、確認 change 存在未封存、以分支 speclink/<change名> 於 sibling 巢 <repo資料夾名>.worktrees/<change名>/ 建立或沿用 worktree、印建置成本提示、其後流程於 worktree 內執行）與 `crates/speclink-core/assets/skills/apply-worktree-post.md`（於 worktree 內完成 commit、不合併不移除 worktree、點名 worktree-merge 技能交棒）。驗證：內容審閱對照 worktree-apply-skill spec 的三需求，字面含指定指示。 <!-- speclink-task:tsk_01KZ5KCS345J62GW8SS5VD8BTQ -->
- [x] 3.2 worktree-merge 模板資產（對應需求：worktree-merge 技能的收尾流程指示）：新增 `crates/speclink-core/assets/skills/worktree-merge.md` 獨立完整模板——preflight（主樹乾淨、分支已提交，缺一即停且不代 stash 不代提交）、合併回主分支、衝突即停（回報衝突檔案、不代編、中止合併後回報）、成功後移除 worktree 刪除分支、提示續走品質站或封存。驗證：內容審閱對照 worktree-merge-skill spec 的兩需求。 <!-- speclink-task:tsk_01KZ5KCS3412A0JGTPT91CP2WM -->
- [x] 3.3 registry 組合與生成（對應需求：apply-with-worktree 技能的生成與組合；worktree-merge 技能的生成）：`crates/speclink-core/src/skills.rs`——以 concat!(include_str!) 編譯期拼出 apply-with-worktree 的 body（pre ＋ 既有 apply.md ＋ post，維持靜態字串型別），registry 增兩條目（apply-with-worktree：fork false、disallow_edit false、for_codex true；worktree-merge：fork false、disallow_edit true、for_codex true），skill_body 查表同步。先落測試再實作：斷言 claude 再生後 .claude/skills/speclink-apply-with-worktree/SKILL.md 與 .claude/skills/speclink-worktree-merge/SKILL.md 存在、前者完整包含 apply 本體段落、既有 speclink-apply 渲染輸出位元級不變；兩新技能渲染輸出釘 golden。驗證：cargo test -p speclink-core 通過（含 render_golden::）。 <!-- speclink-task:tsk_01KZ5KCS345RZEMH88NB5FQ94R -->
- [x] 3.4 注入區塊清單：`crates/speclink-core/src/init.rs` 的 CLAUDE.md／AGENTS.md 注入區塊「Use /speclink-* skills when:」清單增兩條（並行實作 change → /speclink-apply-with-worktree；worktree 完工收尾 → /speclink-worktree-merge），版本 marker 隨既有機制遞增；於本 repo 執行技能再生使 CLAUDE.md 與 AGENTS.md 實際更新。驗證：注入區塊相關的既有測試同批更新且通過；再生後本 repo CLAUDE.md 可見兩條目。 <!-- speclink-task:tsk_01KZ5KCS34533K8E1RWE7TWQ0N -->

## 4. 詞彙

- [x] 4.1 LANGUAGE.md 明文例外：openspec/LANGUAGE.md 原則區新增「worktree 直出」例外條目——適用範圍（worktree 流程的使用者可見文案，含後續 desktop 卡片標示與抽屜資訊）、理由與先例（config.yaml 頁簽、討論 slug）、裁定出處（討論 worktree-parallel-apply）與日期。驗證：speclink language show 呈現該條目，內容審閱與討論結論一致。 <!-- speclink-task:tsk_01KZ5KCS34R43VM5XFPB5AZCW9 -->
