## 1. meta 欄位與階段判定（D2、D7）

- [x] 1.1 紅燈：在 crates/engine/speclink-core/src/lifecycle/model.rs 的測試模組新增「meta 新欄位向後相容」測試——含 `depends_on: a, b` 的 meta 解析出 `depends_on()` 為 [a, b]、缺席為空清單；在 crates/engine/speclink-core/src/lifecycle/listing.rs 沿 `list_json_payload_is_unchanged_by_board_rank` 家族新增 `list_json_payload_is_unchanged_by_depends_on`。驗證：`cargo test -p speclink-core` 兩測試先紅。 <!-- speclink-task:tsk_01M2J1KTEQMYYCC839R51208GB -->
- [x] 1.2 綠燈：`ChangeMeta` 新增 `#[serde(default)] depends_on: Option<String>` 與 `depends_on()` 讀取器（與 `restale_from()` 同形），新增 `model::stage(store, &Change) -> Stage`（Ready／InProgress／Proposed，規則與桌面 core 的 change_stage 一致）並補三態測試（D7）。驗證：`cargo test -p speclink-core` 全綠，list 與 status 的既有 golden 不變。 <!-- speclink-task:tsk_01M2J1KTEQ7BAT844JDACKNGS7 -->

## 2. plan 演算法（D1、D5）

- [x] 2.1 紅燈：新增 crates/engine/speclink-core/src/lifecycle/plan.rs 的測試模組，以 TestStore 夾具覆蓋「執行順序的基底與拓樸修正」：基底三鍵（階段 → 有 rank 字典序在前、缺 rank 置後且 created 升冪、缺 created 殿後 → 名稱決斷）、宣告依賴推後波次、重疊互斥依基底順序定先後、指向已封存視為滿足、已開工者排在未開工者之前、規格 Example 的五 change 表（waves 為 [[r1, p2, n5], [n3], [n4]]、next 為 n5）、成環回 `PlanError::Cycle` 並列出環、壞 meta 列入 skipped。驗證：`cargo test -p speclink-core plan::` 先紅。 <!-- speclink-task:tsk_01M2J1KTEQQKTJMS0AC45GVSTW -->
- [x] 2.2 綠燈：實作 `plan::compute` 與其內部 `compute_with_ranks(&dyn Store, &BTreeMap<String, String>)`（compute 以 meta 的 board_rank 當 ranks；補一測試以外部 ranks 覆蓋 meta 值），`Plan { waves, changes, next, skipped }` 以 serde camelCase 序列化（`dependsOn`、`blockedBy`、`overlaps[].capabilities` 升冪），於 crates/engine/speclink-core/src/lifecycle/mod.rs 註冊模組並自 lib 匯出。驗證：2.1 全綠；序列化測試斷言 JSON 鍵名為 camelCase 且 `skipped` 空時為 `[]`。 <!-- speclink-task:tsk_01M2J1KTEQPYC2XMC90HMNJ8A0 -->
- [x] 2.3 紅燈→綠燈：「rank 移動的依賴檢查」——測試「拖到宣告前置之前被拒（must_follow 含前置）」「宣告依賴它的 change 排到它前面被拒（must_precede）」「重疊夥伴翻轉允許」「檢查不寫檔」，再實作純函式 `plan::violations(sequence, depends_on)` 與其上的 `plan::check_rank_move(store, name, rank) -> Result<(), RankMoveBlocked>`（含 Display 一句人眼訊息；violations 另補純序列測試）。驗證：`cargo test -p speclink-core plan::` 全綠。 <!-- speclink-task:tsk_01M2J1KTEQ5W28WSPT0K4FAKCB -->
- [x] 2.4 對 plan 模組的公開 API（compute、check_rank_move、Stage、depends_on 讀取器）套用 sharp-edges 稽核清單（`speclink instructions --skill audit`）：depends_on 含空白項、重複項、指向自己的殘留值、名稱含路徑分隔符的殘留值都不得讓 compute 崩潰或成環誤報；把每一項寫成測試。驗證：`cargo test -p speclink-core plan::` 全綠。 <!-- speclink-task:tsk_01M2J1KTEQ3DAN67HXRMRJVKW4 -->

## 3. command runtime 與 CLI 動詞（D3、D4、D6）

- [x] 3.1 紅燈：新增 crates/adapters/speclink-cli/tests/it/plan_verbs.rs（於 tests/it/main.rs 註冊）覆蓋「plan 動詞輸出波次與阻擋清單」：`--json` 的 payload 含 waves、changes、next、skipped 四鍵且 changes 每項七鍵皆為 camelCase 與規格型別；`--no-color` 人眼輸出含 `Wave 1` 與 `next:` 行且無 ANSI；成環時 exit code 非零、stderr 含 `dependency cycle: a -> b -> a`、stdout 空；壞 meta 進 skipped；無 change 時 waves 為 `[]`、next 為 null。驗證：`cargo test -p speclink-cli --test it plan_verbs` 先紅。 <!-- speclink-task:tsk_01M2J1KTEQ1GX5DM45BXE8Q725 -->
- [x] 3.2 綠燈：`Command::Plan` 與 `PlanReport` 加進 crates/engine/speclink-core/src/command/mod.rs 與 typed.rs；新增 crates/adapters/speclink-cli/src/verbs/plan.rs 的 `cmd_plan`（與 `cmd_list` 同一套 observed_facts → WorktreeOverlay 組裝 store）、人眼渲染與 `--json`；於 crates/adapters/speclink-cli/src/main.rs 以 `fs_only` 宣告 `plan`（拒絕句依 D6）。驗證：3.1 全綠。 <!-- speclink-task:tsk_01M2J1KTEQZG6DPFYGPSTG509K -->
- [x] 3.3 紅燈：同檔補「change depends 動詞寫入宣告依賴」測試——寫入單一前置後 meta 多一行 `depends_on: a` 其餘位元組不變；追加後為 `depends_on: a, b`、`--remove` 後為 `depends_on: b`；移除至空整行移除；四種守門（自己、不存在、已封存、成環）皆 exit code 非零且 meta 逐位元不變；冪等重加不改檔；CRLF meta 新行以 CRLF 結尾；`--json` 回 `{ change, dependsOn }`。驗證：`cargo test -p speclink-cli --test it plan_verbs` 先紅。 <!-- speclink-task:tsk_01M2J1KTEQE773KMRKQX5E32SR -->
- [x] 3.4 綠燈：`Command::ChangeDepends { name, on, remove }` 與 `DependsOutcome { change, depends_on, changed }`，實際改動時發布 `DomainEvent::ChangeDependsChanged`（kind `change-depends-changed`）、冪等零事件（command 測試斷言事件有無）；引擎 `plan::set_depends(store, name, on, remove)` 依 D4 順序守門、單次 `edit_meta` 內逐一 `push_list`／`remove_list`；CLI 新增 `change` 頂層動詞（子指令 `depends`，旗標 `--on` 多值、`--remove`、`--json`）並以 `fs_only` 宣告。驗證：3.3 全綠。 <!-- speclink-task:tsk_01M2J1KTEQN36PACY4RV2GANWZ -->
- [x] 3.5 「模式分岔的單點宣告」：在 crates/adapters/speclink-cli/tests/it/mode_dispatch.rs 的 FsOnly 段補 `plan` 與 `change depends` 於 remote 模式且 server 不可達時 exit code 非零、stderr 含各自拒絕句、零請求。驗證：`cargo test -p speclink-cli --test it mode_dispatch` 全綠；`cargo test -p speclink-cli --test it remote_verb_parity` 不變。 <!-- speclink-task:tsk_01M2J1KTEQ7QK9NAEN937AD1VZ -->
- [x] 3.6 worktree 聚合面：沿 crates/adapters/speclink-cli/tests/it/worktree_overlay.rs 的夾具補一例——worktree 內已勾任務的 change 在主 checkout 執行 `speclink plan --json` 時 stage 為 in-progress。驗證：`cargo test -p speclink-cli --test it worktree_overlay` 全綠。 <!-- speclink-task:tsk_01M2J1KTEQS1KCY2H9WN2VGCV0 -->

## 4. 技能資產與 golden（D8）

- [x] 4.1 改寫 crates/engine/speclink-core/assets/skills/propose.md 的「Pending-change landscape check」為 D8 的流程（只判本次 change 的軟依賴 → `speclink change depends` 落檔 → `speclink plan --json` 依 worktree 政策分波呈現；作用中只有本 change 時跳過），對應規格「收尾盤點提案中變更的執行順序」。驗證：內容審閱——段落含 `speclink change depends` 與 `speclink plan --json` 字面、不再含「read each proposal's Impact and tasks」的全盤判定。 <!-- speclink-task:tsk_01M2J1KTEQ6FNJ2P4J6P2JBANZ -->
- [x] 4.2 改寫 crates/engine/speclink-core/assets/skills/apply.md 第 1 步「Select the change」為「apply 技能以 plan 挑選與守門」：先跑 `speclink plan --json`，未指名取 next（null 即列 blockedBy 並停止），指名且 blockedBy 非空即印前置並 STOP、不跑 review prepare 與 in-progress add；移除「只有一個 change 就自動選」。驗證：內容審閱——apply-with-worktree 經 B_APPLY 拼接自動帶到（4.4 的 claude-worktree golden 可見同段）。 <!-- speclink-task:tsk_01M2J1KTEQDX77E1N221G753ZC -->
- [x] 4.3 於 crates/engine/speclink-core/assets/skills/archive.md 封存前置檢查加「Plan order hint」段落，對應「archive 技能以 plan 建議封存順序」：跑 `speclink plan --json`，目標 blockedBy 非空時提醒先封存前置，明載僅建議不阻擋。驗證：內容審閱——段落含 `speclink plan --json` 與「建議、不阻擋」語意。 <!-- speclink-task:tsk_01M2J1KTEQX50RRS1ENBB5JHST -->
- [x] 4.4 crates/engine/speclink-core/src/workspace/init.rs 的 `ASSET_VERSION` 升為 v1.36.0，並同批更新 crates/engine/speclink-core/tests/golden/ 下 claude.snapshot.md、claude-worktree.snapshot.md、codex.snapshot.md 與 assets.lock。驗證：`cargo test -p speclink-core --test it render_golden::` 與 assets.lock 鎖定測試全綠；三份快照皆含 `speclink plan --json` 字面。 <!-- speclink-task:tsk_01M2J1KTEQDMFCJ663MN5VMSBH -->
- [x] 4.5 `cargo build -p speclink-cli` 後執行 `speclink update` 再生 .claude/skills 與 .agents/skills 下的 SKILL.md，並以 `git status` 確認 speclink-apply、speclink-apply-with-worktree、speclink-propose、speclink-archive 四組（claude 與 codex）皆更新；這批再生檔不進 evidence，收尾 commit 前手動帶上。驗證：`grep -l "speclink plan --json" .claude/skills/*/SKILL.md` 列出上述四份；`node --test scripts/*.test.mjs scripts/*/*.test.mjs` 全綠。 <!-- speclink-task:tsk_01M2J1KTEQMVM97J011KJ97JN9 -->
