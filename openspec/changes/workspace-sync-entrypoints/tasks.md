<!-- TDD：每個行為變化先寫紅燈測試，再改實作轉綠。本變更不動 render，golden 只當回歸對照。 -->

## 1. 探測涵蓋描述子

- [ ] 1.1 RED：在 `crates/speclink-core/src/init.rs` tests 新增四個測試——`probe_reports_missing_for_a_descriptor_without_skills`（tools 為 claude 加合法描述子 cursor／.cursor/skills，.claude/skills 現版、.cursor/skills 不存在 → status 為 missing，tools 含 `{tool: "cursor", missing: true, workspaceVersion: None}`，differingFiles 每項以 `.cursor/skills/speclink-` 起頭、以 `/SKILL.md` 結尾、不含 .claude 路徑、不含兩顆 worktree 技能）；`probe_reports_stale_for_a_descriptor_behind_the_engine`（描述子技能檔版號改舊 → stale，差異清單列其路徑）；`probe_lists_descriptor_paths_in_differing_files`（描述子與內建同時過期 → 兩邊路徑都在）；`probe_ignores_an_invalid_descriptor`（描述子缺 skills_dir → current、tools 只含 claude）。對應規格「技能檔過期探測」的三個新 scenario。驗證：`cargo test -p speclink-core init::tests::probe_` 四個紅燈（missing 案例今天回 current）。 <!-- speclink-task:tsk_01M1XH91R18F5AJ8EFMXKH7G33 -->
- [ ] 1.2 GREEN：依 design「探測涵蓋描述子：differing_files 與 probe_assets 走全部 target」改 `SyncPlan::differing_files`（移除 Builtin 過濾）與 `probe_assets`（迴圈走全部 target，`ToolAssetState.tool` 取 `target.label`），刪 doc comment 的「自訂描述子第一版不涵蓋」句。契約：`AssetProbe` serde 形狀不變，無描述子的專案輸出逐位元不變。驗證：1.1 四個測試轉綠；既有 `probe_*` 與 `skill_probe_*` 全綠；`cargo test -p speclink-desktop-core project::` 的 `probe_assets_*` 全綠；`grep -n "第一版不涵蓋" crates/speclink-core/src/init.rs` 零命中。 <!-- speclink-task:tsk_01M1XH91R1QFZW6HRA72S724K4 -->

## 2. init 與 init_remote 改走 apply

- [ ] 2.1 RED：在 `crates/speclink-core/src/init.rs` tests 新增五個測試——`init_force_switching_tools_prunes_the_deselected_footprint`（tools=[claude] 現版工作區，`init(root, &[Codex], true, "openspec")` 後 `.claude/skills` 下無 speclink- 目錄、`.claude` 目錄不存在、`.agents/skills` 含 Codex 生成集合、`.speclink.yaml` tools 僅 codex）；`init_force_prunes_a_renamed_skill_directory`（殘留 speclink-onboard，`init --force --tools claude` 後不存在）；`init_force_resets_custom_footprint_state`（先以含描述子的 `.speclink.yaml` 跑 update 生成足跡，再 `init(root, &[Claude], true, …)` → 描述子 skills_dir 下 speclink- 目錄移除、`.speclink/generated-tools.yaml` 不存在）；`init_strips_the_unselected_tools_legacy_marker`（CLAUDE.md 含遺留區塊與使用者段落，`init --force --tools codex` 後區塊剝除、段落保留、AGENTS.md 不被建立）；`init_over_residual_skill_files_rewrites_them`（無 `.speclink.yaml`、無 openspec，但 `.claude/skills/speclink-apply/SKILL.md` 為舊內容且 speclink-onboard 殘留，不帶 force 的 init 後 SKILL.md 為現版、speclink-onboard 不存在）。對應規格「update 清除孤兒技能目錄」的四個新 scenario。驗證：`cargo test -p speclink-core init::tests::init_` 五個紅燈。 <!-- speclink-task:tsk_01M1XH91R18J3HWQDZSWMRJQGN -->
- [ ] 2.2 GREEN：依 design「init 與 init_remote 改走 apply，計畫解析兩次」與「init 的寫入順序與失敗後的可觀察狀態」改 `init`／`init_remote`：守門用的計畫在 store_init 前解析，寫入用的計畫在 `.speclink.yaml` 與 gitignore 之後重新解析並呼叫 `apply(root)`（回傳丟棄）；刪 init 自己的遺留剝除迴圈；刪 `SyncPlan::write_skills`；`reconcile_builtin_tools` 與 `init` 的 doc comment 改為「五個入口共用 `SyncPlan::apply`」。契約：見 design「init --force 的清理語意與設定檔重寫不變」——`app_config_text` 不動，`InitOutcome` 不動。驗證：2.1 五個測試轉綠；既有 `init_force_over_a_legacy_workspace_strips_the_marker`、`init_force_refuses_a_workspace_that_leads_the_engine`、`reconcile_matches_init_output_for_the_same_selection`、`init_on_a_fresh_project_writes_no_instruction_file`、`init_does_not_create_the_user_settings_file`、`generation_omits_worktree_skills_when_the_policy_is_false` 全綠；`grep -n "write_skills" crates/speclink-core/src/init.rs` 零命中。 <!-- speclink-task:tsk_01M1XH91R1QBVAJFS99B8EBKSZ -->
- [ ] 2.3 CLI 與 desktop 消費端回歸（零程式碼變更，只跑測試）：契約＝空目錄 init 的 stdout、exit code 與檔案效果不變，remote init 不建 openspec、寫 remote section。驗證：`cargo test -p speclink-cli --test it init_tools` 全綠；`cargo test -p speclink-cli --test it remote_connect` 全綠；`cargo test -p speclink-desktop-core project::` 全綠（`init_project_at`／`adopt_project_at`）。 <!-- speclink-task:tsk_01M1XH91R1GV0MZK30F172AY9Z -->

## 3. 收尾

- [ ] 3.1 受影響面全跑並確認零 golden 變動：`cargo test -p speclink-core`、`cargo test -p speclink-core --test it render_golden::`（`git status --porcelain crates/speclink-core/tests/golden` 為空）、`cargo test -p speclink-cli --test it`、`cargo test -p speclink-desktop-core`；`speclink validate workspace-sync-entrypoints --strict` 通過；最後跑 `node --test scripts/*.test.mjs`。 <!-- speclink-task:tsk_01M1XH91R1X4PVZ9HYTJ34GA2C -->
